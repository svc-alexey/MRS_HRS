# Анализ функции `ПолучитьМаркиЕГАИСИз1С`

**Дата:** 10.02.2026  
**Модуль:** `CommonModules\MRS_ИнтеграцияSimphonyСервер\Ext\Module.bsl`  
**Функция:** `ПолучитьМаркиЕГАИСИз1С()` (строки 2093-2136)  
**Статус:** ⚠️ Требует доработки

---

## 1. Текущее состояние функции

### 1.1. Описание функции

Функция предназначена для получения акцизных марок ЕГАИС из 1С для синхронизации с системой Simphony Fiscal DB.

### 1.2. Обнаруженные проблемы

| № | Проблема | Строки | Критичность |
|---|----------|--------|-------------|
| 1 | **Несоответствие сигнатуры**: Функция принимает только `Номенклатура`, но вызывается с двумя параметрами | 2099, 2371 | 🔴 Критическая |
| 2 | **Параметры запроса не заполнены**: `ТекДата`, `Склад`, `Номенклатура` передаются пустыми | 2120-2122 | 🔴 Критическая |
| 3 | **Логика обработки отсутствует**: Цикл пустой, результат не формируется | 2127-2129 | 🔴 Критическая |
| 4 | **Неправильное соединение таблиц**: Соединение через `Серия.Номер` с `АкцизнаяМарка.ЗначениеШтрихкода` некорректно | 2116 | 🔴 Критическая |
| 5 | **Отсутствует возврат данных**: Возвращается пустой массив независимо от результата запроса | 2132-2134 | 🔴 Критическая |

### 1.3. Текущий код функции

```2093:2136:CommonModules/MRS_ИнтеграцияSimphonyСервер/Ext/Module.bsl
// Возвращаемое значение:
//  Массив из Структура - массив марок ЕГАИС:
//   * code - Строка - уникальный код марки ЕГАИС (обязательно)
//   * ean - Строка - штрихкод EAN (опционально)
//   * store - Строка - идентификатор склада (опционально)
//
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура)
	
	Запрос = Новый Запрос;

	Запрос.Текст =
	"ВЫБРАТЬ
	|	ТоварыНаСкладахОстатки.Номенклатура КАК Номенклатура,
	|	ТоварыНаСкладахОстатки.Склад КАК Склад,
	|	ТоварыНаСкладахОстатки.Серия.Номер КАК СерияНомер,
	|	ТоварыНаСкладахОстатки.ВНаличииОстаток КАК ВНаличииОстаток,
	|	АкцизныеМаркиЕГАИС.Статус КАК Статус
	|ИЗ
	|	РегистрНакопления.ТоварыНаСкладах.Остатки(
	|			&ТекДата,
	|			Номенклатура В (&Номенклатура)
	|				И Склад В (&Склад)) КАК ТоварыНаСкладахОстатки
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
	|		ПО (ТоварыНаСкладахОстатки.Серия.Номер = АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода)
	|ГДЕ
	|	ТоварыНаСкладахОстатки.ВНаличииОстаток > 0";

	Запрос.УстановитьПараметр("ТекДата", );
	Запрос.УстановитьПараметр("Склад",);
	Запрос.УстановитьПараметр("Номенклатура", );
	
	РезультатЗапроса = Запрос.Выполнить();

	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		// Код
	КонецЦикла;
	
	// 
	Результат = Новый Массив;
	
	Возврат Результат;
	
КонецФункции
```

---

## 2. Контекст использования

### 2.1. Место вызова функции

**Функция:** `СинхронизироватьМаркиЕГАИС()` (строка 2371)

```2369:2372:CommonModules/MRS_ИнтеграцияSimphonyСервер/Ext/Module.bsl
// Шаг 2: Получить марки из 1С
ДанныеПозиции = ПолучитьДанныеПозицииЕГАИС(Позиция);
Марки1С = ПолучитьМаркиЕГАИСИз1С(ДанныеПозиции.Номенклатура, ДанныеПозиции.Характеристика);
```

**Проблема:** Функция вызывается с **двумя параметрами** (Номенклатура, Характеристика), но в определении функции принимается только **один параметр** (Номенклатура).

### 2.2. Функция получения данных позиции

**Функция:** `ПолучитьДанныеПозицииЕГАИС()` (строки 1950-1982)

```1950:1982:CommonModules/MRS_ИнтеграцияSimphonyСервер/Ext/Module.bsl
Функция ПолучитьДанныеПозицииЕГАИС(Позиция)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	питМеню.MRS_КодSimphony КАК ObjNum,
		|	питМеню.Номенклатура.ОбъемДАЛ КАК ОбъемДал,
		|	ПРЕДСТАВЛЕНИЕ(питМеню.Номенклатура.ЕдиницаИзмерения) КАК ЕдиницаИзмерения,
		|	питМеню.Номенклатура КАК Номенклатура
		|ИЗ
		|	Справочник.питМеню КАК питМеню
		|ГДЕ
		|	питМеню.Ссылка = &Позиция";
	
	Запрос.УстановитьПараметр("Позиция", Позиция);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Новый Структура;
	Результат.Вставить("ObjNum", 0);
	Результат.Вставить("ОбъемДал", 0);
	Результат.Вставить("ЕдиницаИзмерения", "");
	Результат.Вставить("Номенклатура", Неопределено);
	
	Если Выборка.Следующий() Тогда
		Результат.ObjNum = Выборка.ObjNum;
		Результат.ОбъемДал = Выборка.ОбъемДал;
		Результат.ЕдиницаИзмерения = Выборка.ЕдиницаИзмерения;
		Результат.Номенклатура = Выборка.Номенклатура;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции
```

**Проблема:** Функция `ПолучитьДанныеПозицииЕГАИС()` **НЕ возвращает** поле `Характеристика`, но при вызове `ПолучитьМаркиЕГАИСИз1С()` пытается передать `ДанныеПозиции.Характеристика`.

### 2.3. Бизнес-процесс синхронизации марок

**Последовательность операций:**

1. Получить марки из Simphony (функция `ПолучитьМаркиЕГАИСИзSimphony()`)
2. **Получить марки из 1С** (функция `ПолучитьМаркиЕГАИСИз1С()`) ← **ТЕКУЩАЯ ПРОБЛЕМА**
3. Вычислить расхождения (функция `ВычислитьРазницуМарокЕГАИС()`)
4. Удалить устаревшие марки из Simphony (DELETE запросы)
5. Добавить новые марки в Simphony (POST batch запрос)

---

## 3. Структура данных конфигурации

### 3.1. РегистрСведений.АкцизныеМаркиЕГАИС

**Ключевые поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `АкцизнаяМарка` | СправочникСсылка.ШтрихкодыУпаковокТоваров | Ссылка на справочник марок |
| `АкцизнаяМарка.ЗначениеШтрихкода` | Строка | **Код марки ЕГАИС** (это и есть `code`) |
| `Статус` | Перечисление | Статус марки (в наличии/продано/списано) |
| `АлкогольнаяПродукция` | СправочникСсылка.Номенклатура | Номенклатура алкогольной продукции |

**Источник данных:** Найдено в модуле `MRS_ЗагрузкаЕГАИСФронт` (строки 616, 665, 839, 1417, 1664).

### 3.2. Справочник.ШтрихкодыУпаковокТоваров

**Поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `ЗначениеШтрихкода` | Строка | Штрихкод марки (код марки ЕГАИС) |
| `Серия` | СправочникСсылка.СерииНоменклатуры | Серия номенклатуры |

### 3.3. Справочник.СерииНоменклатуры

**Поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `Номер` | Строка | Номер серии |

### 3.4. РегистрНакопления.ТоварыНаСкладах

**Измерения:**

| Поле | Тип | Описание |
|------|-----|----------|
| `Номенклатура` | СправочникСсылка.Номенклатура | Номенклатура |
| `Характеристика` | СправочникСсылка.ХарактеристикиНоменклатуры | Характеристика |
| `Серия` | СправочникСсылка.СерииНоменклатуры | Серия |
| `Склад` | СправочникСсылка.Склады | Склад |

**Ресурсы:**

| Поле | Тип | Описание |
|------|-----|----------|
| `ВНаличииОстаток` | Число | Остаток в наличии |

---

## 4. Архитектурный анализ

### 4.1. Логика запроса (текущая vs правильная)

#### Текущая логика запроса (НЕПРАВИЛЬНАЯ)

```
ТоварыНаСкладах.Остатки(ТекДата, Номенклатура И Склад)
  ЛЕВОЕ СОЕДИНЕНИЕ АкцизныеМаркиЕГАИС
    ПО Серия.Номер = АкцизнаяМарка.ЗначениеШтрихкода
```

**Проблемы:**

1. ❌ Соединение по `Серия.Номер = АкцизнаяМарка.ЗначениеШтрихкода` **некорректно**
   - `Серия.Номер` - это номер серии (например, "123456")
   - `АкцизнаяМарка.ЗначениеШтрихкода` - это код акцизной марки ЕГАИС (68 символов)
   - Эти поля **не связаны напрямую**

2. ❌ Запрос возвращает **остатки товаров**, а не **марки**
   - Одна номенклатура может иметь много марок
   - Остаток товаров ≠ количество марок

3. ❌ Параметры не заполнены

#### Правильная логика запроса

**Вариант 1: Через РегистрСведений.АкцизныеМаркиЕГАИС**

```sql
ВЫБРАТЬ
	АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода КАК code,
	ЕСТЬNULL(АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода, "") КАК ean,
	"" КАК store
ИЗ
	РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
ГДЕ
	АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = &Номенклатура
	И АкцизныеМаркиЕГАИС.Статус = &СтатусВНаличии
```

**Вариант 2: С учётом остатков через соединение (более сложный)**

```sql
ВЫБРАТЬ
	АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода КАК code,
	ЕСТЬNULL(АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода, "") КАК ean,
	ЕСТЬNULL(ТоварыНаСкладахОстатки.Склад.Код, "") КАК store
ИЗ
	РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
		ЛЕВОЕ СОЕДИНЕНИЕ РегистрНакопления.ТоварыНаСкладах.Остатки(
			&ТекДата,
			Номенклатура = &Номенклатура) КАК ТоварыНаСкладахОстатки
		ПО АкцизныеМаркиЕГАИС.АкцизнаяМарка.Серия = ТоварыНаСкладахОстатки.Серия
			И АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = ТоварыНаСкладахОстатки.Номенклатура
ГДЕ
	АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = &Номенклатура
	И АкцизныеМаркиЕГАИС.Статус = &СтатусВНаличии
	И ТоварыНаСкладахОстатки.ВНаличииОстаток > 0
```

### 4.2. Анализ примеров из других модулей

**Источник:** `MRS_ЗагрузкаЕГАИСФронт.bsl` (строки 1475-1478)

```sql
ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.СерииНоменклатуры КАК СерииНоменклатуры
	ПО ОстаткиМарокИСОстатки.Штрихкод.ЗначениеШтрихкода = СерииНоменклатуры.Номер
ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
	ПО ОстаткиМарокИСОстатки.Штрихкод.ЗначениеШтрихкода = АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода
```

**Вывод:** В других модулях соединение идет через **одно поле** `ЗначениеШтрихкода`, а не через разные таблицы.

---

## 5. Рекомендуемое решение

### 5.1. Правильная сигнатура функции

```bsl
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика = Неопределено)
```

**Обоснование:**
- Характеристика передается при вызове функции (строка 2371)
- Параметр должен быть опциональным (может быть пустой ссылкой)
- Характеристика используется для фильтрации марок, если заполнена

### 5.2. Структура возвращаемых данных

**Массив структур:**

```bsl
Структура:
  * code  - Строка - код акцизной марки ЕГАИС (из АкцизнаяМарка.ЗначениеШтрихкода)
  * ean   - Строка - штрихкод EAN (тот же код марки, т.к. марка и есть штрихкод)
  * store - Строка - идентификатор склада (код склада или пустая строка)
```

**Важно:** Поле `code` - это **обязательное** уникальное значение марки ЕГАИС.

### 5.3. Логика заполнения параметров запроса

| Параметр | Источник | Логика |
|----------|----------|--------|
| `ТекДата` | `ТекущаяДатаСеанса()` | Текущая дата для получения актуальных остатков |
| `Номенклатура` | Параметр функции | Обязательный параметр |
| `Характеристика` | Параметр функции | Опциональный параметр (если не задана, не фильтруем) |
| `СтатусВНаличии` | Константа/Перечисление | Статус "В наличии" для фильтрации марок |

### 5.4. Рекомендуемая реализация функции

**Вариант 1: Упрощенный (только по номенклатуре)**

```bsl
// Получает марки ЕГАИС из 1С для синхронизации с Simphony
//
// Параметры:
//  Номенклатура - СправочникСсылка.Номенклатура - номенклатура алкогольной продукции
//  Характеристика - СправочникСсылка.ХарактеристикиНоменклатуры - характеристика (опционально)
//
// Возвращаемое значение:
//  Массив из Структура - массив марок ЕГАИС:
//   * code - Строка - уникальный код марки ЕГАИС (обязательно)
//   * ean - Строка - штрихкод EAN (опционально)
//   * store - Строка - идентификатор склада (опционально)
//
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика = Неопределено)
	
	Результат = Новый Массив;
	
	Если НЕ ЗначениеЗаполнено(Номенклатура) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода КАК code
		|ИЗ
		|	РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
		|ГДЕ
		|	АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = &Номенклатура
		|	И АкцизныеМаркиЕГАИС.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыАкцизныхМарокЕГАИС.ВНаличии)
		|	И НЕ АкцизныеМаркиЕГАИС.АкцизнаяМарка.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Если НЕ ЗначениеЗаполнено(Выборка.code) Тогда
			Продолжить;
		КонецЕсли;
		
		Марка = Новый Структура;
		Марка.Вставить("code", Выборка.code);
		Марка.Вставить("ean", Выборка.code); // Для ЕГАИС code = ean
		Марка.Вставить("store", ""); // Склад пока не определяем
		
		Результат.Добавить(Марка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции
```

**Вариант 2: С учетом остатков на складе (более корректный)**

```bsl
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика = Неопределено)
	
	Результат = Новый Массив;
	
	Если НЕ ЗначениеЗаполнено(Номенклатура) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	АкцизныеМаркиЕГАИС.АкцизнаяМарка.ЗначениеШтрихкода КАК code,
		|	ЕСТЬNULL(ТоварыНаСкладахОстатки.Склад.Код, """") КАК store
		|ИЗ
		|	РегистрСведений.АкцизныеМаркиЕГАИС КАК АкцизныеМаркиЕГАИС
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрНакопления.ТоварыНаСкладах.Остатки(
		|			&ТекДата,
		|			Номенклатура = &Номенклатура
		|				И &УсловиеХарактеристика) КАК ТоварыНаСкладахОстатки
		|		ПО АкцизныеМаркиЕГАИС.АкцизнаяМарка.Серия = ТоварыНаСкладахОстатки.Серия
		|			И АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = ТоварыНаСкладахОстатки.Номенклатура
		|ГДЕ
		|	АкцизныеМаркиЕГАИС.АлкогольнаяПродукция = &Номенклатура
		|	И АкцизныеМаркиЕГАИС.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыАкцизныхМарокЕГАИС.ВНаличии)
		|	И НЕ АкцизныеМаркиЕГАИС.АкцизнаяМарка.ПометкаУдаления
		|	И (ТоварыНаСкладахОстатки.ВНаличииОстаток > 0
		|		ИЛИ ТоварыНаСкладахОстатки.ВНаличииОстаток ЕСТЬ NULL)";
	
	Запрос.УстановитьПараметр("ТекДата", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	
	// Условие для характеристики
	Если ЗначениеЗаполнено(Характеристика) Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&УсловиеХарактеристика", "Характеристика = &Характеристика");
		Запрос.УстановитьПараметр("Характеристика", Характеристика);
	Иначе
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&УсловиеХарактеристика", "ИСТИНА");
	КонецЕсли;
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Если НЕ ЗначениеЗаполнено(Выборка.code) Тогда
			Продолжить;
		КонецЕсли;
		
		Марка = Новый Структура;
		Марка.Вставить("code", Выборка.code);
		Марка.Вставить("ean", Выборка.code); // Для ЕГАИС code = ean
		Марка.Вставить("store", ?(ЗначениеЗаполнено(Выборка.store), Выборка.store, ""));
		
		Результат.Добавить(Марка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции
```

---

## 6. Дополнительные проблемы в связанных функциях

### 6.1. Функция `ПолучитьДанныеПозицииЕГАИС()`

**Проблема:** Функция НЕ возвращает поле `Характеристика`, хотя оно передается при вызове.

**Рекомендация:** Добавить в возвращаемую структуру поле `Характеристика`.

**Исправленная функция:**

```bsl
Функция ПолучитьДанныеПозицииЕГАИС(Позиция)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	питМеню.MRS_КодSimphony КАК ObjNum,
		|	питМеню.Номенклатура.ОбъемДАЛ КАК ОбъемДал,
		|	ПРЕДСТАВЛЕНИЕ(питМеню.Номенклатура.ЕдиницаИзмерения) КАК ЕдиницаИзмерения,
		|	питМеню.Номенклатура КАК Номенклатура,
		|	питМеню.Номенклатура.Характеристика КАК Характеристика
		|ИЗ
		|	Справочник.питМеню КАК питМеню
		|ГДЕ
		|	питМеню.Ссылка = &Позиция";
	
	Запрос.УстановитьПараметр("Позиция", Позиция);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Новый Структура;
	Результат.Вставить("ObjNum", 0);
	Результат.Вставить("ОбъемДал", 0);
	Результат.Вставить("ЕдиницаИзмерения", "");
	Результат.Вставить("Номенклатура", Неопределено);
	Результат.Вставить("Характеристика", Неопределено); // ДОБАВЛЕНО
	
	Если Выборка.Следующий() Тогда
		Результат.ObjNum = Выборка.ObjNum;
		Результат.ОбъемДал = Выборка.ОбъемДал;
		Результат.ЕдиницаИзмерения = Выборка.ЕдиницаИзмерения;
		Результат.Номенклатура = Выборка.Номенклатура;
		Результат.Характеристика = Выборка.Характеристика; // ДОБАВЛЕНО
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции
```

---

## 7. Бизнес-логика обработки результатов

### 7.1. Алгоритм синхронизации марок

**Шаги после получения марок из 1С:**

1. **Получение марок из Simphony** → Массив структур `МаркиSimphony`
2. **Получение марок из 1С** → Массив структур `Марки1С`
3. **Вычисление расхождений** → Структура `Расхождения`:
   - `НаДобавление` - марки, которые есть в 1С, но нет в Simphony
   - `НаУдаление` - марки, которые есть в Simphony, но нет в 1С

4. **Удаление марок из Simphony**:
   ```
   Для каждой марки из НаУдаление:
     DELETE /rvc/{rvcId}/egais/marks/{code}
   ```

5. **Добавление марок в Simphony**:
   ```
   POST /rvc/{rvcId}/egais/marks/batch
   Body: [
     { "objnum": 123, "code": "марка1", "ean": "марка1", "store": "01" },
     { "objnum": 123, "code": "марка2", "ean": "марка2", "store": "02" }
   ]
   ```

### 7.2. Формат данных для Simphony API

**Endpoint добавления марок:** `POST /rvc/{rvcId}/egais/marks/batch`

**Формат запроса:**

```json
[
  {
    "objnum": 123456,           // Код позиции Simphony
    "code": "0104601234567890", // Код акцизной марки ЕГАИС (обязательно)
    "ean": "0104601234567890",  // Штрихкод (обычно совпадает с code)
    "store": "01"               // Код склада (опционально)
  }
]
```

### 7.3. Статусы марок ЕГАИС

| Статус | Описание | Синхронизировать? |
|--------|----------|-------------------|
| **В наличии** | Марка доступна для продажи | ✅ Да |
| Продано | Марка уже продана | ❌ Нет |
| Списано | Марка списана | ❌ Нет |
| Утилизировано | Марка утилизирована | ❌ Нет |

**Вывод:** Синхронизируем только марки со статусом **"В наличии"**.

---

## 8. Архитектурные рекомендации

### 8.1. Производительность

**Проблема:** Если марок много (> 1000), запрос может работать медленно.

**Рекомендации:**

1. **Индексирование:**
   - Проверить наличие индекса по полю `АлкогольнаяПродукция` в регистре `АкцизныеМаркиЕГАИС`
   - Проверить индекс по полю `Статус`

2. **Ограничение количества:**
   - Добавить параметр `ТОП 1000` в запрос при необходимости
   - Логировать случаи, когда марок больше 1000

3. **Кеширование:**
   - Рассмотреть возможность кеширования результата на время сессии
   - Использовать `Соответствие` для хранения уже полученных марок

### 8.2. Обработка ошибок

**Рекомендации:**

1. **Валидация входных параметров:**
   ```bsl
   Если НЕ ЗначениеЗаполнено(Номенклатура) Тогда
       ЗаписьЖурналаРегистрации("MRS.Simphony.EGAIS", 
           УровеньЖурналаРегистрации.Предупреждение,,,
           "ПолучитьМаркиЕГАИСИз1С: Не заполнена номенклатура");
       Возврат Новый Массив;
   КонецЕсли;
   ```

2. **Логирование количества марок:**
   ```bsl
   ЗаписьЖурналаРегистрации("MRS.Simphony.EGAIS", 
       УровеньЖурналаРегистрации.Информация,,,
       СтрШаблон("Получено марок ЕГАИС для номенклатуры %1: %2 шт.", 
           Номенклатура, Результат.Количество()));
   ```

3. **Обработка исключений:**
   ```bsl
   Попытка
       РезультатЗапроса = Запрос.Выполнить();
   Исключение
       ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
       ЗаписьЖурналаРегистрации("MRS.Simphony.EGAIS", 
           УровеньЖурналаРегистрации.Ошибка,,,
           СтрШаблон("Ошибка получения марок ЕГАИС: %1", ТекстОшибки));
       Возврат Новый Массив;
   КонецПопытки;
   ```

### 8.3. Тестирование

**Сценарии тестирования:**

1. **Номенклатура без марок:**
   - Входные данные: Номенклатура, у которой нет марок в регистре
   - Ожидаемый результат: Пустой массив

2. **Номенклатура с 1 маркой:**
   - Входные данные: Номенклатура с одной маркой
   - Ожидаемый результат: Массив с 1 элементом

3. **Номенклатура с множественными марками:**
   - Входные данные: Номенклатура с 10+ марками
   - Ожидаемый результат: Массив с 10+ элементами

4. **Марки с разными статусами:**
   - Входные данные: Номенклатура с марками в статусах "В наличии", "Продано"
   - Ожидаемый результат: Только марки со статусом "В наличии"

5. **Пустая номенклатура:**
   - Входные данные: Пустая ссылка на номенклатуру
   - Ожидаемый результат: Пустой массив, без ошибок

---

## 9. Вопросы для уточнения с заказчиком

| № | Вопрос | Зачем нужно |
|---|--------|-------------|
| 1 | **Используется ли характеристика в справочнике питМеню?** | Определить, нужно ли добавлять характеристику в запрос |
| 2 | **Какой статус у марок в перечислении `СтатусыАкцизныхМарокЕГАИС`?** | Правильно указать статус в фильтре запроса |
| 3 | **Нужно ли учитывать склад при синхронизации марок?** | Определить, заполнять ли поле `store` |
| 4 | **Есть ли ограничение на количество марок для одной позиции?** | Понять, нужна ли пагинация или ограничение ТОП |
| 5 | **Как связаны марки с остатками на складе?** | Понять, нужна ли проверка остатков или достаточно статуса марки |
| 6 | **Могут ли быть марки без привязки к серии?** | Определить, нужна ли проверка на заполненность серии |

---

## 10. План реализации

### Этап 1: Исправление сигнатуры и базовой логики (2-3 часа)

✅ **Задачи:**
1. Исправить сигнатуру функции `ПолучитьМаркиЕГАИСИз1С()` - добавить параметр `Характеристика`
2. Добавить поле `Характеристика` в функцию `ПолучитьДанныеПозицииЕГАИС()`
3. Реализовать упрощенный запрос к регистру `АкцизныеМаркиЕГАИС` (Вариант 1)
4. Реализовать цикл формирования результата
5. Добавить базовую валидацию и логирование

### Этап 2: Уточнение бизнес-логики (1-2 часа)

✅ **Задачи:**
1. Уточнить у заказчика вопросы из раздела 9
2. Определить точное название перечисления статусов марок
3. Проверить структуру регистра `АкцизныеМаркиЕГАИС` в конфигурации

### Этап 3: Оптимизация запроса (2-3 часа)

✅ **Задачи:**
1. Реализовать запрос с учетом остатков на складе (Вариант 2), если требуется
2. Добавить фильтрацию по характеристике, если используется
3. Оптимизировать запрос (индексы, план запроса)

### Этап 4: Тестирование (2-3 часа)

✅ **Задачи:**
1. Создать тестовые данные (номенклатура с марками)
2. Протестировать все сценарии из раздела 8.3
3. Проверить интеграцию с функцией `СинхронизироватьМаркиЕГАИС()`
4. Провести нагрузочное тестирование (100+ марок)

### Этап 5: Документация и деплой (1 час)

✅ **Задачи:**
1. Обновить комментарии к функциям
2. Записать результаты тестирования
3. Задеплоить изменения в тестовую среду
4. Провести приемочное тестирование с заказчиком

**Общая оценка:** 8-12 часов работы

---

## 11. Критерии готовности

- ✅ Функция `ПолучитьМаркиЕГАИСИз1С()` принимает правильные параметры
- ✅ Запрос корректно соединяет таблицы через правильные поля
- ✅ Параметры запроса заполнены корректно
- ✅ Цикл обработки результатов реализован
- ✅ Возвращается массив структур с полями `code`, `ean`, `store`
- ✅ Функция `ПолучитьДанныеПозицииЕГАИС()` возвращает `Характеристика`
- ✅ Добавлена валидация входных параметров
- ✅ Добавлено логирование
- ✅ Все тестовые сценарии пройдены успешно
- ✅ Интеграция с `СинхронизироватьМаркиЕГАИС()` работает корректно

---

## 12. Связанные документы

- **Спецификация:** `ii_docs/SPEC_EGAIS_Integration_v1.0.md`
- **TODO:** `ii_docs/TODO_Реализация_запроса_марок_ЕГАИС.md`
- **Модуль загрузки ЕГАИС:** `CommonModules/MRS_ЗагрузкаЕГАИСФронт/Ext/Module.bsl`

---

**Автор анализа:** AI Business Analyst  
**Дата:** 10.02.2026  
**Статус:** ✅ Готово к реализации
