# TODO: Реализация запроса марок ЕГАИС

**Статус:** ⚠️ Требует доработки  
**Приоритет:** Средний  
**Модуль:** `MRS_ИнтеграцияSimphonyСервер`  
**Функция:** `ПолучитьМаркиЕГАИСИз1С()`

---

## 📋 Что нужно сделать

Реализовать запрос к источнику марок ЕГАИС в конфигурации 1С для получения актуальных акцизных марок алкогольной продукции.

---

## 🔍 Где находится заглушка

**Файл:** `CommonModules\MRS_ИнтеграцияSimphonyСервер\Ext\Module.bsl`

**Функция:** `ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика)`

**Строки:** ~210-250 (ориентировочно, после функции `ПолучитьШтрихкодыИз1С`)

---

## 📝 Требования к реализации

### Входные параметры
- `Номенклатура` - СправочникСсылка.Номенклатура
- `Характеристика` - СправочникСсылка.ХарактеристикиНоменклатуры (может быть пустой ссылкой)

### Возвращаемое значение
**Массив из Структура**, где каждая структура содержит:

| Поле | Тип | Обязательность | Описание |
|------|-----|----------------|----------|
| `code` | Строка | **Обязательно** | Уникальный код акцизной марки ЕГАИС |
| `ean` | Строка | Опционально | Штрихкод EAN, связанный с маркой (может быть пустой) |
| `store` | Строка | Опционально | Идентификатор склада (может быть пустой) |

### Требования к данным

1. **Статус марок:**
   - Выбирать только марки в статусе "В наличии" или аналогичном
   - Не выбирать проданные/списанные марки

2. **Фильтрация:**
   - По номенклатуре (обязательно)
   - По характеристике (если заполнена)
   - По актуальности/статусу марки

---

## 💡 Примеры реализации

### Вариант 1: Справочник с марками

```bsl
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	МаркиЕГАИС.КодМарки КАК code,
		|	МаркиЕГАИС.Штрихкод КАК ean,
		|	МаркиЕГАИС.Склад.Код КАК store
		|ИЗ
		|	Справочник.МаркиЕГАИС КАК МаркиЕГАИС
		|ГДЕ
		|	МаркиЕГАИС.Номенклатура = &Номенклатура
		|	И МаркиЕГАИС.Характеристика = &Характеристика
		|	И МаркиЕГАИС.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыМарокЕГАИС.ВНаличии)
		|	И НЕ МаркиЕГАИС.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	Запрос.УстановитьПараметр("Характеристика", Характеристика);
	
	Результат = Новый Массив;
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Марка = Новый Структура;
		Марка.Вставить("code", Выборка.code);
		Марка.Вставить("ean", ?(ЗначениеЗаполнено(Выборка.ean), Выборка.ean, ""));
		Марка.Вставить("store", ?(ЗначениеЗаполнено(Выборка.store), Выборка.store, ""));
		Результат.Добавить(Марка);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции
```

### Вариант 2: Регистр сведений

```bsl
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	МаркиНоменклатуры.КодМарки КАК code,
		|	МаркиНоменклатуры.ШтрихкодEAN КАК ean,
		|	МаркиНоменклатуры.Склад КАК store
		|ИЗ
		|	РегистрСведений.МаркиНоменклатуры КАК МаркиНоменклатуры
		|ГДЕ
		|	МаркиНоменклатуры.Номенклатура = &Номенклатура
		|	И МаркиНоменклатуры.Характеристика = &Характеристика
		|	И МаркиНоменклатуры.Актуальна = ИСТИНА";
	
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	Запрос.УстановитьПараметр("Характеристика", Характеристика);
	
	Результат = Новый Массив;
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Марка = Новый Структура;
		Марка.Вставить("code", Выборка.code);
		Марка.Вставить("ean", ?(ЗначениеЗаполнено(Выборка.ean), Выборка.ean, ""));
		Марка.Вставить("store", ?(ЗначениеЗаполнено(Выборка.store), Выборка.store, ""));
		Результат.Добавить(Марка);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции
```

### Вариант 3: Из документов поступления

```bsl
Функция ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ОстаткиМарок.КодМарки КАК code,
		|	ЕСТЬNULL(ОстаткиМарок.ШтрихкодEAN, """") КАК ean,
		|	ЕСТЬNULL(ОстаткиМарок.Склад.Код, """") КАК store
		|ИЗ
		|	РегистрНакопления.ОстаткиМарокЕГАИС.Остатки(&Дата, 
		|		Номенклатура = &Номенклатура 
		|		И Характеристика = &Характеристика) КАК ОстаткиМарок
		|ГДЕ
		|	ОстаткиМарок.КоличествоОстаток > 0";
	
	Запрос.УстановитьПараметр("Дата", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	Запрос.УстановитьПараметр("Характеристика", Характеристика);
	
	Результат = Новый Массив;
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Марка = Новый Структура;
		Марка.Вставить("code", Выборка.code);
		Марка.Вставить("ean", Выборка.ean);
		Марка.Вставить("store", Выборка.store);
		Результат.Добавить(Марка);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции
```

---

## ✅ Критерии готовности

- [ ] Определен источник марок ЕГАИС в конфигурации
- [ ] Реализован SQL запрос к источнику
- [ ] Запрос возвращает массив структур с полями code, ean, store
- [ ] Проверена работа на тестовых данных
- [ ] Марки корректно синхронизируются с Simphony
- [ ] Логирование работает (операция EGAIS_MARKS)

---

## 🧪 Тестирование

После реализации проверить:

1. **Получение марок из 1С:**
   ```bsl
   Марки = MRS_ИнтеграцияSimphonyСервер.ПолучитьМаркиЕГАИСИз1С(Номенклатура, Характеристика);
   Сообщить("Количество марок: " + Марки.Количество());
   Для Каждого Марка Из Марки Цикл
       Сообщить("Code: " + Марка.code + ", EAN: " + Марка.ean);
   КонецЦикла;
   ```

2. **Полная синхронизация:**
   - Выгрузить алкогольную позицию меню
   - Проверить историю синхронизации
   - Убедиться, что марки добавлены в Simphony

3. **Проверка в Simphony:**
   ```
   GET /rvc/{rvcId}/egais/marks/byobjnum/{objnum}
   ```

---

## 📚 Связанные документы

- **Спецификация:** `ii_docs/SPEC_EGAIS_Integration_v1.0.md`
- **API документация:** `ii_docs/api-merged.yaml` (раздел "EGAIS Marks", строки 1211-1413)
- **Инструкция администратора:** `ii_docs/ADMIN_MANUAL_EGAIS_Integration.md` (раздел 2.3)

---

## 💬 Вопросы для уточнения

1. Где в конфигурации хранятся марки ЕГАИС?
   - Справочник
   - Регистр сведений
   - Регистр накопления (остатки)
   - Документы

2. Какие статусы марок нужно учитывать?
   - В наличии
   - Проданные (исключать)
   - Списанные (исключать)

3. Связаны ли марки с конкретным складом?
   - Да → включить поле store
   - Нет → оставить пустым

4. Есть ли связь между маркой и штрихкодом EAN?
   - Да → заполнять поле ean
   - Нет → оставить пустым

---

**Автор:** AI Assistant  
**Дата создания:** 05.02.2026
