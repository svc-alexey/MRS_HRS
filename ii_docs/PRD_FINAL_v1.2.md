# PRD: Интеграция управления меню 1С:ERP 2.5 ↔ Oracle Simphony POS

**Версия документа:** 1.5
**Дата:** 05.02.2026
**Автор:** Product Manager + Senior 1C Developer + 1C Analyst
**Статус:** ✅ Draft (Updated for EGAIS)

---

## 📋 Краткое содержание

**Цель:** Автоматизированная синхронизация меню точек продаж между 1С:ERP 2.5 (модуль Общепит) и Oracle Simphony POS через REST API, включая интеграцию с фискальной базой данных (ЕГАИС) для алкогольной продукции.

**Ключевые изменения v1.5 (по сравнению с v1.4):**
- ✅ **Интеграция с ЕГАИС (Simphony Fiscal DB):** Добавлена логика синхронизации параметров алкогольной продукции.
- ✅ **Синхронизация штрихкодов:** Механизм обновления штрихкодов для позиций меню в фискальной базе.
- ✅ **Расширение Flow 1:** Добавлен подпроцесс обновления данных ЕГАИС после основной синхронизации меню.
- ✅ **Новые метаданные:** Добавлены реквизиты для маппинга полей ЕГАИС (`marktype`, `groupid`, `cocktid`).

**Ключевые изменения v1.4 (по сравнению с v1.3):**
- 🔄 **Упрощение архитектуры:** Отказ от дополнительных реквизитов для хранения цен и статусов. Вся информация получается динамически из регистров сведений.
- 🔄 **Иерархия классификаторов:** Использование существующей структуры `ГруппыАналитическогоУчетаНоменклатуры` для хранения Major/Family Groups.
- ✅ **Ручная выгрузка меню:** Добавлена команда для выгрузки всех позиций выбранного вида меню.

---

## 1. Context & Goals

### Проблема (AS-IS)

- Меню точек продаж управляется вручную в двух системах.
- **Алкогольная продукция:** Требует двойного ввода данных (в Simphony и в настройки фискализации/ЕГАИС внутри Simphony).
- **Штрихкоды:** Не синхронизируются, что затрудняет продажу сканированием и корректное списание в ЕГАИС.
- Высокий риск ошибок при ручном вводе параметров алкоголя (объем, коды видов продукции), что критично для фискального учета.

### Цели (Objectives)

1. **Исключить ручной ввод** данных меню и параметров ЕГАИС в Simphony.
2. **Обеспечить консистентность** алкогольных признаков и штрихкодов.
3. **Автоматизировать привязку штрихкодов** из 1С к позициям в Simphony Fiscal DB.
4. **Сократить время** заведения новой алкогольной позиции (с 15 мин до 2 мин).

### Non-Goals / Out of Scope

- ❌ Отправка актов списания ЕГАИС из 1С (делается на стороне Simphony или отдельным процессом).
- ❌ Управление остатками ЕГАИС в Simphony через этот интерфейс.

---

## 2. Core Functions

### Основные функции системы

1. **Создание и обновление позиций меню** (Basic Menu Item Sync).
2. **Синхронизация данных ЕГАИС** для алкогольных позиций (Update Egais Attributes).
3. **Синхронизация штрихкодов** (Barcode Sync): добавление новых, удаление устаревших.
4. **Постановка в очередь и массовая выгрузка** (существующий функционал).
5. **Отслеживание статусов** (расширено статусами ЕГАИС, если применимо).

---

## 3. Flows (Детальная логика)

### Flow 1: Создание/изменение позиции меню (Обновленный)

**Актор:** Менеджер меню
**Предусловия:**
- Стандартные предусловия Flow 1.
- Для алкоголя: Заполнена карточка номенклатуры (Вид алкогольной продукции, объем, штрихкоды).

**Основной поток:**

1. Пользователь создает/изменяет позицию в **питМеню**.
2. При сохранении срабатывает стандартная валидация и постановка в очередь (`MRS_ОчередьВыгрузкиМенюSimphony`).
3. **Обработка очередью (или ручная выгрузка):**
    a. Система выполняет `PUT/PATCH /menu-item` (Базовая синхронизация).
    b. Если ответ API успешный (200 OK), система проверяет признак алкогольной продукции:
       - **Критерий:** Заполнен реквизит `Номенклатура.ВидАлкогольнойПродукции` (или `ПодакцизныйТовар` = Истина).
    c. **Если товар алкогольный (Sub-flow EGAIS):**
       1. **Обновление атрибутов ЕГАИС:**
          - Формирует `UpdateMenuItemEgaisDto`.
          - Выполняет `PUT /rvc/{rvcId}/egais/menuitems/egais`.
       2. **Синхронизация штрихкодов:**
          - Получает текущие штрихкоды из Simphony: `GET /rvc/{rvcId}/egais/barcodes/{objnum}`.
          - Получает актуальные штрихкоды из 1С (`РегистрСведений.ШтрихкодыНоменклатуры`).
          - Вычисляет расхождения (Diff):
            - **Удалить:** Штрихкоды, которые есть в Simphony, но нет в 1С.
            - **Добавить:** Штрихкоды, которые есть в 1С, но нет в Simphony.
          - Выполняет удаление: `DELETE /rvc/{rvcId}/egais/barcodes/{objnum}/{barcode}` для каждого удаляемого.
          - Выполняет добавление: `POST /rvc/{rvcId}/egais/barcodes/batch` для списка добавляемых.
    d. Результат всех операций пишется в историю синхронизации.

**Постусловия:** Позиция обновлена в Simphony, для алкоголя проставлены признаки ЕГАИС и актуализированы штрихкоды.

---

## 4. Data & Integrations

### Новые сущности и реквизиты 1С

#### ✅ Справочник: ВидыАлкогольнойПродукции (или Номенклатура)

*Рекомендуется добавлять в `ВидыАлкогольнойПродукции`, так как эти параметры общие для вида. Если справочник типовой и на поддержке, можно добавить в `Номенклатура` или через Дополнительные Реквизиты.*

| Реквизит | Тип | Имя поля 1С (Предлагаемое) | Назначение |
|---|---|---|---|
| **Mark Type** | Число (5,0) | `MRS_ТипМаркировкиSimphony` | Код типа маркировки для Simphony (`marktype`). |
| **Group ID** | Число (10,0) | `MRS_КодГруппыEGAIS` | ID группы алкогольной продукции в Simphony (`groupid`). |

#### ✅ Справочник: Номенклатура

| Реквизит | Тип | Имя поля 1С (Предлагаемое) | Назначение |
|---|---|---|---|
| **Cocktail ID** | Число (10,0) | `MRS_КодКоктейляEGAIS` | ID коктейля для ЕГАИС (если применимо) (`cocktid`). |

---

### Маппинг данных: 1С ↔ Simphony API (EGAIS)

#### `PUT /rvc/{rvcId}/egais/menuitems/egais` (Update Egais Attributes)

Условие запуска: `Номенклатура.ВидАлкогольнойПродукции` НЕ Пусто (или `ПодакцизныйТовар` = Истина).

| Поле DTO (`UpdateMenuItemEgaisDto`) | Тип | Источник данных в 1С | Комментарий |
|---|---|---|---|
| `objnum` | Integer | `питМеню.MRS_КодSimphony` | ID позиции в Simphony |
| `volume` | Number | `Номенклатура.ЕмкостьТары` (или `Объем`) | Объём в литрах. Если в 1С в далах — конвертировать. |
| `unit` | String | `Номенклатура.ЕдиницаИзмерения.Наименование` | Например, "л", "мл", "шт". |
| `marktype` | Integer | `Номенклатура.ВидАлкогольнойПродукции.MRS_ТипМаркировкиSimphony` | Тип маркировки (1 - Партионный, 3 - Помарочный и т.д. - уточнить справочник Simphony) |
| `groupid` | Integer | `Номенклатура.ВидАлкогольнойПродукции.MRS_КодГруппыEGAIS` | ID группы продукции в классификаторе Simphony Fiscal |
| `cocktid` | Integer | `Номенклатура.MRS_КодКоктейляEGAIS` | Заполняется только для коктейлей |

#### `POST /rvc/{rvcId}/egais/barcodes/batch` (Add Barcodes)

Источник: `РегистрСведений.ШтрихкодыНоменклатуры`.
Отбор: По `Номенклатуре` (и `Характеристике`, если используется).

| Поле DTO (`AddBarcodeDto`) | Источник данных в 1С |
|---|---|
| `objnum` | `питМеню.MRS_КодSimphony` |
| `barcode` | `РегистрСведений.ШтрихкодыНоменклатуры.Штрихкод` |

---

## 5. Metadata (1С Objects) — Обновление

### ⚠️ ТРЕБУЕТСЯ доработка существующих объектов

#### 1. Справочник `ВидыАлкогольнойПродукции`
- Добавить реквизит `MRS_ТипМаркировкиSimphony` (Число 5,0)
- Добавить реквизит `MRS_КодГруппыEGAIS` (Число 10,0)

*(Альтернатива: Добавить эти реквизиты в `Номенклатура`, если изменение `ВидыАлкогольнойПродукции` невозможно).*

#### 2. Справочник `Номенклатура`
- Добавить реквизит `MRS_КодКоктейляEGAIS` (Число 10,0) — для специфичных случаев.

---

## 12. Technical Design (Дополнения)

### Общий модуль: `MRS_ИнтеграцияSimphonyСервер`

#### Новая функция: `ОбновитьДанныеЕГАИС(Позиция, КассовыйУзел)`
**Алгоритм:**
1. Проверить, является ли номенклатура алкогольной. Если нет — выход.
2. Сформировать `UpdateMenuItemEgaisDto`.
   - Получить объем, единицу измерения.
   - Получить `marktype`, `groupid` из вида алкогольной продукции.
3. Вызвать `PUT .../egais/menuitems/egais`.
4. Вызвать `СинхронизироватьШтрихкоды(Позиция, КассовыйУзел)`.

#### Новая функция: `СинхронизироватьШтрихкоды(Позиция, КассовыйУзел)`
**Алгоритм:**
1. Получить список штрихкодов из Simphony (`GET`).
2. Получить список штрихкодов из 1С (`Запрос` к регистру).
3. Найти разницу множеств.
4. В цикле удалить лишние (`DELETE`).
5. Одним пакетом добавить новые (`POST .../batch`).

---

## 13. Implementation Steps (Дополнение)

**Шаг 4.1: Настройка данных ЕГАИС**
- Заполнить `MRS_ТипМаркировкиSimphony` и `MRS_КодГруппыEGAIS` для используемых видов алкогольной продукции.
- Убедиться, что у алкогольной номенклатуры корректно заполнены `ЕмкостьТары` (Объем) и штрихкоды.

---

## Changelog
...
### v1.5 (05.02.2026) — Интеграция с Simphony Fiscal DB (EGAIS)
- ✅ Добавлена логика обновления атрибутов ЕГАИС для алкогольных позиций.
- ✅ Добавлена двусторонняя синхронизация штрихкодов.
- ✅ Расширена модель данных (атрибуты маркировки и групп ЕГАИС).
