# PRD: Интеграция управления меню 1С:ERP 2.5 ↔ Oracle Simphony POS

**Версия документа:** 1.3  
**Дата:** 10.11.2025  
**Автор:** Product Manager + Senior 1C Developer  
**Статус:** ✅ Ready for Implementation

---

## 📋 Краткое содержание

**Цель:** Автоматизированная синхронизация меню точек продаж между 1С:ERP 2.5 (модуль Общепит) и Oracle Simphony POS через REST API.

**Ключевые изменения v1.3 (по сравнению с v1.2):**
- ✅ **Массовая установка цен:** Добавлена функциональность массового обновления цен на позиции меню. Система показывает диалог с тремя вариантами обработки: выбранные элементы, все элементы в группе, или весь вид меню. Цены автоматически извлекаются из регистра `ЦеныНоменклатуры` по виду цен кассового узла. Пользователь может отменить операцию на этапе выбора области действия.

**Ключевые изменения v1.2 (по сравнению с v1.1):**
- ✅ **Архитектура на основе RVC:** Отказ от глобального меню (`hierStrucId=1`). Все операции (создание, обновление, удаление) теперь выполняются в контексте конкретной точки продаж (RVC/аутлета), для идентификации которой используется существующий реквизит `Префикс` плана обмена `питУдаленныеКассы`.
- ✅ **Поддержка обновления позиций:** Добавлены новые методы API (`PATCH`) для изменения существующих позиций меню и их цен без пересоздания.
- ✅ **Синхронизация справочников:** Добавлен механизм для автоматической синхронизации классификаторов (Major/Family Groups) из Simphony в 1С, что снижает ошибки ручного ввода.
- ✅ **Минимальные изменения метаданных:** Используется существующий реквизит `Префикс` для `hierStrucId`, что отменяет необходимость добавления новых реквизитов в план обмена.
- ✅ **Актуализация API-контрактов:** Все эндпоинты, HTTP-методы и структуры данных приведены в полное соответствие с последней версией OpenAPI.

---

## 1. Context & Goals

### Проблема (AS-IS)

- Меню точек продаж управляется вручную в двух системах (1С и Simphony).
- Отсутствует автоматическая синхронизация позиций меню, цен и классификаторов.
- Нет контроля статусов выгрузки и механизма обработки ошибок.
- Высокий риск расхождения данных между системами.
- Затраты времени персонала на дублирование операций (~10 минут на позицию).

### Бизнес-контекст

- **Система:** 1С:ERP 2.5.17.103 (кастомизированная) + модули Catering и КТ-2000
- **Фронт-система:** Oracle Simphony POS (кассовые терминалы)
- **Метод интеграции:** REST API (OpenAPI 3.0)
- **Направление данных:** Двустороннее (Master Data from ERP → Simphony, статусы обратно, справочники Simphony → ERP)

### Цели (Objectives)

1. **Исключить ручной ввод** данных меню в Simphony — 100% позиций создаются из 1С.
2. **Обеспечить консистентность данных** и гибкое управление ценами и ассортиментом для каждой точки продаж.
3. **Контролировать статусы синхронизации** в режиме реального времени с историей.
4. **Автоматизировать выгрузку** по расписанию с механизмом повторных попыток.
5. **Сократить время создания/обновления позиции** с 10 минут до 2 минут (80% экономия).

### Non-Goals / Out of Scope

- ❌ Синхронизация транзакций продаж (чеков) — выполняется существующим планом обмена.
- ❌ Управление рецептурами и калькуляциями блюд — типовой функционал 1С.
- ❌ Интеграция с системой лояльности или складским учетом.
- ❌ Мобильное приложение для управления меню.
- ❌ Изменение ядра типовой конфигурации 1С.

---

## 2. Core Functions

### Основные функции системы

1. **Создание и обновление позиций меню** для конкретных точек продаж.
2. **Массовая установка цен** на позиции меню (одна позиция, группа, весь вид меню) с автоматическим извлечением цен из регистра `ЦеныНоменклатуры`.
3. **Выгрузка изменений в Simphony** (ручной и автоматический режимы) через REST API.
4. **Отслеживание статусов синхронизации** (НеВыгружено → ВОчереди → Выгружено / Ошибка).
5. **Механизм повторных попыток** при сбоях API (до 3 попыток с интервалом 5 мин).
6. **Логирование операций** в регистр сведений с хранением истории 30 дней.
7. **Синхронизация справочников** (Major/Family Groups) из Simphony в 1С по расписанию.
8. **Пакетная обработка** через очередь выгрузки (до 50 позиций за запрос).
9. **Удаление позиций** с синхронизацией изменений в Simphony.

---

## 3. Flows (Детальная логика)

### Flow 1: Создание/изменение позиции меню

**Актор:** Менеджер меню  
**Предусловия:** 
- Создан кассовый узел (`питУдаленныеКассы`) с заполненным `Префикс` (который является `hierStrucId`).
- Синхронизированы справочники `MRS_MajorGroups` и `MRS_FamilyGroups`.
- Номенклатуре сопоставлены Major и Family группы.
- Установлены цены в регистре `ЦеныНоменклатуры`.

**Основной поток:**

1. Пользователь открывает справочник **питМеню**.
2. Создает новую позицию или изменяет существующую.
4. При сохранении (Ctrl+S):
5. **Валидация:**
   - Проверка обязательных полей.
   - Проверка, что для вида меню заданы кассовые узлы с заполненным `Префикс`.
6. **При успешной валидации:**
   - Запись сохраняется.
   - Создается запись в регистре `MRS_ОчередьВыгрузкиМенюSimphony` для каждого привязанного кассового узла с операцией "CREATE" или "UPDATE".
   - Статус меняется на "ВОчереди".

**Постусловия:** Позиция сохранена и поставлена в очередь на выгрузку для кассового узла связанного с Видом меню.

### Flow 2: Ручная выгрузка позиций в Simphony

**Актор:** Администратор  
**Предусловия:** Есть записи в очереди.

**Основной поток:**

1. Пользователь открывает форму **"Управление очередью Simphony"**.
2. Выделяет одну или несколько записей.
3. Нажимает **"Выгрузить"**.
4. **Система формирует запрос(ы):**
   - Endpoint: `PUT /menu-item` (создание), `PATCH /menu-item` (обновление).
   - Тело JSON формируется на основе данных из `питМеню` и `Префикса` кассового узла.
5. **Обработка ответа:**
   - **При успехе (200):** Статусы обновляются на "Выгружено", запись из очереди удаляется, пишется история.
   - **При ошибке:** Статусы обновляются на "Ошибка", увеличивается счетчик попыток в очереди, пишется история.

**Постусловия:** Выбранные позиции из очереди обработаны, статусы обновлены.

### Flow 3: Автоматическая выгрузка по расписанию

**Актор:** Регламентное задание (каждые 15 минут)

**Основной поток:**
1. Задание выбирает из очереди до 50 записей со статусом "ВОчереди" или "Ошибка" (если попыток < 3).
2. Группирует их по кассовым узлам (`Префикс`) для bulk-запросов.
3. Выполняет bulk-запросы и обрабатывает результаты аналогично ручной выгрузке.
4. При ошибке увеличивает счетчик попыток. После 3 неудачных попыток устанавливает статус "ТребуетВнимания".

**Постусловия:** Очередь обработана, статусы обновлены.

### Flow 4: Синхронизация справочников из Simphony

**Актор:** Регламентное задание (ежедневно)

**Основной поток:**
1. Задание поочередно выполняет запросы `GET /referencedata/major-groups` и `GET /referencedata/family-groups`.
2. На основе полученных данных создает или обновляет элементы в справочниках 1С `MRS_MajorGroups` и `MRS_FamilyGroups`.

**Постусловия:** Справочники классификаторов в 1С приведены в соответствие с Simphony.

---

### Flow 5: Массовая установка цен на позиции меню

**Актор:** Менеджер меню  
**Предусловия:**
- В справочнике `питМеню` существуют позиции с привязкой к виду меню (`Владелец`).
- Для вида меню указан кассовый узел в плане обмена `питУдаленныеКассы`.
- У кассового узла заполнен реквизит `ВидЦен`.
- Для соответствующей номенклатуры есть записи в регистре `ЦеныНоменклатуры` с этим видом цен.

**Основной поток:**

1. Пользователь открывает форму списка **питМеню** (форма `МФормаСписка` с деревом групп).
2. Пользователь выделяет элемент(ы) в форме:
   - Одну или несколько позиций меню в списке справа.
   - Группу в дереве слева.
   - Или не выделяет ничего (будет обработан весь вид меню).
3. Пользователь нажимает команду **"Установить цены"** (кнопка на форме).
4. **Система задает вопрос пользователю о области действия:**
   - **Вариант A:** "Обработать выбранные элементы" (если выделены позиции в списке).
   - **Вариант B:** "Обработать все элементы в выбранной группе" (если выделена группа в дереве).
   - **Вариант C:** "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп" (весь вид меню).
5. Пользователь выбирает нужный вариант обработки.
6. **Для каждой позиции в области действия система выполняет:**
   1. Определяет вид меню позиции (`питМеню.Владелец` → `питВидыМеню`).
   2. По виду меню находит кассовый узел, выполнив запрос к табличной части `питУдаленныеКассы.ВидыМеню`.
   3. Извлекает из кассового узла реквизит `ВидЦен`.
   4. Выполняет запрос к регистру `ЦеныНоменклатуры`:
      - Отбор: `Номенклатура` = `питМеню.Номенклатура`, `ВидЦены` = `ВидЦен из узла`.
      - Получает значение поля `Цена`.
   5. Если цена найдена:
      - Устанавливает новую цену в реквизит позиции меню `MRS_Цена`.
      - Меняет статус на "НеВыгружено" (чтобы изменение попало в очередь).
   6. Если цена не найдена:
      - Позиция пропускается, фиксируется в логе (опционально).
7. **После обработки всех позиций:**
   - Система выводит сообщение с результатом: "Обновлено N позиций, пропущено M (не найдена цена)".
   - Список обновляется.


**Альтернативный поток:**
- **A1: Отмена пользователем:** Если пользователь закрывает диалог выбора области действия или нажимает "Отмена" → операция прерывается без выполнения.
- **A2: Кассовый узел не найден:** Если для вида меню не найден кассовый узел → вывести предупреждение "Не найден кассовый узел для вида меню X", операция прерывается.
- **A3: Вид цен не заполнен:** Если у кассового узла не заполнен `ВидЦен` → вывести предупреждение "Не указан вид цен для кассового узла Y", операция прерывается.

**Постусловия:** Цены обновлены для выбранных позиций, позиции поставлены в очередь на синхронизацию.

---

## 4. Data & Integrations

### Основные сущности 1С

#### ✅ Справочник: питМеню (Menu Items)

**Новые реквизиты (ДОБАВЛЕНЫ):**

| Реквизит | Тип | Длина | Примечание |
|----------|-----|-------|------------|
| `MRS_КодSimphony` | Число | 10, 0 | `stringNumberId` из Simphony |
**❌ НИЧЕГО добавлять не нужно!**

**Существующие реквизиты (ИСПОЛЬЗУЮТСЯ):**
- `Владелец` (СправочникСсылка.питВидыМеню) — связь с видом меню.
- `Номенклатура` (СправочникСсылка.Номенклатура) — для получения цены из регистра.
- `НомерВГруппе` — используется как `fileNum` в API.

**Новые команды на форме списка:**
- **"Установить цены"** (УстановитьЦены) — массовая установка цен на выбранные позиции.

---

#### ✅ ПланОбмена: питУдаленныеКассы (POS Outlets)

**Существующие реквизиты (ИСПОЛЬЗУЮТСЯ):**
- `ВидЦен` (СправочникСсылка.ВидыЦен) — для получения цены.
- `Префикс` (Строка) — **Используется как `hierStrucId` для Simphony.**

**❌ НИЧЕГО добавлять не нужно!**

---

#### ✅ Новые Справочники: `MRS_MajorGroups` и `MRS_FamilyGroups`

Создаются два новых справочника для хранения классификаторов из Simphony.
- **Код:** `object_number` из Simphony
- **Наименование:** `name` из Simphony

**Связь с Номенклатурой:** Реализуется через стандартный механизм дополнительных реквизитов.

---

#### Прочие объекты
- **РегистрСведений: MRS_ОчередьВыгрузкиМенюSimphony** (структура как в v1.1)
- **РегистрСведений: MRS_ИсторияСинхронизацииМенюSimphony** (структура как в v1.1)
- **Перечисление: MRS_СтатусыСинхронизацииSimphony** (значения как в v1.1)
- **Константа: MRS_URLСервисаSimphony**

---

### Бизнес-логика получения цены для позиции меню

**Цепочка получения данных:**

1. **Входные данные:** Позиция меню (`питМеню`)
2. **Шаг 1:** Из позиции меню берем `Владелец` → получаем `питВидыМеню`
3. **Шаг 2:** По виду меню находим кассовый узел:
   - Запрос к табличной части `ПланОбмена.питУдаленныеКассы.ВидыМеню`
   - Условие: `ВидМеню = питВидыМеню.Ссылка`
   - Результат: `питУдаленныеКассы.Ссылка` (кассовый узел)
4. **Шаг 3:** Из кассового узла извлекаем реквизит `ВидЦен` (СправочникСсылка.ВидыЦен)
5. **Шаг 4:** Выполняем запрос к регистру `ЦеныНоменклатуры`:
   - Условия отбора:
     - `Номенклатура` = `питМеню.Номенклатура`
     - `ВидЦены` = `ВидЦен` (из кассового узла)
   - Результат: `Цена` (Число 15,2)


**Пример SQL-запроса для получения кассового узла:**

```sql
ВЫБРАТЬ
    питУдаленныеКассыВидыМеню.Ссылка КАК КассовыйУзел,
    питУдаленныеКассыВидыМеню.Ссылка.ВидЦен КАК ВидЦен,
    питУдаленныеКассыВидыМеню.Ссылка.Префикс КАК Префикс
ИЗ
    ПланОбмена.питУдаленныеКассы.ВидыМеню КАК питУдаленныеКассыВидыМеню
        ЛЕВОЕ СОЕДИНЕНИЕ Справочник.питВидыМеню КАК питВидыМеню
        ПО питУдаленныеКассыВидыМеню.ВидМеню = питВидыМеню.Ссылка
ГДЕ
    питВидыМеню.Ссылка = &ВидМеню
```

**Валидация:**
- Если кассовый узел не найден → **Ошибка:** "Не найден кассовый узел для вида меню [Наименование]"
- Если `ВидЦен` не заполнен → **Ошибка:** "Не указан вид цен для кассового узла [Наименование]"
- Если цена не найдена в регистре → **Предупреждение:** "Цена не найдена для номенклатуры [Наименование], вид цен [Наименование]"

---

### Маппинг данных: 1С ↔ Simphony API

#### `PUT /menu-item` (Create Single Item)

| Поле API | Источник 1С |
|----------|-------------|
| `name` | `питМеню.Наименование` |
| `stringNumberId` | `питМеню.MRS_КодSimphony` |
| `majGrpObjNum` | Код из `MRS_MajorGroups` (через номенклатуру) |
| `famGrpObjNum` | Код из `MRS_FamilyGroups` (через номенклатуру) |
| `hierStrucId` | `питУдаленныеКассы.Префикс` |
| `menuItemClass`| **Константа = 1001** |
| `price` | `питМеню.MRS_Цена` |
| `fileNum` | `питМеню.НомерВГруппе` |

#### `PATCH /menu-item` (Update Single Item)

| Поле API | Источник 1С |
|----------|-------------|
| `stringNumberId` | `питМеню.MRS_КодSimphony` |
| `hierStrucId` | `питУдаленныеКассы.Префикс` |
| `name`, `price`, `famGrpObjNum`, `majGrpObjNum` | Соответствующие поля из 1С |

#### `DELETE /menu-item/{stringNumberId}` (Delete Single Item)

- **Path `stringNumberId`**: `питМеню.MRS_КодSimphony`
- **Query `hierStrucId`**: `питУдаленныеКассы.Префикс`

---

## 5. Metadata (1С Objects) — Итоговый статус

### ✅ НОВЫЕ объекты
1. **Справочник:** `MRS_MajorGroups`
2. **Справочник:** `MRS_FamilyGroups`
3. **Перечисление:** `MRS_СтатусыСинхронизацииSimphony`
4. **РегистрСведений:** `MRS_ОчередьВыгрузкиМенюSimphony`
5. **РегистрСведений:** `MRS_ИсторияСинхронизацииМенюSimphony`
6. **Константа:** `MRS_URLСервисаSimphony`
7. **Общие модули:** (серверный, клиент-серверный, клиентский)
8. **Регламентные задания:** `ВыгрузкаМеню`, `ОчисткаИстории`, `СинхронизацияСправочников` (Новое)
9. **Команды и Формы** для `питМеню`.

### ⚠️ ТРЕБУЕТСЯ доработка существующих объектов

#### 1. Справочник `питМеню`
- **НИЧЕГО добавлять не нужно!**. Добавлен только реквизит `MRS_КодSimphony`.

#### 2. ПланОбмена `питУдаленныеКассы`
- **НИЧЕГО добавлять не нужно!** Убедиться, что реквизит `Префикс` заполнен корректно.

#### 3. ГруппаАналитическогоУчетаНоменклатуры
- Добавлены реквизиты для связи с `MRS_MajorGroups` и `MRS_FamilyGroups`.

---

## 6. UI/UX спецификация для массовой установки цен

### Форма: МФормаСписка (справочник питМеню)

**Элементы интерфейса:**

| Элемент | Тип | Расположение | Название | Действие |
|---------|-----|--------------|----------|----------|
| Кнопка | КнопкаКоманды | Командная панель списка | "Установить цены" | Вызов команды `УстановитьЦены` |

**Логика активности кнопки:**
- Кнопка доступна всегда (не зависит от выделения).
- При нажатии система показывает диалог выбора области действия.
- Пользователь выбирает один из трех вариантов обработки.

**Диалог выбора области действия:**

| Вариант | Текст варианта | Когда показывать |
|---------|----------------|------------------|
| 1 | "Обработать выбранные элементы" | Всегда доступен |
| 2 | "Обработать все элементы в выбранной группе" | Если выделена группа в дереве |
| 3 | "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп" | Всегда доступен |

**Сообщения пользователю:**

| Ситуация | Тип | Текст |
|----------|-----|-------|
| Успешное обновление | Информация | "Обновлено цен: N. Пропущено позиций: M (не найдена цена)." |
| Отмена пользователем | — | Операция отменяется без сообщения |
| Не найден кассовый узел | Предупреждение | "Не найден кассовый узел для вида меню '[Наименование]'." |
| Не заполнен ВидЦен | Предупреждение | "Не указан вид цен для кассового узла '[Наименование]'." |
| Нет позиций для обработки | Предупреждение | "Не найдено позиций меню для обработки." |

**Поведение после выполнения:**
- Список автоматически обновляется для отображения новых цен.
- Статусы синхронизации изменяются на "НеВыгружено" для измененных позиций.

---

## 7. Use Cases (Сценарии использования)

### UC-1: Массовое обновление цен при акции
**Сценарий:** В ресторане запускается акция "Счастливые часы" с новыми ценами на напитки.

**Действия менеджера:**
1. Открывает форму списка `питМеню`
2. В дереве слева выбирает группу "Напитки"
3. Нажимает "Установить цены"
4. Система показывает диалог выбора области действия
5. Менеджер выбирает "Обработать все элементы в выбранной группе"
6. Система обновляет цены для всех позиций в группе из регистра `ЦеныНоменклатуры` по виду цен "Акция"
7. Позиции автоматически попадают в очередь на выгрузку
8. Регламентное задание синхронизирует изменения с Simphony

**Результат:** 20 позиций обновлены за 30 секунд вместо 200 минут вручную.

---

### UC-2: Открытие новой точки продаж с собственным меню
**Сценарий:** Открывается новый киоск с ограниченным ассортиментом и специальными ценами.

**Действия администратора:**
1. Создает новый узел в `питУдаленныеКассы`, заполняет `Префикс` (hierStrucId) и `ВидЦен` ("Киоск")
2. Создает новый вид меню "Киоск - Летнее меню"
3. Привязывает вид меню к кассовому узлу
4. Создает позиции меню для киоска
5. Открывает форму списка для вида меню "Киоск - Летнее меню"
6. Нажимает "Установить цены"
7. Система показывает диалог выбора области действия
8. Администратор выбирает "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп"
9. Система обновляет все позиции этого вида меню ценами из вида цен "Киоск"

**Результат:** Меню готово к выгрузке, все цены корректны, ручной ввод исключен.

---

### UC-3: Корректировка цены на одну позицию
**Сценарий:** Изменилась закупочная цена на кофе, нужно обновить розничную цену.

**Действия менеджера:**
1. Обновляет цену в регистре `ЦеныНоменклатуры` для номенклатуры "Капучино"
2. Открывает форму списка `питМеню`
3. Находит позицию "Капучино", выделяет её
4. Нажимает "Установить цены"
5. Система показывает диалог выбора области действия
6. Менеджер выбирает "Обработать выбранные элементы"
7. Система обновляет цену только для выделенной позиции

**Результат:** Одна позиция обновлена и синхронизирована с Simphony за 10 секунд.

---

## 8. Допущения и ограничения

### Допущения (Assumptions)
1. **Структура данных:** Предполагается, что структура плана обмена `питУдаленныеКассы` уже содержит табличную часть `ВидыМеню` с полями `ВидМеню` и реквизит `ВидЦен`.
2. **Регистр цен:** Регистр `ЦеныНоменклатуры` актуален и содержит все необходимые виды цен для каждой точки продаж.
3. **Уникальность связи:** Один вид меню связан только с одним кассовым узлом.
4. **Номенклатура заполнена:** У всех позиций меню корректно заполнен реквизит `Номенклатура`.
5. **Префикс = hierStrucId:** Значение реквизита `Префикс` в плане обмена соответствует идентификатору точки продаж (RVC) в Simphony.

### Технические ограничения
1. **Производительность:** Одновременная обработка не более 1000 позиций меню за один вызов команды.
2. **Время ожидания:** Операция должна завершаться не дольше 30 секунд для 100 позиций.
3. **Блокировки:** При массовом обновлении возможны блокировки записей, если другой пользователь редактирует ту же позицию.
4. **Права доступа:** Пользователь должен иметь права на изменение справочника `питМеню`.
5. **Кэширование:** Кассовые узлы и виды цен кэшируются на время выполнения операции для оптимизации.

### Бизнес-ограничения
1. **Один вид цен на узел:** Каждый кассовый узел работает только с одним видом цен.
2. **Ручное управление регистром цен:** Цены в регистре `ЦеныНоменклатуры` управляются вне данной функциональности.
3. **Статус синхронизации:** После установки цены статус всегда сбрасывается на "НеВыгружено".

---

## 9. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Цены не найдены в регистре для части позиций | Высокая | Среднее | Пропускать позиции с недостающими ценами, выводить предупреждение с перечнем |
| Кассовый узел не найден для вида меню | Средняя | Высокое | Валидация на этапе создания вида меню + предупреждение при установке цен |
| Низкая производительность при большом объеме | Средняя | Среднее | Ограничение на 1000 позиций, пакетная обработка, индексация полей |
| Конфликт версий при одновременном редактировании | Низкая | Низкое | Использовать управляемые блокировки 1С |
| Неверный `Префикс` в плане обмена | Средняя | Высокое | Валидация формата префикса при создании узла, документация по заполнению |
| Пользователь забывает запускать выгрузку после установки цен | Высокая | Среднее | Автоматическая постановка в очередь + регламентное задание |

---

## 10. Приоритизация функций (MoSCoW)

### Must Have (Критично для запуска)
1. ✅ Создание и обновление позиций меню в Simphony (API интеграция)
2. ✅ Отслеживание статусов синхронизации
3. ✅ Очередь выгрузки с механизмом повторных попыток
4. ✅ Логирование операций
5. ✅ Установка цен на одну позицию меню

### Should Have (Важно, но можно отложить)
6. ✅ Массовая установка цен по группе
7. ✅ Массовая установка цен по виду меню
8. ✅ Синхронизация справочников из Simphony
9. ✅ Автоматическая выгрузка по расписанию

### Could Have (Желательно)
10. ✅ Пакетная обработка (bulk API)
11. ⚠️ Расширенная аналитика по ошибкам синхронизации
12. ⚠️ Уведомления при критических ошибках

### Won't Have (Исключено из текущей версии)
13. ❌ Синхронизация транзакций продаж (отдельный план обмена)
14. ❌ Управление рецептурами через API
15. ❌ Мобильное приложение

---

## 11. Acceptance Criteria (Критерии приемки)

### Функциональные критерии

#### AC-1: Установка цены на выбранные элементы
- ✅ GIVEN: Пользователь выделил одну или несколько позиций в списке меню
- ✅ WHEN: Нажимает кнопку "Установить цены"
- ✅ THEN: 
  - Система показывает диалог с вопросом о области действия
  - Пользователь выбирает "Обработать выбранные элементы"
  - Система обрабатывает только выделенные позиции:
    - Находит вид меню позиции
    - Находит кассовый узел по виду меню
    - Извлекает вид цен из кассового узла
    - Получает цену из регистра `ЦеныНоменклатуры`
    - Записывает цену в `MRS_Цена`
    - Устанавливает статус "НеВыгружено"
  - Выводит сообщение "Обновлено цен: N. Пропущено позиций: M."

#### AC-2: Установка цен на все элементы в группе
- ✅ GIVEN: Пользователь выделил группу в дереве (например, "Напитки")
- ✅ WHEN: Нажимает кнопку "Установить цены"
- ✅ THEN:
  - Система показывает диалог с вопросом о области действия
  - Пользователь выбирает "Обработать все элементы в выбранной группе"
  - Система находит все позиции меню в этой группе
  - Обрабатывает каждую позицию согласно AC-1
  - Выводит сообщение с количеством обновленных и пропущенных позиций
  - Список автоматически обновляется

#### AC-3: Установка цен на весь вид меню
- ✅ GIVEN: Пользователь открыл форму списка меню (вид меню указан в шапке формы)
- ✅ WHEN: Нажимает кнопку "Установить цены"
- ✅ THEN:
  - Система показывает диалог с вопросом о области действия
  - Пользователь выбирает "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп"
  - Система находит все позиции меню данного вида (исключая группы)
  - Обрабатывает каждую позицию согласно AC-1
  - Выводит сообщение с количеством обновленных и пропущенных позиций

#### AC-4: Отмена операции пользователем
- ✅ GIVEN: Пользователь нажал кнопку "Установить цены"
- ✅ WHEN: В диалоге выбора области действия нажимает "Отмена" или закрывает диалог
- ✅ THEN:
  - Операция не выполняется
  - Изменения в данные не вносятся
  - Сообщения пользователю не выводятся
  - Форма остается в исходном состоянии

#### AC-5: Обработка отсутствия цены
- ✅ GIVEN: Для номенклатуры нет записи в регистре `ЦеныНоменклатуры` с нужным видом цен
- ✅ WHEN: Выполняется установка цен
- ✅ THEN:
  - Позиция пропускается
  - Цена не изменяется
  - Статус не изменяется
  - В итоговом сообщении увеличивается счетчик пропущенных
  - Опционально: в лог записывается предупреждение

#### AC-6: Обработка отсутствия кассового узла
- ✅ GIVEN: Для вида меню не найден кассовый узел в плане обмена
- ✅ WHEN: Выполняется установка цен
- ✅ THEN:
  - Выводится предупреждение "Не найден кассовый узел для вида меню '[Наименование]'"
  - Обработка останавливается

#### AC-7: Обработка отсутствия вида цен
- ✅ GIVEN: У кассового узла не заполнен реквизит `ВидЦен`
- ✅ WHEN: Выполняется установка цен
- ✅ THEN:
  - Выводится предупреждение "Не указан вид цен для кассового узла '[Наименование]'"
  - Обработка останавливается

### Нефункциональные критерии

#### AC-8: Производительность
- ✅ Обработка 100 позиций завершается не дольше 30 секунд
- ✅ Обработка 1000 позиций завершается не дольше 5 минут
- ✅ Операция не блокирует UI (выполняется на сервере)

#### AC-9: Безопасность
- ✅ Проверка прав доступа на изменение справочника `питМеню`
- ✅ Пользователь без прав получает сообщение об ошибке доступа

#### AC-10: Юзабилити
- ✅ Кнопка "Установить цены" видна и доступна на форме списка
- ✅ Диалог выбора области действия понятен и интуитивен
- ✅ Пользователь может отменить операцию на этапе выбора области действия
- ✅ Сообщения пользователю информативны и понятны
- ✅ После выполнения список автоматически обновляется
- ✅ Операция может быть прервана (Ctrl+Break) во время обработки

---

## 12. Technical Design (Техническая архитектура)

### Модули и их ответственность

#### 1. Команда формы: `MRS_УстановитьЦены`
**Расположение:** Форма `МФормаСписка` справочника `питМеню`  
**Тип:** Команда формы  
**Ответственность:** 
- Показ диалога выбора области действия пользователю
- Определение контекста выполнения на основе выбора пользователя
- Вызов серверной процедуры обработки
- Отображение результата пользователю

**Псевдокод:**
```
Процедура МRS_УстановитьЦены(Команда)
    
    // Показываем диалог выбора области действия
    СписокВыбора = Новый СписокЗначений;
    СписокВыбора.Добавить("ВыбранныеЭлементы", "Обработать выбранные элементы");
    СписокВыбора.Добавить("ВсяГруппа", "Обработать все элементы в выбранной группе");
    СписокВыбора.Добавить("ВесьВидМеню", "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп");
    
    ВыбранноеЗначение = СписокВыбора.ВыбратьЭлемент("Выберите область действия:");
    
    Если ВыбранноеЗначение = Неопределено Тогда
        Возврат; // Пользователь отменил
    КонецЕсли;
    
    // Определяем параметры на основе выбора
    Режим = ВыбранноеЗначение.Значение;
    ТекущиеДанные = Элементы.Список.ТекущиеДанные;
    ВыделенныеСтроки = Элементы.Список.ВыделенныеСтроки;
    
    // Вызов серверной обработки
    Результат = МRS_УстановитьЦеныНаСервере(Режим, ВыделенныеСтроки, ТекущиеДанные, Владелец);
    
    // Отображение результата
    Сообщить(Результат.Сообщение)
    Элементы.Список.Обновить()
КонецПроцедуры
```

#### 2. Серверная процедура: `MRS_УстановитьЦеныНаСервере()`
**Расположение:** Модуль формы `МФормаСписка` (серверная часть)  
**Тип:** Серверная процедура  
**Ответственность:**
- Получение списка позиций для обработки на основе выбранного режима
- Вызов серверного модуля для обработки каждой позиции
- Формирование итогового результата

**Псевдокод:**
```
Функция МRS_УстановитьЦеныНаСервере(Режим, ВыделенныеСтроки, ТекущиеДанные, ВидМеню)
    
    СписокПозиций = Новый Массив;
    
    // Формируем список позиций в зависимости от режима
    Если Режим = "ВыбранныеЭлементы" Тогда
        // Обрабатываем только выделенные строки
        Для Каждого ВыделеннаяСтрока Из ВыделенныеСтроки Цикл
            СписокПозиций.Добавить(ВыделеннаяСтрока);
        КонецЦикла;
        
    ИначеЕсли Режим = "ВсяГруппа" Тогда
        // Обрабатываем все позиции в выбранной группе
        СписокПозиций = ПолучитьВсеПозицииГруппы(ТекущиеДанные.Ссылка);
        
    ИначеЕсли Режим = "ВесьВидМеню" Тогда
        // Обрабатываем все позиции вида меню (кроме групп)
        СписокПозиций = ПолучитьВсеПозицииВидаМеню(ВидМеню);
        
    КонецЕсли;
    
    Обновлено = 0;
    Пропущено = 0;
    
    Для Каждого Позиция Из СписокПозиций Цикл
        Результат = MRS_ИнтеграцияSimphonyСервер.УстановитьЦенуДляПозиции(Позиция);
        
        Если Результат.Успех Тогда
            Обновлено = Обновлено + 1;
        Иначе
            Пропущено = Пропущено + 1;
        КонецЕсли;
    КонецЦикла;
    
    Сообщение = СтрШаблон("Обновлено цен: %1. Пропущено позиций: %2.", Обновлено, Пропущено);
    
    Возврат Новый Структура("Сообщение", Сообщение);
КонецФункции
```

#### 3. Общий модуль: `MRS_ИнтеграцияSimphonyСервер`
**Расположение:** Общий модуль (серверный)  
**Тип:** Общий серверный модуль  
**Ответственность:**
- Бизнес-логика получения кассового узла
- Получение вида цен
- Запрос цены из регистра
- Запись цены в позицию меню
- Изменение статуса синхронизации

**Ключевые функции:**

##### 3.1. `УстановитьЦенуДляПозиции(Позиция)`
**Входные параметры:** СправочникСсылка.питМеню  
**Возвращает:** Структура {Успех: Булево, Сообщение: Строка}

**Псевдокод:**
```
Функция УстановитьЦенуДляПозиции(ПозицияСсылка)
    
    // 1. Получаем объект для редактирования
    ПозицияОбъект = ПозицияСсылка.ПолучитьОбъект()
    
    // 2. Получаем вид меню
    ВидМеню = ПозицияОбъект.Владелец
    
    // 3. Получаем кассовый узел
    КассовыйУзел = ПолучитьКассовыйУзелДляВидаМеню(ВидМеню)
    
    Если НЕ ЗначениеЗаполнено(КассовыйУзел) Тогда
        Возврат Новый Структура("Успех, Сообщение", Ложь, "Не найден кассовый узел")
    КонецЕсли
    
    // 4. Получаем вид цен
    ВидЦен = КассовыйУзел.ВидЦен
    
    Если НЕ ЗначениеЗаполнено(ВидЦен) Тогда
        Возврат Новый Структура("Успех, Сообщение", Ложь, "Не указан вид цен")
    КонецЕсли
    
    // 5. Получаем цену из регистра
    Цена = ПолучитьЦенуИзРегистра(ПозицияОбъект.Номенклатура, ВидЦен)
    
    Если Цена = Неопределено Тогда
        Возврат Новый Структура("Успех, Сообщение", Ложь, "Цена не найдена")
    КонецЕсли
    
    // 6. Записываем цену и меняем статус
    ПозицияОбъект.MRS_Цена = Цена
    ПозицияОбъект.MRS_СтатусСинхронизацииSimphony = Перечисления.MRS_СтатусыСинхронизацииSimphony.НеВыгружено
    ПозицияОбъект.Записать()
    
    Возврат Новый Структура("Успех, Сообщение", Истина, "")
    
КонецФункции
```

##### 3.2. `ПолучитьКассовыйУзелДляВидаМеню(ВидМеню)`
**Входные параметры:** СправочникСсылка.питВидыМеню  
**Возвращает:** ПланОбменаСсылка.питУдаленныеКассы

**Реализация:** SQL-запрос к табличной части плана обмена (см. раздел 4 "Бизнес-логика получения цены")

##### 3.3. `ПолучитьЦенуИзРегистра(Номенклатура, ВидЦен)`
**Входные параметры:** 
- СправочникСсылка.Номенклатура
- СправочникСсылка.ВидыЦен

**Возвращает:** Число (15,2) или Неопределено

**Псевдокод:**
```
Функция ПолучитьЦенуИзРегистра(Номенклатура, ВидЦен)
    
    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |   ЦеныНоменклатуры.Цена КАК Цена
    |ИЗ
    |   РегистрСведений.ЦеныНоменклатуры КАК ЦеныНоменклатуры
    |ГДЕ
    |   ЦеныНоменклатуры.Номенклатура = &Номенклатура
    |   И ЦеныНоменклатуры.ВидЦены = &ВидЦены";
    
    Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
    Запрос.УстановитьПараметр("ВидЦены", ВидЦен);
    
    Результат = Запрос.Выполнить();
    
    Если Результат.Пустой() Тогда
        Возврат Неопределено;
    КонецЕсли;
    
    Выборка = Результат.Выбрать();
    Выборка.Следующий();
    
    Возврат Выборка.Цена;
    
КонецФункции
```

### Диаграмма последовательности

```
Пользователь -> Форма: Выделяет элемент(ы) в списке/дереве
Пользователь -> Форма: Нажатие "Установить цены"
Форма -> Форма: Показать диалог выбора области действия
Форма -> Пользователь: "Выберите область действия: 1/2/3"
Пользователь -> Форма: Выбирает режим обработки
Форма -> Форма(Сервер): МRS_УстановитьЦеныНаСервере(Режим, Параметры)
Форма(Сервер) -> Форма(Сервер): Формирование СписокПозиций по режиму
Цикл [Для каждой позиции]
    Форма(Сервер) -> MRS_Сервер: УстановитьЦенуДляПозиции(Позиция)
    MRS_Сервер -> MRS_Сервер: ПолучитьКассовыйУзел()
    MRS_Сервер -> MRS_Сервер: ПолучитьЦенуИзРегистра()
    MRS_Сервер -> БД: Записать позицию (MRS_Цена, Статус)
    MRS_Сервер --> Форма(Сервер): Результат {Успех, Сообщение}
Конец цикла
Форма(Сервер) --> Форма: Статистика {Обновлено, Пропущено}
Форма -> Пользователь: Сообщение "Обновлено N, Пропущено M"
Форма -> Список: Обновить()
```

---

## 13. Implementation Steps (План внедрения)

1.  **Шаг 1: Изменение метаданных (10 минут)**
    - Добавить 3 реквизита в `питМеню`: `MRS_КодSimphony`, `MRS_СтатусСинхронизацииSimphony`, `MRS_Цена`.
    - Создать 2 новых справочника: `MRS_MajorGroups`, `MRS_FamilyGroups`.
    - Добавить команду `MRS_УстановитьЦены` на форму списка `питМеню`.
    - Обновить конфигурацию БД.
2.  **Шаг 2: Настройка интеграции (5 минут)**
    - Установить URL в константе.
    - Реализовать и раскомментировать код в модуле объекта `питМеню`.
3.  **Шаг 3: Синхронизация справочников (5 минут)**
    - Запустить вручную регл. задание `MRS_СинхронизацияСправочниковSimphony`.
    - Проверить, что справочники `MRS_MajorGroups` и `MRS_FamilyGroups` заполнились.
4.  **Шаг 4: Настройка данных в 1С (20 минут)**
    - Для всех узлов в `питУдаленныеКассы` проверить/заполнить `Префикс` значениями `hierStrucId` из Simphony.
    - Настроить связь номенклатуры с новыми справочниками.
5.  **Шаг 5: Включение регламентных заданий (3 минуты)**
    - Включить все 3 задания с рекомендованным расписанием.
6.  **Шаг 6: Тестирование массовой установки цен (10 минут)**
    - Открыть форму списка `питМеню`.
    - Протестировать установку цены на одну позицию.
    - Протестировать установку цен на группу позиций.
    - Протестировать установку цен на весь вид меню.
    - Проверить корректность получения цен из регистра `ЦеныНоменклатуры`.
    - Проверить изменение статуса на "НеВыгружено" после установки цены.
7.  **Шаг 7: Тестирование интеграции (15 минут)**
    - Протестировать полный цикл: создание, обновление, ручная и автоматическая выгрузка, удаление.
    - Проверить историю и статусы.

---

## 14. Глоссарий терминов

| Термин | Описание |
|--------|----------|
| **питМеню** | Справочник 1С, содержащий позиции меню для точек продаж |
| **питВидыМеню** | Справочник 1С, содержащий виды меню (например, "Завтрак", "Обед", "Бар") |
| **питУдаленныеКассы** | План обмена 1С, описывающий кассовые узлы (точки продаж) |
| **Префикс** | Реквизит плана обмена, используется как `hierStrucId` для Simphony API |
| **ВидЦен** | Справочник 1С, определяющий тип цены (например, "Розничная", "Акция", "VIP") |
| **ЦеныНоменклатуры** | Регистр сведений 1С, хранящий актуальные цены номенклатуры по различным видам цен |
| **hierStrucId** | Идентификатор точки продаж (RVC) в Oracle Simphony POS |
| **stringNumberId** | Уникальный числовой идентификатор позиции меню в Simphony |
| **Major Group** | Основная группа классификации меню в Simphony (например, "Еда", "Напитки") |
| **Family Group** | Подгруппа классификации меню в Simphony (например, "Горячие напитки", "Салаты") |
| **RVC** (Revenue Center) | Точка продаж в Simphony (кассовый узел, ресторан, бар и т.д.) |
| **Simphony POS** | Oracle Simphony Point of Sale — система управления продажами на кассах |
| **MRS_** | Префикс для всех новых объектов метаданных в рамках интеграции |
| **Статус синхронизации** | Состояние выгрузки позиции: НеВыгружено, ВОчереди, Выгружено, Ошибка |
| **Очередь выгрузки** | Регистр сведений `MRS_ОчередьВыгрузкиМенюSimphony`, содержащий позиции, ожидающие синхронизации |
| **История синхронизации** | Регистр сведений `MRS_ИсторияСинхронизацииМенюSimphony`, хранящий лог всех операций синхронизации |
| **Bulk API** | Пакетный API для одновременной обработки нескольких позиций меню |
| **menuItemClass** | Класс позиции меню в Simphony (константа = 1001) |
| **fileNum** | Номер позиции в группе меню (`питМеню.НомерВГруппе`) |

---

## 15. Контакты и поддержка

**Product Owner:** Менеджер по продукту  
**Tech Lead:** Senior 1C Developer  
**Дата готовности:** 10.11.2025  
**Версия системы:** 1С:ERP 2.5.17.103 + Catering + КТ-2000  

**Вопросы по PRD:** Обращаться к Product Manager  
**Технические вопросы:** Обращаться к Tech Lead  

---

**© 2025 | PRD v1.3 | Статус: ✅ Ready for Implementation**
