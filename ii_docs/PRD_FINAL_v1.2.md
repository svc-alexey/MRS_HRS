# PRD: Интеграция управления меню 1С:ERP 2.5 ↔ Oracle Simphony POS

**Версия документа:** 1.4  
**Дата:** 10.11.2025  
**Автор:** Product Manager + Senior 1C Developer  
**Статус:** ✅ Ready for Implementation

---

## 📋 Краткое содержание

**Цель:** Автоматизированная синхронизация меню точек продаж между 1С:ERP 2.5 (модуль Общепит) и Oracle Simphony POS через REST API.

**Ключевые изменения v1.4 (по сравнению с v1.3):**
- 🔄 **Упрощение архитектуры:** Отказ от дополнительных реквизитов для хранения цен и статусов. Вся информация получается динамически из регистров сведений.
- 🔄 **Иерархия классификаторов:** Использование существующей структуры `ГруппыАналитическогоУчетаНоменклатуры` для хранения Major/Family Groups (1 уровень = Major Group, 2 уровень = Family Group), отказ от создания отдельных справочников.
- ✅ **Ручная выгрузка меню:** Добавлена команда для выгрузки всех позиций выбранного вида меню в Simphony одним нажатием.

**Ключевые изменения v1.3 (по сравнению с v1.2):**
- ✅ **Массовая установка цен:** Постановка позиций в очередь на выгрузку с актуализацией цен из регистра `ЦеныНоменклатуры`. Диалог с тремя вариантами обработки: выбранные элементы, группа, или весь вид меню.

**Ключевые изменения v1.2 (по сравнению с v1.1):**
- ✅ **Архитектура на основе RVC:** Все операции выполняются в контексте конкретной точки продаж (RVC/аутлета), используется реквизит `Префикс` плана обмена `питУдаленныеКассы`.
- ✅ **Поддержка обновления позиций:** Методы API (`PATCH`) для изменения существующих позиций меню без пересоздания.
- ✅ **Минимальные изменения метаданных:** Максимальное использование существующих структур данных конфигурации.
- ✅ **Актуализация API-контрактов:** Полное соответствие OpenAPI спецификации Simphony.

---

## 1. Context & Goals

### Проблема (AS-IS)

- Меню точек продаж управляется вручную в двух системах (1С и Simphony).
- Отсутствует автоматическая синхронизация позиций меню, цен и классификаторов.
- Нет контроля статусов выгрузки и механизма обработки ошибок.
- Высокий риск расхождения данных между системами.
- Затраты времени персонала на дублирование операций (~10 минут на позицию).

### Бизнес-контекст

- **Система:** 1С:ERP 2.5.17.103 (кастомизированная) + модули Catering и КТ-2000
- **Фронт-система:** Oracle Simphony POS (кассовые терминалы)
- **Метод интеграции:** REST API (OpenAPI 3.0)
- **Направление данных:** Двустороннее (Master Data from ERP → Simphony, статусы обратно, справочники Simphony → ERP)

### Цели (Objectives)

1. **Исключить ручной ввод** данных меню в Simphony — 100% позиций создаются из 1С.
2. **Обеспечить консистентность данных** и гибкое управление ценами и ассортиментом для каждой точки продаж.
3. **Контролировать статусы синхронизации** в режиме реального времени с историей.
4. **Автоматизировать выгрузку** по расписанию с механизмом повторных попыток.
5. **Сократить время создания/обновления позиции** с 10 минут до 2 минут (80% экономия).

### Non-Goals / Out of Scope

- ❌ Синхронизация транзакций продаж (чеков) — выполняется существующим планом обмена.
- ❌ Управление рецептурами и калькуляциями блюд — типовой функционал 1С.
- ❌ Интеграция с системой лояльности или складским учетом.
- ❌ Мобильное приложение для управления меню.
- ❌ Изменение ядра типовой конфигурации 1С.

---

## 2. Core Functions

### Основные функции системы

1. **Создание и обновление позиций меню** для конкретных точек продаж.
2. **Постановка в очередь на выгрузку** с автоматическим получением цен из регистра `ЦеныНоменклатуры` по виду цен кассового узла.
3. **Ручная выгрузка меню:** Команда для немедленной выгрузки всех позиций вида меню в Simphony (минуя очередь).
4. **Автоматическая выгрузка по расписанию** через очередь выгрузки (до 50 позиций за запрос).
5. **Отслеживание статусов синхронизации** через регистры `MRS_ОчередьВыгрузкиМенюSimphony` и `MRS_ИсторияСинхронизацииМенюSimphony`.
6. **Механизм повторных попыток** при сбоях API (до 3 попыток с интервалом 5 мин).
7. **Логирование операций** в регистр сведений с хранением истории 30 дней.
8. **Синхронизация классификаторов** (Major/Family Groups) из Simphony в иерархию `ГруппыАналитическогоУчетаНоменклатуры`.
9. **Удаление позиций** с синхронизацией изменений в Simphony.

---

## 3. Flows (Детальная логика)

### Flow 1: Создание/изменение позиции меню

**Актор:** Менеджер меню  
**Предусловия:** 
- Создан кассовый узел (`питУдаленныеКассы`) с заполненным `Префикс` (hierStrucId) и `ВидЦен`.
- У номенклатуры заполнена `ГруппаАналитическогоУчета` с правильной иерархией (1 уровень = Major Group, 2 уровень = Family Group).
- В реквизитах `ГруппыАналитическогоУчетаНоменклатуры` заполнены `MRS_КодMajorGroup` и `MRS_КодFamilyGroup`.
- Установлены цены в регистре `ЦеныНоменклатуры` по виду цен кассового узла.

**Основной поток:**

1. Пользователь открывает справочник **питМеню**.
2. Создает новую позицию или изменяет существующую.
4. При сохранении (Ctrl+S):
5. **Валидация:**
   - Проверка обязательных полей.
   - Проверка, что для вида меню заданы кассовые узлы с заполненным `Префикс`.
6. **При успешной валидации:**
   - Запись сохраняется.
   - Создается запись в регистре `MRS_ОчередьВыгрузкиМенюSimphony` для каждого привязанного кассового узла с операцией "CREATE" или "UPDATE".
   - Статус меняется на "ВОчереди".

**Постусловия:** Позиция сохранена и поставлена в очередь на выгрузку для кассового узла связанного с Видом меню.

### Flow 2: Ручная выгрузка позиций в Simphony

**Актор:** Администратор  
**Предусловия:** Есть записи в очереди.

**Основной поток:**

1. Пользователь открывает форму **"Управление очередью Simphony"**.
2. Выделяет одну или несколько записей.
3. Нажимает **"Выгрузить"**.
4. **Система формирует запрос(ы):**
   - Endpoint: `PUT /menu-item` (создание), `PATCH /menu-item` (обновление).
   - Тело JSON формируется на основе данных из `питМеню` и `Префикса` кассового узла.
5. **Обработка ответа:**
   - **При успехе (200):** Статусы обновляются на "Выгружено", запись из очереди удаляется, пишется история.
   - **При ошибке:** Статусы обновляются на "Ошибка", увеличивается счетчик попыток в очереди, пишется история.

**Постусловия:** Выбранные позиции из очереди обработаны, статусы обновлены.

### Flow 3: Автоматическая выгрузка по расписанию

**Актор:** Регламентное задание (каждые 15 минут)

**Основной поток:**
1. Задание выбирает из очереди до 50 записей со статусом "ВОчереди" или "Ошибка" (если попыток < 3).
2. Группирует их по кассовым узлам (`Префикс`) для bulk-запросов.
3. Выполняет bulk-запросы и обрабатывает результаты аналогично ручной выгрузке.
4. При ошибке увеличивает счетчик попыток. После 3 неудачных попыток устанавливает статус "ТребуетВнимания".

**Постусловия:** Очередь обработана, статусы обновлены.

### Flow 2a: Ручная выгрузка всего меню в Simphony

**Актор:** Администратор / Менеджер меню  
**Предусловия:** В справочнике `питМеню` есть позиции для выбранного вида меню.

**Основной поток:**

1. Пользователь открывает форму списка **питМеню** для нужного вида меню.
2. Нажимает команду **"Выгрузить меню в Simphony"**.
3. **Система выполняет:**
   - Получает все позиции меню данного вида (кроме групп).
   - Для каждой позиции формирует запрос к API Simphony с актуальными данными (включая цену из регистра `ЦеныНоменклатуры`).
   - Отправляет запросы `PUT /menu-item` или `PATCH /menu-item`.
   - Записывает результаты в `MRS_ИсторияСинхронизацииМенюSimphony`.
4. **После завершения:**
   - Выводится сообщение: "Выгружено успешно: N позиций. Ошибок: M позиций."
   - При наличии ошибок предлагается открыть форму мониторинга.

**Постусловия:** Все позиции вида меню отправлены в Simphony, результаты записаны в историю.

**Примечание:** Данная команда выполняет немедленную выгрузку без использования очереди, для срочной синхронизации.

---

### Flow 4: Синхронизация классификаторов из Simphony

**Актор:** Регламентное задание (ежедневно)

**Основной поток:**
1. Задание поочередно выполняет запросы `GET /referencedata/major-groups` и `GET /referencedata/family-groups`.
2. На основе полученных данных создает или обновляет элементы в справочнике `ГруппыАналитическогоУчетаНоменклатуры`:
   - **Major Groups** → создаются/обновляются элементы 1-го уровня иерархии
   - **Family Groups** → создаются/обновляются элементы 2-го уровня иерархии (подчинены Major Group)
   - Коды из Simphony записываются в реквизиты `MRS_КодMajorGroup` и `MRS_КодFamilyGroup`

**Постусловия:** Иерархия классификаторов в 1С приведена в соответствие с Simphony.

---

### Flow 5: Массовая постановка в очередь на выгрузку

**Актор:** Менеджер меню  
**Предусловия:**
- В справочнике `питМеню` существуют позиции с привязкой к виду меню (`Владелец`).
- Для вида меню указан кассовый узел в плане обмена `питУдаленныеКассы`.
- У кассового узла заполнен реквизит `ВидЦен`.
- Для соответствующей номенклатуры есть записи в регистре `ЦеныНоменклатуры` с этим видом цен.

**Основной поток:**

1. Пользователь открывает форму списка **питМеню** (форма `МФормаСписка` с деревом групп).
2. Пользователь выделяет элемент(ы) в форме:
   - Одну или несколько позиций меню в списке справа.
   - Группу в дереве слева.
   - Или не выделяет ничего (будет обработан весь вид меню).
3. Пользователь нажимает команду **"Поставить в очередь выгрузки"** (кнопка на форме).
4. **Система задает вопрос пользователю о области действия:**
   - **Вариант A:** "Обработать выбранные элементы" (если выделены позиции в списке).
   - **Вариант B:** "Обработать все элементы в выбранной группе" (если выделена группа в дереве).
   - **Вариант C:** "Обработать все элементы из выбранного вида меню (реквизит Владелец), кроме групп" (весь вид меню).
5. Пользователь выбирает нужный вариант обработки.
6. **Для каждой позиции в области действия система выполняет:**
   1. Определяет вид меню позиции (`питМеню.Владелец` → `питВидыМеню`).
   2. По виду меню находит кассовый узел, выполнив запрос к табличной части `питУдаленныеКассы.ВидыМеню`.
   3. Проверяет наличие цены в регистре `ЦеныНоменклатуры` (отбор: `Номенклатура` = `питМеню.Номенклатура`, `ВидЦены` = `ВидЦен из узла`).
   4. Если цена найдена:
      - Создает/обновляет запись в регистре `MRS_ОчередьВыгрузкиМенюSimphony` со статусом "ВОчереди".
   5. Если цена не найдена:
      - Позиция пропускается, фиксируется в логе (опционально).
7. **После обработки всех позиций:**
   - Система выводит сообщение с результатом: "Поставлено в очередь: N позиций. Пропущено: M (не найдена цена)".

**Альтернативный поток:**
- **A1: Отмена пользователем:** Если пользователь закрывает диалог выбора области действия или нажимает "Отмена" → операция прерывается без выполнения.
- **A2: Кассовый узел не найден:** Если для вида меню не найден кассовый узел → вывести предупреждение "Не найден кассовый узел для вида меню X", операция прерывается.
- **A3: Вид цен не заполнен:** Если у кассового узла не заполнен `ВидЦен` → вывести предупреждение "Не указан вид цен для кассового узла Y", операция прерывается.

**Постусловия:** Выбранные позиции поставлены в очередь на выгрузку. Автоматическое регламентное задание выполнит их синхронизацию с Simphony.

---

## 4. Data & Integrations

### Основные сущности 1С

#### ✅ Справочник: питМеню (Menu Items)

**Добавленный реквизит:**

| Реквизит | Тип | Длина | Примечание |
|----------|-----|-------|------------|
| `MRS_КодSimphony` | Число | 10, 0 | `stringNumberId` из Simphony |

**Существующие реквизиты (ИСПОЛЬЗУЮТСЯ):**
- `Владелец` (СправочникСсылка.питВидыМеню) — связь с видом меню.
- `Номенклатура` (СправочникСсылка.Номенклатура) — для получения цены из регистра `ЦеныНоменклатуры`.
- `НомерВГруппе` (Число) — используется как `fileNum` в API Simphony.

**Команды на форме списка:**
- **"Поставить в очередь выгрузки"** — массовая постановка позиций в очередь на синхронизацию.
- **"Выгрузить меню в Simphony"** — немедленная выгрузка всего вида меню.

**❌ Дополнительные реквизиты НЕ требуются!** Цены и статусы получаются динамически из регистров сведений.

---

#### ✅ ПланОбмена: питУдаленныеКассы (POS Outlets)

**Существующие реквизиты (ИСПОЛЬЗУЮТСЯ):**
- `ВидЦен` (СправочникСсылка.ВидыЦен) — для получения цены из регистра `ЦеныНоменклатуры`.
- `Префикс` (Строка) — **Используется как `hierStrucId` для Simphony API.**
- Табличная часть `ВидыМеню` (связь с видами меню).

**❌ НИЧЕГО добавлять не нужно!**

---

#### ✅ Справочник: ГруппыАналитическогоУчетаНоменклатуры (Классификаторы Major/Family Groups)

**Назначение:** Хранение иерархии классификаторов Simphony для связи с номенклатурой.

**Структура иерархии:**
- **1-й уровень (без родителя):** Major Group — основная группа (например, "FOOD", "ALC", "N/ALC")
- **2-й уровень (подчинен Major Group):** Family Group — подгруппа (например, "Горячие блюда", "Салаты", "Пиво")

**Добавленные реквизиты:**

| Реквизит | Тип | Длина | Назначение |
|----------|-----|-------|------------|
| `MRS_КодMajorGroup` | Число | 10, 0 | Код Major Group из Simphony (заполняется у элементов 1-го уровня) |
| `MRS_КодFamilyGroup` | Число | 10, 0 | Код Family Group из Simphony (заполняется у элементов 2-го уровня) |

**Связь с номенклатурой:** У каждой номенклатуры заполняется реквизит `ГруппаАналитическогоУчета`, который указывает на элемент 2-го уровня иерархии (Family Group). Родитель этого элемента — Major Group.

**Пример иерархии:**
```
FOOD (MRS_КодMajorGroup=1000001)
  └─ Горячие блюда (MRS_КодFamilyGroup=100)
  └─ Салаты (MRS_КодFamilyGroup=101)
ALC (MRS_КодMajorGroup=2000001)
  └─ Пиво (MRS_КодFamilyGroup=200)
  └─ Вино (MRS_КодFamilyGroup=201)
```

---

#### Прочие объекты
- **РегистрСведений: MRS_ОчередьВыгрузкиМенюSimphony** — очередь на выгрузку позиций.
- **РегистрСведений: MRS_ИсторияСинхронизацииМенюSimphony** — история всех операций синхронизации.
- **Перечисление: MRS_СтатусыСинхронизацииSimphony** — статусы синхронизации (НеВыгружено, ВОчереди, Выгружено, Ошибка, ТребуетВнимания).
- **Константа: MRS_URLСервисаSimphony** — базовый URL API Simphony.

**Примечание:** Статусы синхронизации НЕ хранятся в реквизитах справочника `питМеню`, а получаются запросами к регистрам `MRS_ОчередьВыгрузкиМенюSimphony` и `MRS_ИсторияСинхронизацииМенюSimphony`.

---

### Бизнес-логика получения цены для позиции меню

**Цепочка получения данных:**

1. **Входные данные:** Позиция меню (`питМеню`)
2. **Шаг 1:** Из позиции меню берем `Владелец` → получаем `питВидыМеню`
3. **Шаг 2:** По виду меню находим кассовый узел:
   - Запрос к табличной части `ПланОбмена.питУдаленныеКассы.ВидыМеню`
   - Условие: `ВидМеню = питВидыМеню.Ссылка`
   - Результат: `питУдаленныеКассы.Ссылка` (кассовый узел)
4. **Шаг 3:** Из кассового узла извлекаем реквизит `ВидЦен` (СправочникСсылка.ВидыЦен)
5. **Шаг 4:** Выполняем запрос к регистру `ЦеныНоменклатуры`:
   - Условия отбора:
     - `Номенклатура` = `питМеню.Номенклатура`
     - `ВидЦены` = `ВидЦен` (из кассового узла)
   - Результат: `Цена` (Число 15,2)


**Пример SQL-запроса для получения кассового узла:**

```sql
ВЫБРАТЬ
    питУдаленныеКассыВидыМеню.Ссылка КАК КассовыйУзел,
    питУдаленныеКассыВидыМеню.Ссылка.ВидЦен КАК ВидЦен,
    питУдаленныеКассыВидыМеню.Ссылка.Префикс КАК Префикс
ИЗ
    ПланОбмена.питУдаленныеКассы.ВидыМеню КАК питУдаленныеКассыВидыМеню
        ЛЕВОЕ СОЕДИНЕНИЕ Справочник.питВидыМеню КАК питВидыМеню
        ПО питУдаленныеКассыВидыМеню.ВидМеню = питВидыМеню.Ссылка
ГДЕ
    питВидыМеню.Ссылка = &ВидМеню
```

**Валидация:**
- Если кассовый узел не найден → **Ошибка:** "Не найден кассовый узел для вида меню [Наименование]"
- Если `ВидЦен` не заполнен → **Ошибка:** "Не указан вид цен для кассового узла [Наименование]"
- Если цена не найдена в регистре → **Предупреждение:** "Цена не найдена для номенклатуры [Наименование], вид цен [Наименование]"

---

### Маппинг данных: 1С ↔ Simphony API

#### `PUT /menu-item` (Create Single Item)

| Поле API | Источник 1С |
|----------|-------------|
| `name` | `питМеню.Наименование` |
| `stringNumberId` | `питМеню.MRS_КодSimphony` |
| `majGrpObjNum` | `питМеню.Номенклатура.ГруппаАналитическогоУчета.Родитель.MRS_КодMajorGroup` |
| `famGrpObjNum` | `питМеню.Номенклатура.ГруппаАналитическогоУчета.MRS_КодFamilyGroup` |
| `hierStrucId` | `питУдаленныеКассы.Префикс` |
| `menuItemClass`| **Константа = 1001** |
| `price` | Запрос к `РегистрСведений.ЦеныНоменклатуры` (по номенклатуре и виду цен узла) |
| `fileNum` | `питМеню.НомерВГруппе` |

**Примечание:** `price` получается динамически при формировании запроса через функцию `ПолучитьЦенуНоменклатуры(Номенклатура, КассовыйУзел)`.

#### `PATCH /menu-item` (Update Single Item)

| Поле API | Источник 1С |
|----------|-------------|
| `stringNumberId` | `питМеню.MRS_КодSimphony` |
| `hierStrucId` | `питУдаленныеКассы.Префикс` |
| `name` | `питМеню.Наименование` |
| `price` | Запрос к `РегистрСведений.ЦеныНоменклатуры` |
| `famGrpObjNum` | Через иерархию `ГруппыАналитическогоУчета` |
| `majGrpObjNum` | Через иерархию `ГруппыАналитическогоУчета` |

#### `DELETE /menu-item/{stringNumberId}` (Delete Single Item)

- **Path `stringNumberId`**: `питМеню.MRS_КодSimphony`
- **Query `hierStrucId`**: `питУдаленныеКассы.Префикс`

---

## 5. Metadata (1С Objects) — Итоговый статус

### ✅ НОВЫЕ объекты (созданы)
1. **Перечисление:** `MRS_СтатусыСинхронизацииSimphony` (НеВыгружено, ВОчереди, Выгружено, Ошибка, ТребуетВнимания)
2. **РегистрСведений:** `MRS_ОчередьВыгрузкиМенюSimphony` (измерения: ПозицияМеню, КассовыйУзел)
3. **РегистрСведений:** `MRS_ИсторияСинхронизацииМенюSimphony` (лог операций)
4. **Константа:** `MRS_URLСервисаSimphony`
5. **Общие модули:** `MRS_ИнтеграцияSimphonyСервер`, `MRS_ИнтеграцияSimphonyКлиент`, `MRS_ИнтеграцияSimphonyКлиентСервер`
6. **Формы:** `питМеню.MRS_ФормаМониторинга`, `питМеню.MRS_ФормаУправленияОчередью`
7. **Роль:** `MRS_ОсновнаяРоль`

### ⚠️ ТРЕБУЕТСЯ доработка существующих объектов

#### 1. Справочник `питМеню`
- ✅ Добавлен реквизит `MRS_КодSimphony` (Число 10,0)
- **Команды на форме списка:**
  - "Поставить в очередь выгрузки" — массовая постановка в очередь
  - "Выгрузить меню в Simphony" — немедленная выгрузка всего вида меню

#### 2. ПланОбмена `питУдаленныеКассы`
- ✅ Используются существующие реквизиты: `Префикс`, `ВидЦен`
- ✅ Используется табличная часть `ВидыМеню`
- **НИЧЕГО добавлять не нужно!**

#### 3. Справочник `ГруппыАналитическогоУчетаНоменклатуры`
- ✅ Добавлены реквизиты: `MRS_КодMajorGroup` (Число 10,0), `MRS_КодFamilyGroup` (Число 10,0)
- **Используется для хранения иерархии классификаторов Simphony:**
  - 1-й уровень = Major Groups
  - 2-й уровень = Family Groups

### 🔄 ТРЕБУЕТСЯ создать

#### Регламентные задания:
1. **`MRS_ВыгрузкаМенюSimphony`** — автоматическая обработка очереди (каждые 15 минут)
2. **`MRS_ОчисткаИсторииSimphony`** — удаление старых записей истории (ежедневно)
3. **`MRS_СинхронизацияКлассификаторовSimphony`** — обновление Major/Family Groups (ежедневно)

---

## 6. UI/UX спецификация команд формы списка питМеню

### Форма: МФормаСписка (справочник питМеню)

**Элементы интерфейса:**

| Элемент | Тип | Расположение | Название | Действие |
|---------|-----|--------------|----------|----------|
| Кнопка 1 | КнопкаКоманды | Командная панель | "Поставить в очередь выгрузки" | Массовая постановка в очередь |
| Кнопка 2 | КнопкаКоманды | Командная панель | "Выгрузить меню в Simphony" | Немедленная выгрузка всего вида меню |

---

### Команда 1: "Поставить в очередь выгрузки"

**Логика активности:**
- Кнопка доступна всегда (не зависит от выделения).
- При нажатии показывается диалог выбора области действия.

**Диалог выбора области действия:**

| Вариант | Текст варианта |
|---------|----------------|
| 1 | "Обработать выбранные элементы" |
| 2 | "Обработать все элементы в выбранной группе" |
| 3 | "Обработать все элементы из выбранного вида меню (кроме групп)" |

**Сообщения пользователю:**

| Ситуация | Тип | Текст |
|----------|-----|-------|
| Успех | Информация | "Поставлено в очередь: N позиций. Пропущено: M (не найдена цена)." |
| Отмена | — | Операция отменяется без сообщения |
| Нет кассового узла | Предупреждение | "Не найден кассовый узел для вида меню '[Наименование]'." |
| Не заполнен ВидЦен | Предупреждение | "Не указан вид цен для кассового узла '[Наименование]'." |
| Нет позиций | Предупреждение | "Не найдено позиций меню для обработки." |

---

### Команда 2: "Выгрузить меню в Simphony"

**Назначение:** Немедленная выгрузка всех позиций вида меню в Simphony без использования очереди.

**Логика активности:**
- Кнопка доступна, если форма открыта с отбором по виду меню (параметр `Владелец`).

**Выполнение:**
1. Система собирает все позиции вида меню (кроме групп).
2. Для каждой позиции формирует запрос к API Simphony с актуальными данными (цена из регистра `ЦеныНоменклатуры`).
3. Отправляет запросы `PUT /menu-item` или `PATCH /menu-item`.
4. Записывает результаты в `MRS_ИсторияСинхронизацииМенюSimphony`.

**Сообщения пользователю:**

| Ситуация | Тип | Текст |
|----------|-----|-------|
| Успех полный | Информация | "Выгружено успешно: N позиций." |
| Успех частичный | Предупреждение | "Выгружено успешно: N позиций. Ошибок: M. Открыть форму мониторинга?" |
| Ошибка | Ошибка | "Ошибка выгрузки: [текст ошибки]" |

**Поведение после выполнения:**
- Если были ошибки — предлагается открыть форму мониторинга для просмотра деталей.

---

## 7. Use Cases (Сценарии использования)

### UC-1: Немедленная выгрузка нового меню
**Сценарий:** В ресторане подготовлено новое сезонное меню, требуется срочно выгрузить его в Simphony.

**Действия менеджера:**
1. Открывает форму списка `питМеню` для вида меню "Сезонное меню - Весна 2025"
2. Нажимает кнопку **"Выгрузить меню в Simphony"**
3. Система:
   - Собирает все 35 позиций вида меню
   - Для каждой позиции получает актуальную цену из регистра `ЦеныНоменклатуры`
   - Отправляет запросы в Simphony API
4. После завершения показывается: "Выгружено успешно: 35 позиций"

**Результат:** Новое меню выгружено в Simphony за 2 минуты, готово к продажам.

---

### UC-1a: Массовая постановка в очередь при изменении цен
**Сценарий:** В ресторане запускается акция "Счастливые часы", обновлены цены на напитки в регистре `ЦеныНоменклатуры`.

**Действия менеджера:**
1. Открывает форму списка `питМеню`
2. В дереве слева выбирает группу "Напитки"
3. Нажимает **"Поставить в очередь выгрузки"**
4. Система показывает диалог выбора области действия
5. Менеджер выбирает "Обработать все элементы в выбранной группе"
6. Система ставит 20 позиций в очередь выгрузки
7. Регламентное задание (каждые 15 минут) синхронизирует изменения с Simphony

**Результат:** 20 позиций поставлены в очередь за 10 секунд, автоматически выгружены в течение 15 минут.

---

### UC-2: Открытие новой точки продаж с собственным меню
**Сценарий:** Открывается новый киоск с ограниченным ассортиментом и специальными ценами.

**Действия администратора:**
1. Создает новый узел в `питУдаленныеКассы`, заполняет `Префикс` (hierStrucId) и `ВидЦен` ("Киоск")
2. Создает новый вид меню "Киоск - Летнее меню", привязывает к кассовому узлу
3. Создает позиции меню для киоска (15 позиций)
4. Заполняет цены в регистре `ЦеныНоменклатуры` по виду цен "Киоск"
5. Открывает форму списка для вида меню "Киоск - Летнее меню"
6. Нажимает **"Выгрузить меню в Simphony"**
7. Система выгружает все 15 позиций с ценами из регистра

**Результат:** Меню киоска выгружено в Simphony за 1 минуту, готово к работе.

---

### UC-3: Корректировка цены на одну позицию
**Сценарий:** Изменилась закупочная цена на кофе, нужно синхронизировать с Simphony.

**Действия менеджера:**
1. Обновляет цену в регистре `ЦеныНоменклатуры` для номенклатуры "Капучино"
2. Открывает форму списка `питМеню`
3. Находит позицию "Капучино", выделяет её
4. Нажимает **"Поставить в очередь выгрузки"**
5. Выбирает "Обработать выбранные элементы"
6. Система ставит позицию в очередь
7. Регламентное задание синхронизирует изменение с Simphony (актуальная цена берется из регистра)

**Результат:** Позиция поставлена в очередь за 5 секунд, синхронизирована в течение 15 минут.

---

## 8. Допущения и ограничения

### Допущения (Assumptions)
1. **Структура данных:** Предполагается, что структура плана обмена `питУдаленныеКассы` уже содержит табличную часть `ВидыМеню` с полями `ВидМеню` и реквизит `ВидЦен`.
2. **Регистр цен:** Регистр `ЦеныНоменклатуры` актуален и содержит все необходимые виды цен для каждой точки продаж.
3. **Уникальность связи:** Один вид меню связан только с одним кассовым узлом.
4. **Номенклатура заполнена:** У всех позиций меню корректно заполнен реквизит `Номенклатура`.
5. **Префикс = hierStrucId:** Значение реквизита `Префикс` в плане обмена соответствует идентификатору точки продаж (RVC) в Simphony.

### Технические ограничения
1. **Производительность:** Одновременная обработка не более 1000 позиций меню за один вызов команды.
2. **Время ожидания:** Операция должна завершаться не дольше 30 секунд для 100 позиций.
3. **Блокировки:** При массовом обновлении возможны блокировки записей, если другой пользователь редактирует ту же позицию.
4. **Права доступа:** Пользователь должен иметь права на изменение справочника `питМеню`.
5. **Кэширование:** Кассовые узлы и виды цен кэшируются на время выполнения операции для оптимизации.

### Бизнес-ограничения
1. **Один вид цен на узел:** Каждый кассовый узел работает только с одним видом цен.
2. **Ручное управление регистром цен:** Цены в регистре `ЦеныНоменклатуры` управляются вне данной функциональности (документ `УстановкаЦенНоменклатуры`).
3. **Динамическое получение данных:** Цены и статусы всегда получаются из регистров сведений при формировании запроса, не кэшируются в справочнике.
4. **Иерархия классификаторов:** Обязательна корректная иерархия в `ГруппыАналитическогоУчетаНоменклатуры` (2 уровня: Major/Family Groups).

---

## 9. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Цены не найдены в регистре для части позиций | Высокая | Среднее | Пропускать позиции с недостающими ценами, выводить предупреждение с перечнем |
| Кассовый узел не найден для вида меню | Средняя | Высокое | Валидация на этапе создания вида меню + предупреждение при установке цен |
| Низкая производительность при большом объеме | Средняя | Среднее | Ограничение на 1000 позиций, пакетная обработка, индексация полей |
| Конфликт версий при одновременном редактировании | Низкая | Низкое | Использовать управляемые блокировки 1С |
| Неверный `Префикс` в плане обмена | Средняя | Высокое | Валидация формата префикса при создании узла, документация по заполнению |
| Пользователь забывает запускать выгрузку после установки цен | Высокая | Среднее | Автоматическая постановка в очередь + регламентное задание |

---

## 10. Приоритизация функций (MoSCoW)

### Must Have (Критично для запуска)
1. ✅ Создание и обновление позиций меню в Simphony (API интеграция)
2. ✅ Отслеживание статусов синхронизации
3. ✅ Очередь выгрузки с механизмом повторных попыток
4. ✅ Логирование операций
5. ✅ Установка цен на одну позицию меню

### Should Have (Важно, но можно отложить)
6. ✅ Массовая установка цен по группе
7. ✅ Массовая установка цен по виду меню
8. ✅ Синхронизация справочников из Simphony
9. ✅ Автоматическая выгрузка по расписанию

### Could Have (Желательно)
10. ✅ Пакетная обработка (bulk API)
11. ⚠️ Расширенная аналитика по ошибкам синхронизации
12. ⚠️ Уведомления при критических ошибках

### Won't Have (Исключено из текущей версии)
13. ❌ Синхронизация транзакций продаж (отдельный план обмена)
14. ❌ Управление рецептурами через API
15. ❌ Мобильное приложение

---

## 11. Acceptance Criteria (Критерии приемки)

### Функциональные критерии

#### AC-1: Постановка выбранных элементов в очередь
- ✅ GIVEN: Пользователь выделил одну или несколько позиций в списке меню
- ✅ WHEN: Нажимает кнопку "Поставить в очередь выгрузки"
- ✅ THEN: 
  - Система показывает диалог с вопросом о области действия
  - Пользователь выбирает "Обработать выбранные элементы"
  - Система обрабатывает только выделенные позиции:
    - Находит вид меню позиции
    - Находит кассовый узел по виду меню
    - Извлекает вид цен из кассового узла
    - Проверяет наличие цены в регистре `ЦеныНоменклатуры`
    - Создает запись в `MRS_ОчередьВыгрузкиМенюSimphony` со статусом "ВОчереди"
  - Выводит сообщение "Поставлено в очередь: N позиций. Пропущено: M."

#### AC-2: Постановка в очередь всех элементов группы
- ✅ GIVEN: Пользователь выделил группу в дереве (например, "Напитки")
- ✅ WHEN: Нажимает кнопку "Поставить в очередь выгрузки"
- ✅ THEN:
  - Система показывает диалог с вопросом о области действия
  - Пользователь выбирает "Обработать все элементы в выбранной группе"
  - Система находит все позиции меню в этой группе
  - Обрабатывает каждую позицию согласно AC-1
  - Выводит сообщение с количеством поставленных в очередь и пропущенных позиций

#### AC-3: Немедленная выгрузка всего вида меню
- ✅ GIVEN: Пользователь открыл форму списка меню для конкретного вида меню
- ✅ WHEN: Нажимает кнопку "Выгрузить меню в Simphony"
- ✅ THEN:
  - Система собирает все позиции меню данного вида (исключая группы)
  - Для каждой позиции:
    - Получает актуальную цену из регистра `ЦеныНоменклатуры`
    - Формирует запрос к API Simphony (`PUT` или `PATCH`)
    - Отправляет запрос
    - Записывает результат в `MRS_ИсторияСинхронизацииМенюSimphony`
  - Выводит сообщение с результатом выгрузки
  - При наличии ошибок предлагает открыть форму мониторинга

#### AC-4: Отмена операции пользователем
- ✅ GIVEN: Пользователь нажал кнопку "Поставить в очередь выгрузки"
- ✅ WHEN: В диалоге выбора области действия нажимает "Отмена" или закрывает диалог
- ✅ THEN:
  - Операция не выполняется
  - Изменения в данные не вносятся
  - Сообщения пользователю не выводятся
  - Форма остается в исходном состоянии

#### AC-5: Обработка отсутствия цены
- ✅ GIVEN: Для номенклатуры нет записи в регистре `ЦеныНоменклатуры` с нужным видом цен
- ✅ WHEN: Выполняется постановка в очередь или выгрузка
- ✅ THEN:
  - Позиция пропускается (не ставится в очередь / не выгружается)
  - В итоговом сообщении увеличивается счетчик пропущенных
  - Опционально: в лог записывается предупреждение

#### AC-6: Обработка отсутствия кассового узла
- ✅ GIVEN: Для вида меню не найден кассовый узел в плане обмена
- ✅ WHEN: Выполняется постановка в очередь или выгрузка
- ✅ THEN:
  - Выводится предупреждение "Не найден кассовый узел для вида меню '[Наименование]'"
  - Обработка останавливается

#### AC-7: Обработка отсутствия вида цен
- ✅ GIVEN: У кассового узла не заполнен реквизит `ВидЦен`
- ✅ WHEN: Выполняется постановка в очередь или выгрузка
- ✅ THEN:
  - Выводится предупреждение "Не указан вид цен для кассового узла '[Наименование]'"
  - Обработка останавливается

### Нефункциональные критерии

#### AC-8: Производительность
- ✅ Обработка 100 позиций завершается не дольше 30 секунд
- ✅ Обработка 1000 позиций завершается не дольше 5 минут
- ✅ Операция не блокирует UI (выполняется на сервере)

#### AC-9: Безопасность
- ✅ Проверка прав доступа на изменение справочника `питМеню`
- ✅ Пользователь без прав получает сообщение об ошибке доступа

#### AC-10: Юзабилити
- ✅ Кнопки "Поставить в очередь выгрузки" и "Выгрузить меню в Simphony" видны и доступны на форме списка
- ✅ Диалог выбора области действия понятен и интуитивен (для команды постановки в очередь)
- ✅ Пользователь может отменить операцию на этапе выбора области действия
- ✅ Сообщения пользователю информативны и понятны
- ✅ Операция немедленной выгрузки показывает прогресс (опционально)
- ✅ Операция может быть прервана (Ctrl+Break) во время обработки

---

## 12. Technical Design (Техническая архитектура)

### Модули и их ответственность

#### 1. Команда: `MRS_ПоставитьВОчередьВыгрузки`
**Расположение:** Форма `МФормаСписка` справочника `питМеню`  
**Тип:** Команда формы  
**Ответственность:** 
- Показ диалога выбора области действия
- Формирование списка позиций для обработки
- Постановка позиций в регистр `MRS_ОчередьВыгрузкиМенюSimphony`

**Алгоритм:**
1. Показать диалог выбора области действия (3 варианта)
2. Получить список позиций меню в зависимости от выбора
3. Для каждой позиции:
   - Найти кассовый узел по виду меню
   - Проверить наличие цены в `ЦеныНоменклатуры`
   - Создать/обновить запись в `MRS_ОчередьВыгрузкиМенюSimphony`
4. Вывести итоговое сообщение

---

#### 2. Команда: `MRS_ВыгрузитьМенюВSimphony`
**Расположение:** Форма `МФормаСписка` справочника `питМеню`  
**Тип:** Команда формы  
**Ответственность:** 
- Немедленная выгрузка всех позиций вида меню в Simphony
- Обход очереди для срочной синхронизации

**Алгоритм:**
1. Получить все позиции вида меню (кроме групп)
2. Для каждой позиции:
   - Получить актуальную цену из `ЦеныНоменклатуры`
   - Вызвать `MRS_ИнтеграцияSimphonyСервер.ВыгрузитьПозициюВSimphony()`
   - Записать результат в `MRS_ИсторияСинхронизацииМенюSimphony`
3. Вывести итоговое сообщение с статистикой
4. При наличии ошибок предложить открыть форму мониторинга

---

#### 3. Общий модуль: `MRS_ИнтеграцияSimphonyСервер`
**Расположение:** Общий модуль (серверный)  
**Ответственность:**
- Формирование запросов к API Simphony
- Получение актуальных цен и классификаторов из регистров
- Валидация данных перед выгрузкой
- Логирование операций

**Ключевые функции:**

##### `ВыгрузитьПозициюВSimphony(Позиция, КассовыйУзел)`
- Формирует JSON запрос с данными позиции
- Получает цену из `ЦеныНоменклатуры` динамически
- Определяет Major/Family Group через иерархию `ГруппыАналитическогоУчета`
- Отправляет запрос к API (`PUT` или `PATCH`)
- Записывает результат в `MRS_ИсторияСинхронизацииМенюSimphony`

##### `ПолучитьЦенуНоменклатуры(Номенклатура, КассовыйУзел)`
- Извлекает `ВидЦен` из кассового узла
- Выполняет запрос к `РегистрСведений.ЦеныНоменклатуры`
- Возвращает актуальную цену или Неопределено

##### `ПолучитьКассовыйУзелДляВидаМеню(ВидМеню)`
- Запрос к табличной части `питУдаленныеКассы.ВидыМеню`
- Возвращает ПланОбменаСсылка.питУдаленныеКассы

**Примечание:** Все данные (цены, статусы) получаются динамически при формировании запроса, не требуют предварительного сохранения в справочнике `питМеню`.

---

## 13. Implementation Steps (План внедрения)

1.  **Шаг 1: Проверка метаданных (5 минут)**
    - ✅ Убедиться, что реквизит `MRS_КодSimphony` добавлен в `питМеню`
    - ✅ Убедиться, что реквизиты `MRS_КодMajorGroup` и `MRS_КодFamilyGroup` добавлены в `ГруппыАналитическогоУчетаНоменклатуры`
    - ✅ Проверить, что все регистры сведений и перечисления созданы

2.  **Шаг 2: Настройка форм (10 минут)**
    - Добавить команды на форму `МФормаСписка`:
      - "Поставить в очередь выгрузки"
      - "Выгрузить меню в Simphony"
    - Реализовать обработчики команд в модуле формы

3.  **Шаг 3: Настройка интеграции (5 минут)**
    - Установить URL API Simphony в константе `MRS_URLСервисаSimphony`
    - Проверить доступность API

4.  **Шаг 4: Настройка данных в 1С (20 минут)**
    - Для всех узлов в `питУдаленныеКассы`:
      - Заполнить `Префикс` значениями `hierStrucId` из Simphony
      - Заполнить `ВидЦен`
      - Привязать виды меню в табличной части `ВидыМеню`
    - Настроить иерархию в `ГруппыАналитическогоУчетаНоменклатуры` (2 уровня)
    - Заполнить цены в регистре `ЦеныНоменклатуры`

5.  **Шаг 5: Синхронизация классификаторов (10 минут)**
    - Запустить вручную регл. задание `MRS_СинхронизацияКлассификаторовSimphony`
    - Проверить, что иерархия `ГруппыАналитическогоУчета` заполнилась кодами из Simphony
    - Привязать номенклатуру к элементам 2-го уровня (Family Groups)

6.  **Шаг 6: Создание регламентных заданий (5 минут)**
    - Создать задание `MRS_ВыгрузкаМенюSimphony` (каждые 15 минут)
    - Создать задание `MRS_ОчисткаИсторииSimphony` (ежедневно)
    - Создать задание `MRS_СинхронизацияКлассификаторовSimphony` (ежедневно)

7.  **Шаг 7: Тестирование (15 минут)**
    - Открыть форму списка `питМеню`
    - Протестировать "Поставить в очередь выгрузки" (выборочно, группа, весь вид)
    - Протестировать "Выгрузить меню в Simphony" (немедленная выгрузка)
    - Проверить корректность получения цен из регистра `ЦеныНоменклатуры`
    - Проверить очередь в форме `MRS_ФормаУправленияОчередью`
    - Проверить историю в форме `MRS_ФормаМониторинга`
    - Убедиться, что данные корректно отображаются в Simphony

---

## 14. Глоссарий терминов

| Термин | Описание |
|--------|----------|
| **питМеню** | Справочник 1С, содержащий позиции меню для точек продаж |
| **питВидыМеню** | Справочник 1С, содержащий виды меню (например, "Завтрак", "Обед", "Бар") |
| **питУдаленныеКассы** | План обмена 1С, описывающий кассовые узлы (точки продаж) |
| **Префикс** | Реквизит плана обмена, используется как `hierStrucId` для Simphony API |
| **ВидЦен** | Справочник 1С, определяющий тип цены (например, "Розничная", "Акция", "VIP") |
| **ЦеныНоменклатуры** | Регистр сведений 1С, хранящий актуальные цены номенклатуры по различным видам цен |
| **hierStrucId** | Идентификатор точки продаж (RVC) в Oracle Simphony POS |
| **stringNumberId** | Уникальный числовой идентификатор позиции меню в Simphony |
| **Major Group** | Основная группа классификации меню в Simphony (например, "Еда", "Напитки") |
| **Family Group** | Подгруппа классификации меню в Simphony (например, "Горячие напитки", "Салаты") |
| **RVC** (Revenue Center) | Точка продаж в Simphony (кассовый узел, ресторан, бар и т.д.) |
| **Simphony POS** | Oracle Simphony Point of Sale — система управления продажами на кассах |
| **MRS_** | Префикс для всех новых объектов метаданных в рамках интеграции |
| **Статус синхронизации** | Состояние выгрузки позиции: НеВыгружено, ВОчереди, Выгружено, Ошибка |
| **Очередь выгрузки** | Регистр сведений `MRS_ОчередьВыгрузкиМенюSimphony`, содержащий позиции, ожидающие синхронизации |
| **История синхронизации** | Регистр сведений `MRS_ИсторияСинхронизацииМенюSimphony`, хранящий лог всех операций синхронизации |
| **Bulk API** | Пакетный API для одновременной обработки нескольких позиций меню |
| **menuItemClass** | Класс позиции меню в Simphony (константа = 1001) |
| **fileNum** | Номер позиции в группе меню (`питМеню.НомерВГруппе`) |

---

## 15. Контакты и поддержка

**Product Owner:** Менеджер по продукту  
**Tech Lead:** Senior 1C Developer  
**Дата готовности:** 10.11.2025  
**Версия системы:** 1С:ERP 2.5.17.103 + Catering + КТ-2000  

**Вопросы по PRD:** Обращаться к Product Manager  
**Технические вопросы:** Обращаться к Tech Lead  

---

**© 2025 | PRD v1.4 | Статус: ✅ Ready for Implementation**

---

## Changelog

### v1.4 (10.11.2025) — Упрощение архитектуры
- ❌ Отказ от создания справочников `MRS_MajorGroups` и `MRS_FamilyGroups` → используется иерархия `ГруппыАналитическогоУчетаНоменклатуры` (2 уровня)
- ❌ Отказ от реквизитов `MRS_Цена` и `MRS_СтатусСинхронизацииSimphony` в `питМеню` → данные получаются динамически из регистров
- ✅ Добавлена команда "Выгрузить меню в Simphony" для немедленной выгрузки всего вида меню
- 🔄 Переименована команда "Установить цены" → "Поставить в очередь выгрузки"

### v1.3 (10.11.2025) — Массовая установка цен
- Добавлена функциональность массового обновления цен на позиции меню

### v1.2 (начало проекта)
- Базовая архитектура интеграции на основе RVC
