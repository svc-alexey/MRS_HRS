# План имплементации: Интеграция 1С:ERP ↔ ELMA (Приказы на установку цен)

**Версия:** 1.0  
**Дата:** 14.11.2025  
**Статус:** 📋 Ready for Development

---

## 1. Обзор интеграции

### Назначение
Автоматическая синхронизация документов **"Установка цен номенклатуры"** из 1С:ERP с системой ELMA для запуска процесса согласования и подписания приказов по организации.

### Ключевые принципы
- **Архитектура аналогична Simphony:** Используем те же подходы (очередь, регистры истории, общие модули)
- **Идемпотентность:** Повторная отправка не создает дубли в ELMA
- **Асинхронность:** Основная работа через очередь с регламентным заданием
- **Дата применения цен:** Документ согласовывается заранее, проводится и выгружается в Simphony только после подписания и достижения даты начала действия

---

## 2. Бизнес-логика (детальная)

### 2.1. Жизненный цикл документа "Установка цен"

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Создание документа в 1С                                      │
│    - Дата документа                                             │
│    - Дата начала действия цен (может быть в будущем)            │
│    - Организация (ИНН → ELMA Organization.Uid)                  │
│    - Табличная часть с ценами                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Команда "Отправить в ELMA" (только администратор)            │
│    - Валидация данных                                           │
│    - Генерация Word-файла из шаблона (DecreeType.Template)     │
│    - Заполнение полей приказа                                   │
│    - Постановка в очередь MRS_ОчередьОтправкиВELMA              │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Регламентное задание "Обработка очереди ELMA"                │
│    - Выборка записей со статусом "ВОчереди" или "Ошибка"        │
│    - Авторизация в ELMA (AuthToken)                             │
│    - POST /API/REST/Entity/Insert или Update                    │
│    - Получение entityId/Uid из ответа                           │
│    - Сохранение связки в регистре MRS_СвязкиДокументовELMA      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Регламентное задание "Опрос статусов ELMA"                   │
│    - Выборка документов со связкой ELMA                         │
│    - GET /API/REST/Entity/Load для каждого документа            │
│    - Анализ статуса процесса (WorkflowInstance)                 │
│    - Обновление статуса в регистре MRS_СтатусыПриказовELMA      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Проверка условий для проведения документа                    │
│    ЕСЛИ:                                                        │
│    ✓ Статус в ELMA = "Подписан"                                │
│    ✓ ТекущаяДата >= ДатаНачалаДействияЦен                      │
│    ТО:                                                          │
│    → Провести документ "Установка цен"                          │
│    → Записать цены в регистр ЦеныНоменклатуры                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. Выгрузка цен в Simphony (существующая логика)                │
│    - Отбор по видам цен из питУдаленныеКассы.ВидЦен             │
│    - Постановка в очередь MRS_ОчередьВыгрузкиМенюSimphony       │
│    - Синхронизация с Simphony через API                         │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2. Статусы документа в процессе

| Статус ELMA | Действие в 1С | Выгрузка в Simphony |
|-------------|---------------|---------------------|
| НеОтправлен | Ожидание команды "Отправить в ELMA" | ❌ Нет |
| НаСогласовании | Ожидание согласования в ELMA | ❌ Нет |
| Согласован | Ожидание подписания в ELMA | ❌ Нет |
| Подписан | Проверка даты → Проведение документа | ✅ Да (после проведения) |
| Отклонен | Только запись статуса, ничего не делать | ❌ Нет |
| Отменен | Снять с проведения (если был проведен) | ⚠️ Откат изменений |

### 2.3. Логика отправки в ELMA (Insert vs Update)

```javascript
ЕСЛИ (существует связка в MRS_СвязкиДокументовELMA) ТОГДА
    // Update существующего приказа
    Метод = "POST"
    URL = "/API/REST/Entity/Update/{TypeUid}/{EntityUid}"
    EntityUid = связка.UIDELMA
ИНАЧЕ
    // Insert нового приказа
    Метод = "POST"
    URL = "/API/REST/Entity/Insert/{TypeUid}"
    После успешного ответа → сохранить Id/Uid в связку
КОНЕЦЕСЛИ
```

---

## 3. Архитектура данных

### 3.1. Регистр сведений (единственный!)

#### РегистрСведений.MRS_ДокументыELMA
**Назначение:** Полное состояние документа в процессе интеграции с ELMA

**Периодичность:** Непериодический  
**Режим записи:** Независимый

| Измерение | Тип | Описание |
|-----------|-----|----------|
| Документ1С | ДокументСсылка.УстановкаЦенНоменклатуры | Ссылка на документ |

| Ресурс | Тип | Описание |
|--------|-----|----------|
| **UidELMA** | Строка (36) | **GUID процесса в ELMA** (главное!) |
| IdELMA | Число (15,0) | Числовой Id сущности в ELMA |
| Статус | ПеречислениеСсылка.MRS_СтатусыСинхронизацииELMA | Статус обработки |
| СтатусELMA | Строка (50) | Текстовый статус из ELMA |
| ДатаОтправки | Дата | Момент отправки в ELMA |
| ДатаПоследнегоОпроса | Дата | Момент последнего опроса ELMA |
| КомментарийELMA | Строка (500) | Комментарий (при отклонении) |
| КоличествоПопыток | Число (3,0) | Счётчик попыток отправки |
| ТекстОшибки | Строка (500) | Текст последней ошибки |
| ДатаПоследнейПопытки | Дата | Момент последней попытки отправки |

**Индексы:**
- По полю `Статус` (для выборки очереди)
- По полю `UidELMA` (для поиска по Uid ELMA)

**Логика работы:**
1. **Постановка в очередь:** Статус = "ВОчереди", КоличествоПопыток = 0
2. **РЗ обработки:** Выбирает записи со статусом "ВОчереди" или "Ошибка" (попыток < 3)
3. **После успешной отправки:** UidELMA заполнен, Статус = "НаСогласовании"
4. **РЗ опроса:** Обновляет СтатусELMA из ELMA по UidELMA
5. **Финальные статусы:** "Подписан", "Отклонен", "Отменен" — просто хранятся
6. **Детали процесса:** Смотрим в ELMA по UidELMA!

### 3.2. Новое перечисление

#### Перечисление.MRS_СтатусыСинхронизацииELMA
- НеОтправлен
- ВОчереди
- Отправлен
- Ошибка
- ТребуетВнимания

### 3.2. Константы

| Константа | Тип | Назначение |
|-----------|-----|------------|
| MRS_URLСервисаELMA | Строка (200) | Базовый URL ELMA API |
| MRS_ApplicationTokenELMA | Строка (100) | Токен приложения ELMA |
| MRS_ИмяПользователяELMA | Строка (100) | Логин для авторизации |
| MRS_ПарольELMA | Строка (100) | Пароль (зашифрованный) |
| MRS_TypeUidПриказаELMA | Строка (36) | GUID типа "Оформление приказа" |
| MRS_DecreeTypeUidELMA | Строка (36) | GUID типа приказа на установку цен |
| MRS_ТаймаутЗапросаELMA | Число (10,0) | Таймаут в секундах (по умолчанию 30) |

---

## 4. Маппинг полей: Документ 1С → ELMA WebData

### 4.1. Основные поля приказа

| Поле ELMA | Источник 1С | Тип | Примечание |
|-----------|-------------|-----|------------|
| **Organization** (по ИНН) | УстановкаЦен.Организация.ИНН | String | Передаём ИНН, ELMA найдёт по нему Organization |
| **DecreeType.Uid** | Константа MRS_DecreeTypeUidELMA | GUID | `70f51fc1-1c5c-4ef2-870a-c1bec8788322` |
| **Date** | УстановкаЦен.Дата | DateTime | Дата документа |
| **StartDate** | УстановкаЦен.ДатаНачалаДействия | DateTime | Дата вступления в силу |
| **DecreeNumber** | УстановкаЦен.Номер | String | Номер документа |
| **DecreeSubject** | Шаблон + данные | String | "Об установке цен {ВидЦен} для {Организация}" |
| **Initiator** (по Email) | ТекущийПользователь.Email | String | Email пользователя, ELMA найдёт Initiator |
| **Preamble** | Шаблонный текст | String | "В целях актуализации прейскуранта" |
| **SignatoryPosition** | Из организации или константа | String | "Генеральный директор" |

### 4.2. Коллекция DecreeContents (табличная часть)

```json
"DecreeContents": [
    {
        "Number": "1",
        "Content": "Установить цены согласно приложению №1."
    },
    {
        "Number": "2", 
        "Content": "Контроль за внесением изменений возложить на финансового контролера."
    },
    {
        "Number": "3",
        "Content": "Контроль за исполнением приказа возложить на директора департамента."
    }
]
```

**Источник:** Генерируется программно на основе шаблона и данных документа.

### 4.3. Файловое вложение (DecreeVersion)

**Алгоритм формирования Word-файла:**

1. **Получение шаблона:**
   - Справочник "Типы приказов" → поле `Template` (Uid шаблона в ELMA)
   - GET `/API/REST/Entity/Load?type={DecreeTypeUid}&id={DecreeTypeId}`
   - Извлечение файла шаблона

2. **Заполнение полей:**
   - Замена переменных вида `{$Legal.Name}`, `{$DateOfDecree}` и т.д.
   - Использование COM-объекта Word.Application (на сервере 1С)
   - Заполнение табличной части с ценами

3. **Сохранение и отправка:**
   - Сохранение в временную папку
   - Кодирование в Base64
   - Передача в поле `DecreeVersion` или `File` (уточнить в ELMA API)

---

## 5. Логика общих модулей

### 5.1. MRS_ИнтеграцияELMAСервер (серверный, вызов сервера)

#### Функция: `ПоставитьДокументВОчередьELMA(ДокументСсылка)`
**Назначение:** Постановка документа в очередь на отправку

**Алгоритм:**
```javascript
1. Проверить права доступа (только администратор)
2. Валидировать документ:
   - Заполнена организация
   - Заполнена дата начала действия
   - Есть строки в табличной части
   - У организации заполнен ИНН
3. Создать/обновить запись в MRS_ДокументыELMA:
   - Документ1С = ДокументСсылка
   - Статус = "ВОчереди"
   - ДатаОтправки = Неопределено (пока не отправлен)
   - КоличествоПопыток = 0
   - ТекстОшибки = ""
4. Вернуть Истина
```

#### Функция: `ОбработатьОчередьELMA()`
**Назначение:** Регламентное задание - обработка очереди

**Алгоритм:**
```javascript
1. Авторизоваться в ELMA (получить AuthToken)
2. Выбрать из MRS_ДокументыELMA записи:
   - Статус = "ВОчереди" ИЛИ (Статус = "Ошибка" И КоличествоПопыток < 3)
   - Упорядочить по ДатаПоследнейПопытки (старые первыми)
   - Ограничение: 10 записей
3. Для каждой записи:
   а) Получить данные документа
   б) Сформировать JSON WebData (ИНН, данные документа)
   в) Определить Insert или Update (по наличию UidELMA)
   г) Вызвать POST /API/REST/Entity/Insert или Update
   д) Обработать ответ:
      - Успех (200): 
        * Сохранить UidELMA и IdELMA из ответа
        * Статус = "НаСогласовании"
        * ДатаОтправки = ТекущаяДата()
        * ТекстОшибки = ""
      - Ошибка (4xx, 5xx):
        * Увеличить КоличествоПопыток
        * Записать ТекстОшибки
        * ДатаПоследнейПопытки = ТекущаяДата()
        * Если попыток >= 3: Статус = "ТребуетВнимания"
4. Вернуть статистику обработки (отправлено, ошибок)
```

#### Функция: `ОпроситьСтатусыПриказовELMA()`
**Назначение:** Регламентное задание - опрос статусов из ELMA

**Алгоритм:**
```javascript
1. Авторизоваться в ELMA
2. Выбрать из MRS_ДокументыELMA записи, где:
   - UidELMA заполнен (отправлен в ELMA)
   - Статус НЕ "Подписан" И НЕ "Отклонен" И НЕ "Отменен"
   - ДатаПоследнегоОпроса < (ТекущаяДата - 5 минут) ИЛИ не заполнена
3. Для каждого документа:
   а) Вызвать GET /API/REST/Entity/Load?type={TypeUid}&id={UidELMA}
   б) Извлечь статус из WorkflowInstance
   в) Обновить запись в MRS_ДокументыELMA:
      - СтатусELMA = новый статус из ELMA
      - Статус = преобразованный статус (Согласован, Подписан, Отклонен)
      - ДатаПоследнегоОпроса = ТекущаяДата()
   г) ЕСЛИ статус = "Подписан":
      - Проверить: ТекущаяДата >= ДатаНачалаДействияЦен
      - Если Да: ПровестиДокументУстановкаЦен(Документ)
      - Если Нет: Отложить (проверит РЗ ПроверкаДатыПримененияЦен)
   д) ЕСЛИ статус = "Отклонен":
      - Сохранить КомментарийELMA из ApprovementResult
4. Вернуть статистику опроса (обновлено, подписано, отклонено)
```

#### Функция: `СформироватьJSONДляELMA(ДокументСсылка)`
**Назначение:** Формирование JSON WebData для ELMA

**Возвращает:** Структура с данными для JSON

**Алгоритм:**
```javascript
1. Прочитать документ УстановкаЦенНоменклатуры
2. Получить ИНН организации: Документ.Организация.ИНН
3. Получить Email пользователя: ТекущийПользователь().Email
4. Сформировать DecreeSubject:
   - Шаблон: "Об установке цен для {Организация.Наименование} с {ДатаНачалаДействия}"
5. Сформировать DecreeContents:
   - Пункт 1: "Установить цены согласно приложению №1."
   - Пункт 2: "Контроль за внесением изменений возложить на финансового контролера."
   - Пункт 3: "Контроль за исполнением приказа возложить на директора департамента."
6. Вернуть структуру:
   {
     "OrganizationINN": "9103069850",  // ИНН, ELMA найдёт Organization
     "InitiatorEmail": "user@company.ru",  // Email, ELMA найдёт User
     "DecreeType": {"Uid": "70f51fc1-1c5c-4ef2-870a-c1bec8788322"},
     "Date": "2025-11-14T00:00:00",
     "StartDate": "2025-12-01T00:00:00",
     "DecreeNumber": "ПЦ-000123",
     "DecreeSubject": "...",
     "Preamble": "В целях актуализации прейскуранта",
     "SignatoryPosition": "Генеральный директор",
     "DecreeContents": [...]
   }
```

#### Функция: `ПровестиДокументУстановкаЦен(ДокументСсылка)`
**Назначение:** Проведение документа после подписания приказа

**Алгоритм:**
```javascript
1. Проверить условия:
   - Статус в ELMA = "Подписан"
   - ТекущаяДата >= ДатаНачалаДействияЦен
   - Документ не проведен
2. ЕСЛИ условия выполнены:
   а) ДокументОбъект = Документ.ПолучитьОбъект()
   б) ДокументОбъект.Записать(РежимЗаписиДокумента.Проведение)
   в) Записать в историю ELMA
   г) Запустить выгрузку в Simphony:
      - Отобрать виды цен из документа
      - Проверить наличие в питУдаленныеКассы.ВидЦен
      - Поставить позиции в очередь MRS_ОчередьВыгрузкиМенюSimphony
3. Вернуть Истина/Ложь
```

#### Функция: `ОтменитьПриказВELMA(ДокументСсылка)`
**Назначение:** Отмена/аннулирование приказа в ELMA при пометке на удаление

**Алгоритм:**
```javascript
1. Получить запись из MRS_ДокументыELMA
2. Если UidELMA НЕ заполнен → выход (не отправлялся в ELMA)
3. Авторизоваться в ELMA
4. Сформировать запрос отмены:
   - Метод: POST
   - URL: /API/REST/Entity/Update/{TypeUid}/{UidELMA}
   - Тело: { "Status": "Cancelled" } // Уточнить поле в ELMA
5. Обработать ответ:
   - Успех: Статус = "Отменен", СтатусELMA = "Cancelled"
   - Ошибка: Записать ТекстОшибки
6. Вернуть Истина/Ложь
```

---

### 5.2. MRS_ИнтеграцияELMAКлиент (клиентский)

#### Процедура: `КомандаОтправитьВELMA(Форма)`
**Назначение:** Обработчик команды "Отправить в ELMA" на форме документа

**Алгоритм:**
```javascript
1. Проверить права (только администратор)
2. Если прав нет → Сообщить("Недостаточно прав") → Выход
3. Показать вопрос: "Отправить документ в ELMA для согласования?"
4. Если Да:
   а) Вызвать серверную функцию ПоставитьДокументВОчередьELMA()
   б) Показать результат: "Документ поставлен в очередь на отправку в ELMA"
   в) Обновить форму (отобразить статус)
```

#### Процедура: `ПоказатьИсториюИнтеграцииELMA(ДокументСсылка)`
**Назначение:** Открытие формы истории интеграции для документа

---

### 5.3. MRS_ИнтеграцияELMAКлиентСервер (общий)

#### Функция: `ПолучитьТекстСтатусаELMA(СтатусELMA)`
**Назначение:** Перевод статуса ELMA в текст для пользователя

**Возвращает:** Строка с описанием статуса

---

## 6. Интеграция с выгрузкой в Simphony

### 6.1. Условия выгрузки цен в Simphony

Выгрузка позиций меню в Simphony происходит **только** если:
1. ✅ Документ "Установка цен" проведен
2. ✅ Статус приказа в ELMA = "Подписан"
3. ✅ ТекущаяДата >= ДатаНачалаДействияЦен
4. ✅ Вид цен из документа присутствует в реквизите `ВидЦен` хотя бы одного узла `питУдаленныеКассы`

### 6.2. Алгоритм постановки в очередь Simphony

```javascript
Функция ВыгрузитьЦеныВSimphony(ДокументУстановкаЦен)
    
    // 1. Проверить условия
    Если НЕ ДокументУстановкаЦен.Проведен Тогда
        Возврат Ложь;
    КонецЕсли;
    
    СтатусELMA = ПолучитьСтатусПриказаELMA(ДокументУстановкаЦен);
    Если СтатусELMA <> "Подписан" Тогда
        Возврат Ложь;
    КонецЕсли;
    
    Если ТекущаяДата() < ДокументУстановкаЦен.ДатаНачалаДействия Тогда
        Возврат Ложь; // Отложенное применение
    КонецЕсли;
    
    // 2. Получить виды цен из документа
    ВидыЦенДокумента = Новый Массив;
    Для Каждого СтрокаТЧ Из ДокументУстановкаЦен.ЦеныНоменклатуры Цикл
        Если ВидыЦенДокумента.Найти(СтрокаТЧ.ВидЦены) = Неопределено Тогда
            ВидыЦенДокумента.Добавить(СтрокаТЧ.ВидЦены);
        КонецЕсли;
    КонецЦикла;
    
    // 3. Найти кассовые узлы с этими видами цен
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    питУдаленныеКассы.Ссылка КАК КассовыйУзел,
    |    питУдаленныеКассы.ВидЦен КАК ВидЦен,
    |    питУдаленныеКассыВидыМеню.ВидМеню КАК ВидМеню
    |ИЗ
    |    ПланОбмена.питУдаленныеКассы КАК питУдаленныеКассы
    |        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ПланОбмена.питУдаленныеКассы.ВидыМеню КАК питУдаленныеКассыВидыМеню
    |        ПО питУдаленныеКассы.Ссылка = питУдаленныеКассыВидыМеню.Ссылка
    |ГДЕ
    |    питУдаленныеКассы.ВидЦен В (&ВидыЦен)
    |";
    Запрос.УстановитьПараметр("ВидыЦен", ВидыЦенДокумента);
    Выборка = Запрос.Выполнить().Выбрать();
    
    // 4. Для каждого узла поставить позиции меню в очередь Simphony
    Пока Выборка.Следующий() Цикл
        
        // Получить позиции меню для данного вида меню
        ЗапросПозиций = Новый Запрос;
        ЗапросПозиций.Текст = "
        |ВЫБРАТЬ
        |    питМеню.Ссылка КАК ПозицияМеню,
        |    питМеню.Номенклатура КАК Номенклатура
        |ИЗ
        |    Справочник.питМеню КАК питМеню
        |ГДЕ
        |    питМеню.Владелец = &ВидМеню
        |    И НЕ питМеню.ЭтоГруппа
        |";
        ЗапросПозиций.УстановитьПараметр("ВидМеню", Выборка.ВидМеню);
        ВыборкаПозиций = ЗапросПозиций.Выполнить().Выбрать();
        
        Пока ВыборкаПозиций.Следующий() Цикл
            
            // Проверить, есть ли цена в документе для этой номенклатуры
            НайденаСтрока = Ложь;
            Для Каждого СтрокаТЧ Из ДокументУстановкаЦен.ЦеныНоменклатуры Цикл
                Если СтрокаТЧ.Номенклатура = ВыборкаПозиций.Номенклатура
                    И СтрокаТЧ.ВидЦены = Выборка.ВидЦен Тогда
                    НайденаСтрока = Истина;
                    Прервать;
                КонецЕсли;
            КонецЦикла;
            
            Если НайденаСтрока Тогда
                // Поставить в очередь Simphony
                MRS_ИнтеграцияSimphonyСервер.ПоставитьВОчередьВыгрузки(
                    ВыборкаПозиций.ПозицияМеню,
                    Выборка.КассовыйУзел
                );
            КонецЕсли;
            
        КонецЦикла;
        
    КонецЦикла;
    
    Возврат Истина;
    
КонецФункции
```

---

## 7. Формы для пользователей

### 7.1. Форма документа "Установка цен номенклатуры"

**Добавить элементы:**

| Элемент | Тип | Расположение |
|---------|-----|--------------|
| ГруппаСтатусELMA | Группа | Шапка документа (правая часть) |
| НадписьСтатусELMA | Поле надписи | Внутри ГруппаСтатусELMA |
| КнопкаОтправитьВELMA | Кнопка команды | Командная панель |
| КнопкаИсторияELMA | Кнопка команды | Командная панель |

**Реквизиты формы:**
- `СтатусELMA` (Строка) — текущий статус приказа в ELMA
- `ДатаОтправкиВELMA` (Дата) — когда отправлен
- `IdELMA` (Число) — Id сущности в ELMA (для ссылки)

**Обработчики:**
```javascript
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    // Получить статус из регистра
    СтатусELMA = MRS_ИнтеграцияELMAСервер.ПолучитьСтатусДокумента(Объект.Ссылка);
    
    // Настроить видимость кнопок
    Элементы.КнопкаОтправитьВELMA.Видимость = 
        ПравоДоступа("Администрирование", Метаданные);
    
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтправитьВELMA(Команда)
    MRS_ИнтеграцияELMAКлиент.КомандаОтправитьВELMA(ЭтаФорма);
КонецПроцедуры

&НаКлиенте
Процедура КомандаИсторияELMA(Команда)
    MRS_ИнтеграцияELMAКлиент.ПоказатьИсториюИнтеграцииELMA(Объект.Ссылка);
КонецПроцедуры
```

### 7.2. Обработка "Мониторинг интеграции с ELMA"

**Назначение:** Просмотр состояния документов в процессе интеграции

**Элементы формы:**
1. **Список документов**: Таблица из регистра `MRS_ДокументыELMA`
   - Колонки: Документ, Статус, СтатусELMA, ДатаОтправки, КоличествоПопыток, ТекстОшибки, UidELMA
   - Группировка по Статусу
   - Цветовое выделение строк (ВОчереди — желтый, Ошибка — красный, Подписан — зеленый)

**Команды:**
- "Обновить" — обновление данных
- "Повторить отправку" — переотправка выбранных документов (сбросить КоличествоПопыток, Статус = "ВОчереди")
- "Открыть в ELMA" — открыть процесс в браузере по UidELMA

---

## 8. Регламентные задания

### РЗ-1: MRS_ОбработкаОчередиELMA
- **Метод:** `MRS_ИнтеграцияELMAСервер.ОбработатьОчередьELMA()`
- **Расписание:** Каждые 5 минут
- **Использование:** Да (после настройки)

### РЗ-2: MRS_ОпросСтатусовELMA
- **Метод:** `MRS_ИнтеграцияELMAСервер.ОпроситьСтатусыПриказовELMA()`
- **Расписание:** Каждые 5 минут
- **Использование:** Да

### РЗ-3: MRS_ПроверкаДатыПримененияЦен
- **Метод:** `MRS_ИнтеграцияELMAСервер.ПроверитьДатыПримененияЦен()`
- **Назначение:** Проведение документов с подписанными приказами при наступлении даты начала действия
- **Расписание:** Каждый час (в 00 минут)
- **Использование:** Да



---

## 9. Тестовые сценарии

### ТС-1: Создание и отправка нового приказа
1. Создать документ "Установка цен" с датой начала действия через 2 недели
2. Заполнить табличную часть с ценами
3. Нажать "Отправить в ELMA"
4. **Ожидаемый результат:**
   - Документ в очереди со статусом "ВОчереди"
   - Регламентное задание отправляет в ELMA
   - Создается связка в `MRS_СвязкиДокументовELMA`
   - Статус в `MRS_СтатусыПриказовELMA` = "НаСогласовании"

### ТС-2: Согласование и подписание приказа в ELMA
1. В ELMA согласовать приказ
2. Дождаться опроса статусов (РЗ)
3. **Ожидаемый результат:**
   - Статус в 1С обновлен на "Согласован"
4. В ELMA подписать приказ
5. Дождаться опроса статусов
6. **Ожидаемый результат:**
   - Статус в 1С обновлен на "Подписан"
   - Документ **НЕ** проведен (дата начала действия в будущем)

### ТС-3: Автоматическое проведение при наступлении даты
1. Дождаться наступления даты начала действия цен
2. Дождаться срабатывания РЗ `MRS_ПроверкаДатыПримененияЦен`
3. **Ожидаемый результат:**
   - Документ проведен
   - Позиции меню поставлены в очередь Simphony
   - Цены выгружены в Simphony

### ТС-4: Отклонение приказа в ELMA
1. Создать и отправить документ
2. В ELMA отклонить приказ с комментарием
3. Дождаться опроса статусов
4. **Ожидаемый результат:**
   - Статус в 1С = "Отклонен"
   - Комментарий сохранен в `MRS_СтатусыПриказовELMA`
   - Документ не проведен
   - Выгрузка в Simphony не происходит

### ТС-5: Обновление существующего приказа
1. Изменить документ, который уже отправлен в ELMA
2. Нажать "Отправить в ELMA" повторно
3. **Ожидаемый результат:**
   - В очереди операция "Update"
   - Регламентное задание отправляет UPDATE в ELMA
   - Дублирования приказа в ELMA не происходит
   - Используется существующий Uid из связки

### ТС-6: Пометка на удаление документа
1. Пометить документ на удаление (который отправлен в ELMA)
2. **Ожидаемый результат:**
   - Вызывается `ОтменитьПриказВELMA()`
   - В ELMA отправляется запрос на отмену приказа
   - Статус в 1С = "Отменен"

### ТС-7: Повторные попытки при ошибке
1. Настроить неверный URL ELMA (симуляция ошибки)
2. Отправить документ
3. **Ожидаемый результат:**
   - После 3 неудачных попыток статус = "ТребуетВнимания"
   - Ошибки записаны в историю
   - Дальнейшие попытки не делаются

---

## 10. Чек-лист внедрения

### Фаза 1: Подготовка метаданных (0.5 дня)
- [ ] Создать перечисление `MRS_СтатусыСинхронизацииELMA`
- [ ] Создать регистр `MRS_ДокументыELMA` (со всеми полями)
- [ ] Создать константы (URL, токены, Uid типов)

### Фаза 2: Разработка модулей (3 дня)
- [ ] Создать `MRS_ИнтеграцияELMAСервер`
  - [ ] Авторизация в ELMA
  - [ ] Постановка в очередь
  - [ ] Обработка очереди
  - [ ] Формирование JSON WebData
  - [ ] Опрос статусов
  - [ ] Проведение документа
  - [ ] Отмена приказа
- [ ] Создать `MRS_ИнтеграцияELMAКлиент`
  - [ ] Команда "Отправить в ELMA"
  - [ ] Показ истории
- [ ] Создать `MRS_ИнтеграцияELMAКлиентСервер`
  - [ ] Вспомогательные функции

### Фаза 3: Доработка форм (1 день)
- [ ] Доработать форму документа "Установка цен":
  - [ ] Добавить группу со статусом ELMA
  - [ ] Добавить команды "Отправить в ELMA", "История ELMA"
  - [ ] Реализовать обработчики
- [ ] Создать обработку "Мониторинг интеграции с ELMA"

### Фаза 4: Регламентные задания (0.5 дня)
- [ ] Создать РЗ `MRS_ОбработкаОчередиELMA`
- [ ] Создать РЗ `MRS_ОпросСтатусовELMA`
- [ ] Создать РЗ `MRS_ПроверкаДатыПримененияЦен`
- [ ] Создать РЗ `MRS_ОчисткаИсторииELMA`

### Фаза 5: Настройка и тестирование (2 дня)
- [ ] Заполнить константы (URL, токены ELMA)
- [ ] Проверить ИНН у всех организаций в справочнике
- [ ] Проверить Email у всех пользователей
- [ ] Протестировать все сценарии (ТС-1 — ТС-7)
- [ ] Проверить интеграцию с Simphony

### Фаза 6: Документация и обучение (1 день)
- [ ] Создать инструкцию для администраторов
- [ ] Создать инструкцию для пользователей
- [ ] Провести обучение

**Итого:** ~6-7 рабочих дней

---

## 11. Риски и их митigation

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Несоответствие API ELMA документации | Средняя | Высокое | Тестирование на preprod, тесная коммуникация с командой ELMA |
| Проблемы с формированием Word-файлов | Средняя | Среднее | Использовать готовые библиотеки 1С, упростить шаблон |
| Блокировки документов при проведении | Низкая | Среднее | Использовать управляемые блокировки, обработка ошибок |
| Дублирование приказов при сбоях | Низкая | Высокое | Идемпотентность через регистр связок |
| Расхождение статусов 1С ↔ ELMA | Средняя | Среднее | Регулярный опрос статусов (каждые 5 мин), логирование |
| Большой объем документов в очереди | Низкая | Низкое | Ограничение на 10 записей за раз, приоритезация |

---

## 12. Зависимости и требования

### Внешние зависимости
- [ ] API ELMA доступен и работает
- [ ] Получены токены доступа (preprod и prod)
- [ ] Справочник "Типы приказов" в ELMA содержит нужный тип
- [ ] Шаблон Word-документа доступен для скачивания из ELMA

### Внутренние зависимости
- [ ] Справочник `Организации` — у всех заполнен ИНН
- [ ] Справочник `Пользователи` — у всех заполнен Email
- [ ] План обмена `питУдаленныеКассы` настроен с видами цен
- [ ] Документ `УстановкаЦенНоменклатуры` содержит поле `ДатаНачалаДействия`
- [ ] Интеграция с Simphony работает корректно

---

## 13. Контакты и поддержка

**Product Manager:** [Имя]  
**Senior 1C Developer:** [Имя]  
**Tech Lead ELMA:** [Имя]  

**Дата готовности плана:** 14.11.2025  
**Планируемая дата запуска:** [Дата]

---

**© 2025 | Implementation Plan v1.0 | Статус: 📋 Ready for Development**
