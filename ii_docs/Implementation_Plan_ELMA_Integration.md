# План имплементации: Интеграция 1С:ERP ↔ ЕЛМА (Приказы на установку цен)

**Версия:** 1.1 (обновлена структура API)  
**Дата обновления:** 18.11.2025  
**Статус:** 📋 Ready for Development

---

## ⚠️ ВАЖНЫЕ ИЗМЕНЕНИЯ В СТРУКТУРЕ API

### Новая структура запроса к ЕЛМА

**Метод:** `POST /API/REST/Process/StartWithData`

**Структура запроса:**
```json
{
  "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",
  "Context": {
    "Organization": {"Uid": "c20aa541-d17e-499b-a9e8-755b2803a9b3"},
    "Initiator": {"Uid": "70f51fc1-1c5c-4ef2-870a-c1bec8788322"},
    "DecreeType": {"Uid": "571c78cc-baa0-4529-a00f-8e6c65790ae7"},
    "DecreeHeadline": "Тема приказа",
    "Preamble": "Преамбула",
    "DecreeContent": "Содержание приказа (строка)",
    "StartDate": "2025-12-01T00:00:00Z",
    "DecreeResponsible": {"Uid": "ответственный-uid"},
    "NotificationUsr": [{"Uid": "пользователь1-uid"}, {"Uid": "пользователь2-uid"}],
    "FileDecree": "base64-encoded-docx-content"
  }
}
```

### Ключевые отличия от предыдущей версии:

1. **ProcessToken** — добавлен на верхнем уровне (константа процесса ЕЛМА)
2. **Context** — все данные приказа теперь внутри объекта Context
3. **Organization.Uid** — передается Uid (не ИНН), требуется таблица соответствий
4. **Initiator.Uid** — передается Uid пользователя (не Email), требуется таблица соответствий
5. **DecreeHeadline** — новое название поля для темы приказа (было DecreeSubject)
6. **DecreeContent** — строка (было DecreeContents как массив)
7. **DecreeResponsible.Uid** — новое поле: ответственный за исполнение
8. **NotificationUsr** — новое поле: массив пользователей для ознакомления
9. **FileDecree** — новое название для файла (было DecreeVersion)

---

## 1. Обзор интеграции

### Назначение
Автоматическая синхронизация документов **"Установка цен номенклатуры"** из 1С:ERP с системой ЕЛМА для запуска процесса согласования и подписания приказов по организации.

### Ключевые принципы
- **Архитектура аналогична Simphony:** Используем те же подходы (очередь, регистры истории, общие модули)
- **Таблица соответствий:** Маппинг объектов 1С ↔ Uid ЕЛМА (Организации, Пользователи)
- **ProcessToken:** Запуск процесса через StartWithData (не Insert/Update сущностей)
- **Идемпотентность:** Повторная отправка не создает дубли в ЕЛМА (проверка UidЕЛМА)
- **Асинхронность:** Основная работа через очередь с регламентным заданием
- **Дата применения цен:** Документ согласовывается заранее, проводится и выгружается в Simphony только после подписания и достижения даты начала действия

---

## 2. Бизнес-логика (детальная)

### 2.1. Жизненный цикл документа "Установка цен"

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Создание документа в 1С                                      │
│    - Дата документа                                             │
│    - Дата начала действия цен (может быть в будущем)            │
│    - Организация (ИНН → ЕЛМА Organization.Uid)                  │
│    - Табличная часть с ценами                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Команда "Отправить в ЕЛМА" (только администратор)            │
│    - Валидация данных                                           │
│    - Генерация Word-файла из шаблона (DecreeType.Template)     │
│    - Заполнение полей приказа                                   │
│    - Постановка в очередь MRS_ОчередьОтправкиВЕЛМА              │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Регламентное задание "Обработка очереди ЕЛМА"                │
│    - Выборка записей со статусом "ВОчереди" или "Ошибка"        │
│    - Авторизация в ЕЛМА (AuthToken)                             │
│    - POST /API/REST/Entity/Insert или Update                    │
│    - Получение entityId/Uid из ответа                           │
│    - Сохранение связки в регистре MRS_СтатусПроцессаЕЛМА      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Регламентное задание "Опрос статусов ЕЛМА"                   │
│    - Выборка документов со связкой ЕЛМА                         │
│    - GET /API/REST/Entity/Load для каждого документа            │
│    - Анализ статуса процесса (WorkflowInstance)                 │
│    - Обновление статуса в регистре MRS_СтатусыПриказовЕЛМА      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Проверка условий для проведения документа                    │
│    ЕСЛИ:                                                        │
│    ✓ Статус в ЕЛМА = "Подписан"                                │
│    ✓ ТекущаяДата >= ДатаНачалаДействияЦен                      │
│    ТО:                                                          │
│    → Провести документ "Установка цен"                          │
│    → Записать цены в регистр ЦеныНоменклатуры                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. Выгрузка цен в Simphony (существующая логика)                │
│    - Отбор по видам цен из питУдаленныеКассы.ВидЦен             │
│    - Постановка в очередь MRS_ОчередьВыгрузкиМенюSimphony       │
│    - Синхронизация с Simphony через API                         │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2. Статусы документа в процессе

| Статус ЕЛМА | Действие в 1С | Выгрузка в Simphony |
|-------------|---------------|---------------------|
| НеОтправлен | Ожидание команды "Отправить в ЕЛМА" | ❌ Нет |
| НаСогласовании | Ожидание согласования в ЕЛМА | ❌ Нет |
| Согласован | Ожидание подписания в ЕЛМА | ❌ Нет |
| Подписан | Проверка даты → Проведение документа | ✅ Да (после проведения) |
| Отклонен | Только запись статуса, ничего не делать | ❌ Нет |
| Отменен | Снять с проведения (если был проведен) | ⚠️ Откат изменений |

### 2.3. Логика отправки в ЕЛМА (StartWithData)

**Важно:** Используется метод запуска процесса через `/API/REST/Process/StartWithData`, а не создание сущности через Insert/Update.

```javascript
// Всегда запускаем новый процесс в ЕЛМА
Метод = "POST"
URL = "/API/REST/Process/StartWithData"

Тело запроса = {
  "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",  // Константа процесса
  "Context": {
    "Organization": {"Uid": "..."},
    "Initiator": {"Uid": "..."},
    "DecreeType": {"Uid": "..."},
    "DecreeHeadline": "...",
    "Preamble": "...",
    "DecreeContent": "...",
    "StartDate": "2025-12-01T00:00:00Z",
    "DecreeResponsible": {"Uid": "..."},
    "NotificationUsr": [{"Uid": "..."}, ...],
    "FileDecree": "base64-string"
  }
}

После успешного ответа (200 OK):
  - Получить ProcessId/Uid из ответа ЕЛМА
  - Сохранить UidЕЛМА в регистре MRS_ПроцессыЕЛМА
  - Статус = "НаСогласовании"

Примечание: Каждый документ 1С создает новый процесс в ЕЛМА. 
Повторная отправка того же документа создаст новый процесс (если это требуется).
Для избежания дублей - проверять наличие UidЕЛМА в регистре перед отправкой.
```

---

## 3. Архитектура данных

### 3.1. Регистр сведений (единственный!)

#### РегистрСведений.MRS_ПроцессыЕЛМА
**Назначение:** Полное состояние документа в процессе интеграции с ЕЛМА

**Периодичность:** Непериодический  
**Режим записи:** Независимый

| Измерение | Тип | Описание |
|-----------|-----|----------|
| Документ1С | ДокументСсылка.УстановкаЦенНоменклатуры | Ссылка на документ |

| Ресурс | Тип | Описание |
|--------|-----|----------|
| **UidЕЛМА** | Строка (36) | **GUID процесса в ЕЛМА** (главное!) |
| IdЕЛМА | Число (15,0) | Числовой Id сущности в ЕЛМА |
| Статус | ПеречислениеСсылка.MRS_СтатусыСинхронизацииЕЛМА | Статус обработки |
| СтатусЕЛМА | Строка (50) | Текстовый статус из ЕЛМА |
| ДатаОтправки | Дата | Момент отправки в ЕЛМА |
| ДатаПоследнегоОпроса | Дата | Момент последнего опроса ЕЛМА |
| КомментарийЕЛМА | Строка (500) | Комментарий (при отклонении) |
| КоличествоПопыток | Число (3,0) | Счётчик попыток отправки |
| ТекстОшибки | Строка (500) | Текст последней ошибки |
| ДатаПоследнейПопытки | Дата | Момент последней попытки отправки |

**Индексы:**
- По полю `Статус` (для выборки очереди)
- По полю `UidЕЛМА` (для поиска по Uid ЕЛМА)

**Логика работы:**
1. **Постановка в очередь:** Статус = "ВОчереди", КоличествоПопыток = 0
2. **РЗ обработки:** Выбирает записи со статусом "ВОчереди" или "Ошибка" (попыток < 3)
3. **После успешной отправки:** UidЕЛМА заполнен, Статус = "НаСогласовании"
4. **РЗ опроса:** Обновляет СтатусЕЛМА из ЕЛМА по UidЕЛМА
5. **Финальные статусы:** "Подписан", "Отклонен", "Отменен" — просто хранятся
6. **Детали процесса:** Смотрим в ЕЛМА по UidЕЛМА!

### 3.2. Регистр сведений соответствий объектов 1С ↔ ЕЛМА

#### РегистрСведений.MRS_СоответствияОбъектовЕЛМА
**Назначение:** Хранение соответствий объектов 1С и их Uid в ЕЛМА

**Периодичность:** Непериодический  
**Режим записи:** Независимый

| Измерение | Тип | Описание |
|-----------|-----|----------|
| Объект1С | ЛюбаяСсылка | Ссылка на объект 1С (Организация, Пользователь и т.д.) |

| Ресурс | Тип | Описание |
|--------|-----|----------|
| UidЕЛМА | Строка (36) | GUID объекта в ЕЛМА |
| НаименованиеЕЛМА | Строка (200) | Наименование объекта в ЕЛМА (для отображения) |
| ТипОбъектаЕЛМА | Строка (50) | Тип объекта: "Organization", "User", "DecreeType" |

**Индексы:**
- По полю `Объект1С`
- По полю `UidЕЛМА`

**Логика работы:**
- Заполняется вручную администратором или импортируется из ЕЛМА
- Используется при формировании запросов к ЕЛМА для получения Uid объектов
- Необходимо для маппинга: Организации, Пользователи, Ответственные

### 3.3. Новое перечисление

#### Перечисление.MRS_СтатусыСинхронизацииЕЛМА
- НеОтправлен
- ВОчереди
- Отправлен
- Ошибка
- ТребуетВнимания

### 3.4. Константы

| Константа | Тип | Назначение |
|-----------|-----|------------|
| MRS_URLСервисаЕЛМА | Строка (200) | Базовый URL ЕЛМА API |
| MRS_ApplicationTokenЕЛМА | Строка (100) | Токен приложения ЕЛМА |
| MRS_ИмяПользователяЕЛМА | Строка (100) | Логин для авторизации |
| MRS_ПарольЕЛМА | Строка (100) | Пароль (зашифрованный) |
| MRS_ProcessTokenЕЛМА | Строка (36) | GUID процесса ЕЛМА (262d1c28-1711-4e9d-b65c-65d8ed8c0d68) |
| MRS_DecreeTypeUidЕЛМА | Строка (36) | GUID типа приказа (571c78cc-baa0-4529-a00f-8e6c65790ae7) |
| MRS_ТаймаутЗапросаЕЛМА | Число (10,0) | Таймаут в секундах (по умолчанию 30) |

---

## 4. Маппинг полей: Документ 1С → ЕЛМА WebData

### 4.1. Структура запроса

**Верхний уровень:**
```json
{
  "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",
  "Context": { ... }
}
```

### 4.2. Основные поля приказа (внутри Context)

| Поле ЕЛМА | Источник 1С | Тип | Примечание |
|-----------|-------------|-----|------------|
| **ProcessToken** | Константа MRS_ProcessTokenЕЛМА | GUID | Константа процесса ЕЛМА `262d1c28-1711-4e9d-b65c-65d8ed8c0d68` |
| **Organization.Uid** | Таблица соответствий → УстановкаЦен.Организация | GUID | Uid организации из ЕЛМА (через справочник соответствий) |
| **Initiator.Uid** | Таблица соответствий → ТекущийПользователь() | GUID | Uid физического лица (пользователя) из ЕЛМА |
| **DecreeType.Uid** | Константа MRS_DecreeTypeUidЕЛМА | GUID | `571c78cc-baa0-4529-a00f-8e6c65790ae7` |
| **DecreeHeadline** | Шаблон + данные | String | "Об установке цен для {Организация} с {ДатаНачалаДействия}" |
| **Preamble** | Шаблонный текст | String | "В целях актуализации прейскуранта" |
| **DecreeContent** | Шаблонный текст (объединенная строка) | String | "1. Установить цены...\n2. Контроль...\n3. Контроль..." |
| **StartDate** | УстановкаЦен.ДатаНачалаДействия | DateTime | Дата вступления в силу (ISO 8601: YYYY-MM-DDTHH:mm:ssZ) |
| **DecreeResponsible.Uid** | Таблица соответствий → Ответственный | GUID | Uid ответственного за исполнение из ЕЛМА |
| **NotificationUsr[]** | Таблица соответствий → Список пользователей | Array[GUID] | Массив Uid пользователей для ознакомления |
| **FileDecree** | Печатная форма DOCX → Base64 | String | Файл Word, конвертированный в строку base64 |

### 4.3. Пример запроса в ЕЛМА

```json
{
  "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",
  "Context": {
    "Organization": {"Uid": "c20aa541-d17e-499b-a9e8-755b2803a9b3"},
    "Initiator": {"Uid": "70f51fc1-1c5c-4ef2-870a-c1bec8788322"},
    "DecreeType": {"Uid": "571c78cc-baa0-4529-a00f-8e6c65790ae7"},
    "DecreeHeadline": "Об установке цен для ООО 'Компания' с 01.12.2025",
    "Preamble": "В целях актуализации прейскуранта",
    "DecreeContent": "1. Установить цены согласно приложению №1.\n2. Контроль за внесением изменений возложить на финансового контролера.\n3. Контроль за исполнением приказа возложить на директора департамента.",
    "StartDate": "2025-12-01T00:00:00Z",
    "DecreeResponsible": {"Uid": "ответственный-uid"},
    "NotificationUsr": [
      {"Uid": "пользователь1-uid"},
      {"Uid": "пользователь2-uid"}
    ],
    "FileDecree": "UEsDBBQABgAIAAAAIQ..."
  }
}
```

### 4.4. Файловое вложение (FileDecree)

**Алгоритм формирования Word-файла:**

1. **Генерация печатной формы:**
   - Использование встроенной печатной формы документа "Установка цен номенклатуры"
   - Или создание специальной печатной формы "Приказ на установку цен"
   - Формат: DOCX (Microsoft Word)

2. **Заполнение полей документа:**
   - Наименование организации
   - Номер и дата приказа
   - Тема приказа (DecreeHeadline)
   - Преамбула
   - Содержание приказа (пункты)
   - Табличная часть с ценами (если требуется)
   - Дата вступления в силу

3. **Конвертация в Base64:**
   - Сохранение DOCX во временную папку на сервере 1С
   - Чтение файла как двоичных данных (ПолучитьДвоичныеДанныеИзФайла)
   - Кодирование в Base64 (Base64Строка = Base64Значение(ДвоичныеДанные))
   - Удаление временного файла

4. **Передача в ЕЛМА:**
   - Строка Base64 помещается в поле `FileDecree` внутри `Context`

### 4.5. Таблица соответствий объектов 1С ↔ ЕЛМА

Для корректной работы интеграции необходим справочник соответствий:

**РегистрСведений.MRS_СоответствияОбъектовЕЛМА**

| Измерение | Тип | Описание |
|-----------|-----|----------|
| Объект1С | Ссылка (Любая) | Ссылка на объект 1С (Организация, Пользователь и т.д.) |

| Ресурс | Тип | Описание |
|--------|-----|----------|
| UidЕЛМА | Строка (36) | GUID объекта в ЕЛМА |
| НаименованиеЕЛМА | Строка (200) | Наименование для отображения |
| ТипОбъектаЕЛМА | Строка (50) | Тип объекта: "Organization", "User", "DecreeType" |

**Примеры записей:**
- Объект1С: Справочники.Организации."ООО Компания" → UidЕЛМА: "c20aa541-d17e-499b-a9e8-755b2803a9b3"
- Объект1С: Справочники.Пользователи."Иванов И.И." → UidЕЛМА: "70f51fc1-1c5c-4ef2-870a-c1bec8788322"

---

## 5. Логика общих модулей

### 5.1. MRS_ИнтеграцияЕЛМАСервер (серверный, вызов сервера)

#### Функция: `АвторизоватьсяВЕЛМА()`
**Назначение:** Получение токена авторизации (AuthToken) от ЕЛМА.

**Возвращает:** Структура `{ "Успех": Булево, "Токен": Строка, "ТекстОшибки": Строка }`

**Алгоритм:**
```javascript
1. Получить данные из констант:
   - URL = Константы.MRS_URLСервисаЕЛМА.Получить()
   - ApplicationToken = Константы.MRS_ApplicationTokenЕЛМА.Получить()
   - ИмяПользователя = Константы.MRS_ИмяПользователяЕЛМА.Получить()
   - Пароль = Константы.MRS_ПарольЕЛМА.Получить()
2. Сформировать URL запроса:
   - ПолныйURL = URL + "/API/REST/Authorization/LoginWith?username=" + ИмяПользователя
3. Сформировать POST-запрос:
   - Метод: "POST"
   - Заголовки:
     - "ApplicationToken": ApplicationToken
     - "Content-Type": "text/plain; charset=utf-8"
   - Тело: Пароль
4. Выполнить HTTP-запрос.
5. Обработать ответ:
   - Успех (код 200):
     - Прочитать AuthToken из тела ответа (JSON, поле `AuthToken`).
     - Вернуть `{ Успех: Истина, Токен: AuthToken }`
   - Ошибка (код не 200):
     - Прочитать текст ошибки из тела ответа.
     - Вернуть `{ Успех: Ложь, ТекстОшибки: "..." }`
```

#### Функция: `ПолучитьUidОбъектаЕЛМА(Объект1С, ТипОбъекта)`
**Назначение:** Получение Uid объекта ЕЛМА из таблицы соответствий

**Параметры:**
- Объект1С - ссылка на объект 1С (Организация, Пользователь и т.д.)
- ТипОбъекта - строка ("Organization", "User" и т.д.)

**Возвращает:** Строка (GUID) или Неопределено

**Алгоритм:**
```javascript
1. Запрос к регистру MRS_СоответствияОбъектовЕЛМА:
   - ВЫБРАТЬ UidЕЛМА ГДЕ Объект1С = &Объект1С И ТипОбъектаЕЛМА = &ТипОбъекта
2. ЕСЛИ найдено → Вернуть UidЕЛМА
3. ИНАЧЕ → Вернуть Неопределено (+ записать предупреждение в лог)
```

#### Функция: `ПолучитьМассивUidПользователейЕЛМА(МассивПользователей1С)`
**Назначение:** Получение массива Uid пользователей ЕЛМА

**Параметры:**
- МассивПользователей1С - массив ссылок на пользователей 1С

**Возвращает:** Массив структур [{"Uid": "GUID"}, ...]

**Алгоритм:**
```javascript
1. Создать МассивРезультат = Новый Массив
2. Для каждого Пользователя Из МассивПользователей1С:
   а) Uid = ПолучитьUidОбъектаЕЛМА(Пользователя, "User")
   б) ЕСЛИ Uid <> Неопределено:
      - МассивРезультат.Добавить(Новая Структура("Uid", Uid))
3. Вернуть МассивРезультат
```

#### Функция: `ПоставитьДокументВОчередьЕЛМА(ДокументСсылка)`
**Назначение:** Постановка документа в очередь на отправку

**Алгоритм:**
```javascript
1. Проверить права доступа (только администратор)
2. Валидировать документ:
   - Заполнена организация
   - Заполнена дата начала действия
   - Есть строки в табличной части
   - Есть соответствие организации в таблице соответствий ЕЛМА
   - Есть соответствие текущего пользователя в таблице соответствий ЕЛМА
3. Создать/обновить запись в MRS_ПроцессыЕЛМА:
   - Документ1С = ДокументСсылка
   - Статус = "ВОчереди"
   - ДатаОтправки = Неопределено (пока не отправлен)
   - КоличествоПопыток = 0
   - ТекстОшибки = ""
4. Вернуть Истина
```

#### Функция: `ОбработатьОчередьЕЛМА()`
**Назначение:** Регламентное задание - обработка очереди

**Алгоритм:**
```javascript
1. Получить AuthToken через `АвторизоватьсяВЕЛМА()`. При ошибке - выход с логированием.
2. Выбрать из MRS_ПроцессыЕЛМА записи:
   - Статус = "ВОчереди" ИЛИ (Статус = "Ошибка" И КоличествоПопыток < 3)
   - Упорядочить по ДатаПоследнейПопытки (старые первыми)
   - Ограничение: 10 записей
3. Для каждой записи:
   а) Получить данные документа
   б) Сформировать JSON запроса через СформироватьJSONДляЕЛМА(ДокументСсылка)
      - Структура включает ProcessToken и Context с полями:
        * Organization.Uid (из таблицы соответствий)
        * Initiator.Uid (из таблицы соответствий)
        * DecreeType.Uid (из константы)
        * DecreeHeadline, Preamble, DecreeContent
        * StartDate, DecreeResponsible.Uid, NotificationUsr[]
        * FileDecree (base64)
   в) Определить URL для отправки:
      - URL = Константы.MRS_URLСервисаЕЛМА.Получить() + "/API/REST/Process/StartWithData"
   г) Вызвать POST с JSON в теле запроса
      - Заголовки:
        * "AuthToken": AuthToken
        * "Content-Type": "application/json"
   д) Обработать ответ:
      - Успех (200): 
        * Получить ProcessId/Uid из ответа ЕЛМА
        * Сохранить UidЕЛМА в регистре MRS_ПроцессыЕЛМА
        * Статус = "НаСогласовании"
        * ДатаОтправки = ТекущаяДата()
        * ТекстОшибки = ""
      - Ошибка (4xx, 5xx):
        * Увеличить КоличествоПопыток
        * Записать ТекстОшибки
        * ДатаПоследнейПопытки = ТекущаяДата()
        * Если попыток >= 3: Статус = "ТребуетВнимания"
4. Вернуть статистику обработки (отправлено, ошибок)
```

#### Функция: `ОпроситьСтатусыПриказовЕЛМА()`
**Назначение:** Регламентное задание - опрос статусов из ЕЛМА

**Алгоритм:**
```javascript
1. Получить AuthToken через `АвторизоватьсяВЕЛМА()`. При ошибке - выход с логированием.
2. Выбрать из MRS_ПроцессыЕЛМА записи, где:
   - UidЕЛМА заполнен (отправлен в ЕЛМА)
   - Статус НЕ "Подписан" И НЕ "Отклонен" И НЕ "Отменен"
   - ДатаПоследнегоОпроса < (ТекущаяДата - 5 минут) ИЛИ не заполнена
3. Для каждого документа:
   а) Вызвать GET /API/REST/Entity/Load?type={TypeUid}&id={UidЕЛМА}
   б) Извлечь статус из WorkflowInstance
   в) Обновить запись в MRS_ПроцессыЕЛМА:
      - СтатусЕЛМА = новый статус из ЕЛМА
      - Статус = преобразованный статус (Согласован, Подписан, Отклонен)
      - ДатаПоследнегоОпроса = ТекущаяДата()
   г) ЕСЛИ статус = "Подписан":
      - Проверить: ТекущаяДата >= ДатаНачалаДействияЦен
      - Если Да: ПровестиДокументУстановкаЦен(Документ)
      - Если Нет: Отложить (проверит РЗ ПроверкаДатыПримененияЦен)
   д) ЕСЛИ статус = "Отклонен":
      - Сохранить КомментарийЕЛМА из ApprovementResult
4. Вернуть статистику опроса (обновлено, подписано, отклонено)
```

#### Функция: `СформироватьJSONДляЕЛМА(ДокументСсылка)`
**Назначение:** Формирование JSON WebData для ЕЛМА

**Возвращает:** Структура с данными для JSON

**Алгоритм:**
```javascript
1. Прочитать документ УстановкаЦенНоменклатуры
2. Получить Uid организации из таблицы соответствий (по документу.Организация)
3. Получить Uid инициатора (текущего пользователя) из таблицы соответствий
4. Получить Uid ответственного за исполнение из таблицы соответствий
5. Получить массив Uid пользователей для ознакомления (NotificationUsr)
6. Сформировать DecreeHeadline:
   - Шаблон: "Об установке цен для {Организация.Наименование} с {ДатаНачалаДействия}"
7. Сформировать DecreeContent (объединенная строка):
   - "1. Установить цены согласно приложению №1.\n"
   - "2. Контроль за внесением изменений возложить на финансового контролера.\n"
   - "3. Контроль за исполнением приказа возложить на директора департамента."
8. Сформировать печатную форму DOCX и преобразовать в base64
9. Вернуть структуру:
   {
     "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",  // Константа процесса ЕЛМА
     "Context": {
       "Organization": {"Uid": "c20aa541-d17e-499b-a9e8-755b2803a9b3"},  // Uid организации
       "Initiator": {"Uid": "70f51fc1-1c5c-4ef2-870a-c1bec8788322"},  // Uid инициатора
       "DecreeType": {"Uid": "571c78cc-baa0-4529-a00f-8e6c65790ae7"},  // Uid типа приказа
       "DecreeHeadline": "Об установке цен...",  // Тема приказа
       "Preamble": "В целях актуализации прейскуранта",  // Преамбула
       "DecreeContent": "1. Установить цены...",  // Содержание приказа (строка)
       "StartDate": "2025-12-01T00:00:00Z",  // Дата вступления в силу
       "DecreeResponsible": {"Uid": "ответственный-uid"},  // Ответственный за исполнение
       "NotificationUsr": [  // Массив пользователей для ознакомления
         {"Uid": "пользователь1-uid"},
         {"Uid": "пользователь2-uid"}
       ],
       "FileDecree": "base64-encoded-docx-content"  // Файл Word в base64
     }
   }
```

#### Функция: `ПровестиДокументУстановкаЦен(ДокументСсылка)`
**Назначение:** Проведение документа после подписания приказа

**Алгоритм:**
```javascript
1. Проверить условия:
   - Статус в ЕЛМА = "Подписан"
   - ТекущаяДата >= ДатаНачалаДействияЦен
   - Документ не проведен
2. ЕСЛИ условия выполнены:
   а) ДокументОбъект = Документ.ПолучитьОбъект()
   б) ДокументОбъект.Записать(РежимЗаписиДокумента.Проведение)
   в) Записать в историю ЕЛМА
   г) Запустить выгрузку в Simphony:
      - Отобрать виды цен из документа
      - Проверить наличие в питУдаленныеКассы.ВидЦен
      - Поставить позиции в очередь MRS_ОчередьВыгрузкиМенюSimphony
3. Вернуть Истина/Ложь
```

#### Функция: `ОтменитьПриказВЕЛМА(ДокументСсылка)`
**Назначение:** Отмена/аннулирование приказа в ЕЛМА при пометке на удаление

**Алгоритм:**
```javascript
1. Получить запись из MRS_ПроцессыЕЛМА
2. Если UidЕЛМА НЕ заполнен → выход (не отправлялся в ЕЛМА)
3. Получить AuthToken через `АвторизоватьсяВЕЛМА()`. При ошибке - выход с логированием.
4. Сформировать запрос отмены:
   - Метод: POST
   - URL: /API/REST/Entity/Update/{TypeUid}/{UidЕЛМА}
   - Тело: { "Status": "Cancelled" } // Уточнить поле в ЕЛМА
5. Обработать ответ:
   - Успех: Статус = "Отменен", СтатусЕЛМА = "Cancelled"
   - Ошибка: Записать ТекстОшибки
6. Вернуть Истина/Ложь
```

---

### 5.2. MRS_ИнтеграцияЕЛМАКлиент (клиентский)

#### Процедура: `КомандаОтправитьВЕЛМА(Форма)`
**Назначение:** Обработчик команды "Отправить в ЕЛМА" на форме документа

**Алгоритм:**
```javascript
1. Проверить права (только администратор)
2. Если прав нет → Сообщить("Недостаточно прав") → Выход
3. Показать вопрос: "Отправить документ в ЕЛМА для согласования?"
4. Если Да:
   а) Вызвать серверную функцию ПоставитьДокументВОчередьЕЛМА()
   б) Показать результат: "Документ поставлен в очередь на отправку в ЕЛМА"
   в) Обновить форму (отобразить статус)
```

#### Процедура: `ПоказатьИсториюИнтеграцииЕЛМА(ДокументСсылка)`
**Назначение:** Открытие формы истории интеграции для документа

---

### 5.3. MRS_ИнтеграцияЕЛМАКлиентСервер (общий)

#### Функция: `ПолучитьТекстСтатусаЕЛМА(СтатусЕЛМА)`
**Назначение:** Перевод статуса ЕЛМА в текст для пользователя

**Возвращает:** Строка с описанием статуса

---

## 6. Интеграция с выгрузкой в Simphony

### 6.1. Условия выгрузки цен в Simphony

Выгрузка позиций меню в Simphony происходит **только** если:
1. ✅ Документ "Установка цен" проведен
2. ✅ Статус приказа в ЕЛМА = "Подписан"
3. ✅ ТекущаяДата >= ДатаНачалаДействияЦен
4. ✅ Вид цен из документа присутствует в реквизите `ВидЦен` хотя бы одного узла `питУдаленныеКассы`

### 6.2. Алгоритм постановки в очередь Simphony

```javascript
Функция ВыгрузитьЦеныВSimphony(ДокументУстановкаЦен)
    
    // 1. Проверить условия
    Если НЕ ДокументУстановкаЦен.Проведен Тогда
        Возврат Ложь;
    КонецЕсли;
    
    СтатусЕЛМА = ПолучитьСтатусПриказаЕЛМА(ДокументУстановкаЦен);
    Если СтатусЕЛМА <> "Подписан" Тогда
        Возврат Ложь;
    КонецЕсли;
    
    Если ТекущаяДата() < ДокументУстановкаЦен.ДатаНачалаДействия Тогда
        Возврат Ложь; // Отложенное применение
    КонецЕсли;
    
    // 2. Получить виды цен из документа
    ВидыЦенДокумента = Новый Массив;
    Для Каждого СтрокаТЧ Из ДокументУстановкаЦен.ЦеныНоменклатуры Цикл
        Если ВидыЦенДокумента.Найти(СтрокаТЧ.ВидЦены) = Неопределено Тогда
            ВидыЦенДокумента.Добавить(СтрокаТЧ.ВидЦены);
        КонецЕсли;
    КонецЦикла;
    
    // 3. Найти кассовые узлы с этими видами цен
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    питУдаленныеКассы.Ссылка КАК КассовыйУзел,
    |    питУдаленныеКассы.ВидЦен КАК ВидЦен,
    |    питУдаленныеКассыВидыМеню.ВидМеню КАК ВидМеню
    |ИЗ
    |    ПланОбмена.питУдаленныеКассы КАК питУдаленныеКассы
    |        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ПланОбмена.питУдаленныеКассы.ВидыМеню КАК питУдаленныеКассыВидыМеню
    |        ПО питУдаленныеКассы.Ссылка = питУдаленныеКассыВидыМеню.Ссылка
    |ГДЕ
    |    питУдаленныеКассы.ВидЦен В (&ВидыЦен)
    |";
    Запрос.УстановитьПараметр("ВидыЦен", ВидыЦенДокумента);
    Выборка = Запрос.Выполнить().Выбрать();
    
    // 4. Для каждого узла поставить позиции меню в очередь Simphony
    Пока Выборка.Следующий() Цикл
        
        // Получить позиции меню для данного вида меню
        ЗапросПозиций = Новый Запрос;
        ЗапросПозиций.Текст = "
        |ВЫБРАТЬ
        |    питМеню.Ссылка КАК ПозицияМеню,
        |    питМеню.Номенклатура КАК Номенклатура
        |ИЗ
        |    Справочник.питМеню КАК питМеню
        |ГДЕ
        |    питМеню.Владелец = &ВидМеню
        |    И НЕ питМеню.ЭтоГруппа
        |";
        ЗапросПозиций.УстановитьПараметр("ВидМеню", Выборка.ВидМеню);
        ВыборкаПозиций = ЗапросПозиций.Выполнить().Выбрать();
        
        Пока ВыборкаПозиций.Следующий() Цикл
            
            // Проверить, есть ли цена в документе для этой номенклатуры
            НайденаСтрока = Ложь;
            Для Каждого СтрокаТЧ Из ДокументУстановкаЦен.ЦеныНоменклатуры Цикл
                Если СтрокаТЧ.Номенклатура = ВыборкаПозиций.Номенклатура
                    И СтрокаТЧ.ВидЦены = Выборка.ВидЦен Тогда
                    НайденаСтрока = Истина;
                    Прервать;
                КонецЕсли;
            КонецЦикла;
            
            Если НайденаСтрока Тогда
                // Поставить в очередь Simphony
                MRS_ИнтеграцияSimphonyСервер.ПоставитьВОчередьВыгрузки(
                    ВыборкаПозиций.ПозицияМеню,
                    Выборка.КассовыйУзел
                );
            КонецЕсли;
            
        КонецЦикла;
        
    КонецЦикла;
    
    Возврат Истина;
    
КонецФункции
```

---

## 7. Формы для пользователей

### 7.1. Форма документа "Установка цен номенклатуры"

**Добавить элементы:**

| Элемент | Тип | Расположение |
|---------|-----|--------------|
| ГруппаСтатусЕЛМА | Группа | Шапка документа (правая часть) |
| НадписьСтатусЕЛМА | Поле надписи | Внутри ГруппаСтатусЕЛМА |
| КнопкаОтправитьВЕЛМА | Кнопка команды | Командная панель |
| КнопкаИсторияЕЛМА | Кнопка команды | Командная панель |

**Реквизиты формы:**
- `СтатусЕЛМА` (Строка) — текущий статус приказа в ЕЛМА
- `ДатаОтправкиВЕЛМА` (Дата) — когда отправлен
- `IdЕЛМА` (Число) — Id сущности в ЕЛМА (для ссылки)

**Обработчики:**
```javascript
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    // Получить статус из регистра
    СтатусЕЛМА = MRS_ИнтеграцияЕЛМАСервер.ПолучитьСтатусДокумента(Объект.Ссылка);
    
    // Настроить видимость кнопок
    Элементы.КнопкаОтправитьВЕЛМА.Видимость = 
        ПравоДоступа("Администрирование", Метаданные);
    
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтправитьВЕЛМА(Команда)
    MRS_ИнтеграцияЕЛМАКлиент.КомандаОтправитьВЕЛМА(ЭтаФорма);
КонецПроцедуры

&НаКлиенте
Процедура КомандаИсторияЕЛМА(Команда)
    MRS_ИнтеграцияЕЛМАКлиент.ПоказатьИсториюИнтеграцииЕЛМА(Объект.Ссылка);
КонецПроцедуры
```

### 7.2. Обработка "Мониторинг интеграции с ЕЛМА"

**Назначение:** Просмотр состояния документов в процессе интеграции

**Элементы формы:**
1. **Список документов**: Таблица из регистра `MRS_ПроцессыЕЛМА`
   - Колонки: Документ, Статус, СтатусЕЛМА, ДатаОтправки, КоличествоПопыток, ТекстОшибки, UidЕЛМА
   - Группировка по Статусу
   - Цветовое выделение строк (ВОчереди — желтый, Ошибка — красный, Подписан — зеленый)

**Команды:**
- "Обновить" — обновление данных
- "Повторить отправку" — переотправка выбранных документов (сбросить КоличествоПопыток, Статус = "ВОчереди")
- "Открыть в ЕЛМА" — открыть процесс в браузере по UidЕЛМА

---

## 8. Регламентные задания

### РЗ-1: MRS_ОбработкаОчередиЕЛМА
- **Метод:** `MRS_ИнтеграцияЕЛМАСервер.ОбработатьОчередьЕЛМА()`
- **Расписание:** Каждые 5 минут
- **Использование:** Да (после настройки)

### РЗ-2: MRS_ОпросСтатусовЕЛМА
- **Метод:** `MRS_ИнтеграцияЕЛМАСервер.ОпроситьСтатусыПриказовЕЛМА()`
- **Расписание:** Каждые 5 минут
- **Использование:** Да

### РЗ-3: MRS_ПроверкаДатыПримененияЦен
- **Метод:** `MRS_ИнтеграцияЕЛМАСервер.ПроверитьДатыПримененияЦен()`
- **Назначение:** Проведение документов с подписанными приказами при наступлении даты начала действия
- **Расписание:** Каждый час (в 00 минут)
- **Использование:** Да



---

## 9. Тестовые сценарии

### ТС-1: Создание и отправка нового приказа
1. Создать документ "Установка цен" с датой начала действия через 2 недели
2. Заполнить табличную часть с ценами
3. Нажать "Отправить в ЕЛМА"
4. **Ожидаемый результат:**
   - Документ в очереди со статусом "ВОчереди"
   - Регламентное задание отправляет в ЕЛМА
   - Создается связка в `MRS_СтатусПроцессаЕЛМА`
   - Статус в `MRS_СтатусыПриказовЕЛМА` = "НаСогласовании"

### ТС-2: Согласование и подписание приказа в ЕЛМА
1. В ЕЛМА согласовать приказ
2. Дождаться опроса статусов (РЗ)
3. **Ожидаемый результат:**
   - Статус в 1С обновлен на "Согласован"
4. В ЕЛМА подписать приказ
5. Дождаться опроса статусов
6. **Ожидаемый результат:**
   - Статус в 1С обновлен на "Подписан"
   - Документ **НЕ** проведен (дата начала действия в будущем)

### ТС-3: Автоматическое проведение при наступлении даты
1. Дождаться наступления даты начала действия цен
2. Дождаться срабатывания РЗ `MRS_ПроверкаДатыПримененияЦен`
3. **Ожидаемый результат:**
   - Документ проведен
   - Позиции меню поставлены в очередь Simphony
   - Цены выгружены в Simphony

### ТС-4: Отклонение приказа в ЕЛМА
1. Создать и отправить документ
2. В ЕЛМА отклонить приказ с комментарием
3. Дождаться опроса статусов
4. **Ожидаемый результат:**
   - Статус в 1С = "Отклонен"
   - Комментарий сохранен в `MRS_СтатусыПриказовЕЛМА`
   - Документ не проведен
   - Выгрузка в Simphony не происходит

### ТС-5: Обновление существующего приказа
1. Изменить документ, который уже отправлен в ЕЛМА
2. Нажать "Отправить в ЕЛМА" повторно
3. **Ожидаемый результат:**
   - В очереди операция "Update"
   - Регламентное задание отправляет UPDATE в ЕЛМА
   - Дублирования приказа в ЕЛМА не происходит
   - Используется существующий Uid из связки

### ТС-6: Пометка на удаление документа
1. Пометить документ на удаление (который отправлен в ЕЛМА)
2. **Ожидаемый результат:**
   - Вызывается `ОтменитьПриказВЕЛМА()`
   - В ЕЛМА отправляется запрос на отмену приказа
   - Статус в 1С = "Отменен"

### ТС-7: Повторные попытки при ошибке
1. Настроить неверный URL ЕЛМА (симуляция ошибки)
2. Отправить документ
3. **Ожидаемый результат:**
   - После 3 неудачных попыток статус = "ТребуетВнимания"
   - Ошибки записаны в историю
   - Дальнейшие попытки не делаются

---

## 10. Чек-лист внедрения

### Фаза 1: Подготовка метаданных (0.5 дня)
- [ ] Создать перечисление `MRS_СтатусыСинхронизацииЕЛМА`
- [ ] Создать регистр `MRS_ПроцессыЕЛМА` (со всеми полями)
- [ ] Создать константы (URL, токены, Uid типов)

### Фаза 2: Разработка модулей (3 дня)
- [ ] Создать `MRS_ИнтеграцияЕЛМАСервер`
  - [ ] Авторизация в ЕЛМА
  - [ ] Постановка в очередь
  - [ ] Обработка очереди
  - [ ] Формирование JSON WebData
  - [ ] Опрос статусов
  - [ ] Проведение документа
  - [ ] Отмена приказа
- [ ] Создать `MRS_ИнтеграцияЕЛМАКлиент`
  - [ ] Команда "Отправить в ЕЛМА"
  - [ ] Показ истории
- [ ] Создать `MRS_ИнтеграцияЕЛМАКлиентСервер`
  - [ ] Вспомогательные функции

### Фаза 3: Доработка форм (1 день)
- [ ] Доработать форму документа "Установка цен":
  - [ ] Добавить группу со статусом ЕЛМА
  - [ ] Добавить команды "Отправить в ЕЛМА", "История ЕЛМА"
  - [ ] Реализовать обработчики
- [ ] Создать обработку "Мониторинг интеграции с ЕЛМА"

### Фаза 4: Регламентные задания (0.5 дня)
- [ ] Создать РЗ `MRS_ОбработкаОчередиЕЛМА`
- [ ] Создать РЗ `MRS_ОпросСтатусовЕЛМА`
- [ ] Создать РЗ `MRS_ПроверкаДатыПримененияЦен`
- [ ] Создать РЗ `MRS_ОчисткаИсторииЕЛМА`

### Фаза 5: Настройка и тестирование (2 дня)
- [ ] Заполнить константы (URL, токены ЕЛМА)
- [ ] Проверить ИНН у всех организаций в справочнике
- [ ] Проверить Email у всех пользователей
- [ ] Протестировать все сценарии (ТС-1 — ТС-7)
- [ ] Проверить интеграцию с Simphony

### Фаза 6: Документация и обучение (1 день)
- [ ] Создать инструкцию для администраторов
- [ ] Создать инструкцию для пользователей
- [ ] Провести обучение

**Итого:** ~6-7 рабочих дней

---

## 11. Риски и их митigation

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Несоответствие API ЕЛМА документации | Средняя | Высокое | Тестирование на preprod, тесная коммуникация с командой ЕЛМА |
| Проблемы с формированием Word-файлов | Средняя | Среднее | Использовать готовые библиотеки 1С, упростить шаблон |
| Блокировки документов при проведении | Низкая | Среднее | Использовать управляемые блокировки, обработка ошибок |
| Дублирование приказов при сбоях | Низкая | Высокое | Идемпотентность через регистр связок |
| Расхождение статусов 1С ↔ ЕЛМА | Средняя | Среднее | Регулярный опрос статусов (каждые 5 мин), логирование |
| Большой объем документов в очереди | Низкая | Низкое | Ограничение на 10 записей за раз, приоритезация |

---

## 12. Зависимости и требования

### Внешние зависимости
- [ ] API ЕЛМА доступен и работает
- [ ] Получены токены доступа (preprod и prod)
- [ ] Справочник "Типы приказов" в ЕЛМА содержит нужный тип
- [ ] Шаблон Word-документа доступен для скачивания из ЕЛМА

### Внутренние зависимости
- [ ] Справочник `Организации` — у всех заполнен ИНН
- [ ] Справочник `Пользователи` — у всех заполнен Email
- [ ] План обмена `питУдаленныеКассы` настроен с видами цен
- [ ] Документ `УстановкаЦенНоменклатуры` содержит поле `ДатаНачалаДействия`
- [ ] Интеграция с Simphony работает корректно

---

## 13. Контакты и поддержка

**Product Manager:** [Имя]  
**Senior 1C Developer:** [Имя]  
**Tech Lead ЕЛМА:** [Имя]  

**Дата готовности плана:** 14.11.2025  
**Планируемая дата запуска:** [Дата]

---

**© 2025 | Implementation Plan v1.0 | Статус: 📋 Ready for Development**
