# TODO: Интеграция 1С ↔ Oracle Simphony POS

**Дата создания:** 10.11.2025  
**Версия PRD:** 1.4  
**Статус проекта:** 🟡 В разработке (~60% готово)

---

## 🔴 КРИТИЧНО (Must Have)

### 1. Форма МФормаСписка справочника питМеню

#### 1.1. Команда "Поставить в очередь выгрузки"
- [ ] **Добавить команду на форму** `MRS_ПоставитьВОчередьВыгрузки`
  - Тип: КнопкаКоманды
  - Расположение: Командная панель списка
  - Название: "Поставить в очередь выгрузки"

- [ ] **Реализовать клиентский обработчик** в модуле формы
  ```bsl
  &НаКлиенте
  Процедура MRS_ПоставитьВОчередьВыгрузки(Команда)
      // 1. Показать диалог выбора области действия (3 варианта)
      // 2. Получить список позиций в зависимости от выбора
      // 3. Вызвать серверную процедуру
      // 4. Показать результат
  ```

- [ ] **Реализовать серверную процедуру** `MRS_ПоставитьВОчередьВыгрузкиНаСервере()`
  ```bsl
  &НаСервере
  Функция MRS_ПоставитьВОчередьВыгрузкиНаСервере(Режим, ВыделенныеСтроки, ТекущиеДанные, ВидМеню)
      // 1. Сформировать список позиций по режиму
      // 2. Для каждой позиции:
      //    - Найти кассовый узел
      //    - Проверить цену в регистре
      //    - Создать запись в MRS_ОчередьВыгрузкиМенюSimphony
      // 3. Вернуть статистику
  ```

- [ ] **Добавить вспомогательные функции:**
  - `ПолучитьВсеПозицииГруппы(Группа)` — получить все позиции в группе
  - `ПолучитьВсеПозицииВидаМеню(ВидМеню)` — получить все позиции вида меню

---

#### 1.2. Команда "Выгрузить меню в Simphony" 
- [ ] **Добавить команду на форму** `MRS_ВыгрузитьМенюВSimphony`
  - Тип: КнопкаКоманды
  - Расположение: Командная панель списка
  - Название: "Выгрузить меню в Simphony"
  - Доступность: только если форма открыта с отбором по виду меню

- [ ] **Реализовать клиентский обработчик**
  ```bsl
  &НаКлиенте
  Процедура MRS_ВыгрузитьМенюВSimphony(Команда)
      // 1. Подтверждение пользователя
      // 2. Вызвать серверную выгрузку
      // 3. Показать результат
      // 4. При ошибках - предложить открыть форму мониторинга
  ```

- [ ] **Реализовать серверную процедуру** `MRS_ВыгрузитьМенюВSimphonyНаСервере()`
  ```bsl
  &НаСервере
  Функция MRS_ВыгрузитьМенюВSimphonyНаСервере(ВидМеню)
      // 1. Получить все позиции вида меню (кроме групп)
      // 2. Для каждой позиции:
      //    - Вызвать MRS_ИнтеграцияSimphonyСервер.ВыгрузитьПозициюВSimphony()
      //    - Записать результат в историю
      // 3. Вернуть статистику (успешно/ошибки)
  ```

- [ ] **Добавить проверку доступности команды** — только при наличии отбора по виду меню

---

### 2. Общий модуль MRS_ИнтеграцияSimphonyСервер

#### 2.1. Функция постановки в очередь
- [ ] **Создать функцию** `ПоставитьВОчередьВыгрузки(Позиция, КассовыйУзел)`
  ```bsl
  // Параметры:
  //   Позиция - СправочникСсылка.питМеню
  //   КассовыйУзел - ПланОбменаСсылка.питУдаленныеКассы
  // Возвращает:
  //   Структура {Успех: Булево, Сообщение: Строка}
  //
  // Алгоритм:
  // 1. Проверить наличие цены в ЦеныНоменклатуры
  // 2. Создать/обновить запись в MRS_ОчередьВыгрузкиМенюSimphony
  // 3. Установить статус "ВОчереди"
  ```

#### 2.2. Доработка существующих функций
- [ ] **Убедиться, что `ВыгрузитьПозициюВSimphony()` получает цену динамически**
  - Не должно быть обращений к несуществующему реквизиту `MRS_Цена`
  - Цена должна браться из функции `ПолучитьЦенуНоменклатуры()`

- [ ] **Проверить функцию `СформироватьЗапросПозицииМеню()`**
  - Убедиться, что Major/Family Group получаются через иерархию `ГруппыАналитическогоУчета`
  - Путь: `Номенклатура.ГруппаАналитическогоУчета.Родитель.MRS_КодMajorGroup`
  - Путь: `Номенклатура.ГруппаАналитическогоУчета.MRS_КодFamilyGroup`

#### 2.3. Синхронизация классификаторов
- [ ] **Доработать функцию** `СинхронизироватьMajorGroups()`
  ```bsl
  // Алгоритм:
  // 1. Получить данные из API GET /referencedata/major-groups
  // 2. Для каждого Major Group из ответа:
  //    - Найти или создать элемент 1-го уровня в ГруппыАналитическогоУчета
  //    - Записать object_number в MRS_КодMajorGroup
  //    - Обновить наименование
  ```

- [ ] **Доработать функцию** `СинхронизироватьFamilyGroups()`
  ```bsl
  // Алгоритм:
  // 1. Получить данные из API GET /referencedata/family-groups
  // 2. Для каждого Family Group из ответа:
  //    - Найти соответствующий Major Group (родитель)
  //    - Найти или создать элемент 2-го уровня в ГруппыАналитическогоУчета
  //    - Записать object_number в MRS_КодFamilyGroup
  //    - Обновить наименование
  //    - Установить родителя = Major Group
  ```

---

### 3. Регламентные задания

#### 3.1. Создать задание "MRS_ВыгрузкаМенюSimphony"
- [ ] **Создать регламентное задание в конфигурации**
  - Метод: `MRS_ИнтеграцияSimphonyСервер.ВыполнитьВыгрузкуИзОчереди()`
  - Расписание: Каждые 15 минут
  - Использование: Включено

- [ ] **Реализовать метод** `ВыполнитьВыгрузкуИзОчереди()`
  ```bsl
  // Алгоритм:
  // 1. Выбрать до 50 записей из MRS_ОчередьВыгрузкиМенюSimphony
  //    - Статус = "ВОчереди" ИЛИ
  //    - (Статус = "Ошибка" И КоличествоПопыток < 3 И СледующаяПопытка <= ТекущаяДата())
  // 2. Группировать по кассовым узлам
  // 3. Для каждой позиции вызвать ВыгрузитьПозициюВSimphony()
  // 4. Обновить статусы в очереди
  // 5. При успехе - удалить из очереди
  // 6. При ошибке - увеличить счетчик попыток
  // 7. После 3 попыток - статус "ТребуетВнимания"
  ```

#### 3.2. Создать задание "MRS_ОчисткаИсторииSimphony"
- [ ] **Создать регламентное задание**
  - Метод: `MRS_ИнтеграцияSimphonyСервер.ОчиститьИсториюСинхронизации()`
  - Расписание: Ежедневно в 03:00
  - Использование: Включено

- [ ] **Проверить реализацию метода** `ОчиститьИсториюСинхронизации()`
  - Уже реализовано в модуле (строка 5-50)
  - Удаляет записи старше 90 дней (в PRD было 30, проверить)

#### 3.3. Создать задание "MRS_СинхронизацияКлассификаторовSimphony"
- [ ] **Создать регламентное задание**
  - Метод: `MRS_ИнтеграцияSimphonyСервер.СинхронизироватьКлассификаторы()`
  - Расписание: Ежедневно в 02:00
  - Использование: Включено

- [ ] **Создать метод-обертку** `СинхронизироватьКлассификаторы()`
  ```bsl
  Процедура СинхронизироватьКлассификаторы() Экспорт
      СинхронизироватьMajorGroups();
      СинхронизироватьFamilyGroups();
  КонецПроцедуры
  ```

---

## 🟡 ВАЖНО (Should Have)

### 4. Доработка форм мониторинга

#### 4.1. Форма MRS_ФормаУправленияОчередью
- [ ] **Проверить наличие функционала:**
  - Отображение списка записей из `MRS_ОчередьВыгрузкиМенюSimphony`
  - Кнопка "Выгрузить" для ручной выгрузки выбранных записей
  - Фильтры по статусам
  - Отображение счетчика попыток и даты следующей попытки

- [ ] **Добавить команду "Выгрузить выбранные"** (если нет)
  ```bsl
  // Обработчик должен:
  // 1. Получить выделенные записи
  // 2. Для каждой записи вызвать ВыгрузитьПозициюВSimphony()
  // 3. Обновить статусы
  // 4. Обновить список
  ```

#### 4.2. Форма MRS_ФормаМониторинга
- [ ] **Проверить наличие функционала:**
  - Отображение записей из `MRS_ИсторияСинхронизацииМенюSimphony`
  - Фильтры по датам, статусам, операциям
  - Отображение деталей ошибок (HTTP статус, сообщение)
  - Возможность повторной выгрузки позиции из истории

---

### 5. Модуль объекта питМеню

- [ ] **Проверить обработчик** `ПриЗаписи()`
  - Должен автоматически ставить позицию в очередь при изменении
  - Код может быть закомментирован — раскомментировать

- [ ] **Проверить обработчик** `ПередУдалением()`
  - Должен создавать запись в очереди с операцией "DELETE"
  - Код может быть закомментирован — раскомментировать

---

### 6. Валидация и тестирование

#### 6.1. Проверка данных
- [ ] **Создать процедуру валидации конфигурации**
  ```bsl
  // Проверить:
  // 1. У всех кассовых узлов заполнен Префикс
  // 2. У всех кассовых узлов заполнен ВидЦен
  // 3. У всех узлов есть привязка к видам меню
  // 4. В ГруппыАналитическогоУчета есть иерархия (2 уровня)
  // 5. У элементов 1-го уровня заполнен MRS_КодMajorGroup
  // 6. У элементов 2-го уровня заполнен MRS_КодFamilyGroup
  ```

#### 6.2. Тестовые сценарии
- [ ] **Протестировать постановку в очередь:**
  - Выбранные элементы
  - Вся группа
  - Весь вид меню

- [ ] **Протестировать немедленную выгрузку:**
  - Выгрузка нового меню (все позиции)
  - Проверка получения цен из регистра
  - Обработка ошибок

- [ ] **Протестировать автоматическую выгрузку:**
  - Запустить регламентное задание вручную
  - Проверить обработку очереди
  - Проверить механизм повторных попыток

- [ ] **Протестировать синхронизацию классификаторов:**
  - Запустить синхронизацию Major Groups
  - Запустить синхронизацию Family Groups
  - Проверить корректность иерархии
  - Проверить заполнение кодов

---

## 🟢 ЖЕЛАТЕЛЬНО (Could Have)

### 7. Улучшения UX

- [ ] **Добавить индикатор прогресса** для команды "Выгрузить меню в Simphony"
  - Показывать сколько позиций обработано из общего количества

- [ ] **Добавить настройку расписания** регламентных заданий через интерфейс
  - Форма настроек интеграции

- [ ] **Добавить отчет "Статистика синхронизации"**
  - Количество выгрузок за период
  - Процент успешных/ошибок
  - Топ позиций с ошибками

### 8. Документация

- [ ] **Создать инструкцию пользователя**
  - Как создать новый вид меню
  - Как выгрузить меню в Simphony
  - Как работать с очередью и мониторингом

- [ ] **Создать инструкцию администратора**
  - Настройка кассовых узлов
  - Настройка классификаторов
  - Диагностика ошибок

---

## 📋 Чек-лист готовности к внедрению

### Метаданные
- [x] Создан реквизит `MRS_КодSimphony` в `питМеню`
- [x] Созданы реквизиты `MRS_КодMajorGroup` и `MRS_КодFamilyGroup` в `ГруппыАналитическогоУчета`
- [x] Создано перечисление `MRS_СтатусыСинхронизацииSimphony`
- [x] Создан регистр `MRS_ОчередьВыгрузкиМенюSimphony`
- [x] Создан регистр `MRS_ИсторияСинхронизацииМенюSimphony`
- [x] Создана константа `MRS_URLСервисаSimphony`
- [x] Созданы общие модули MRS_ИнтеграцияSimphony*
- [x] Созданы формы мониторинга и управления очередью

### Формы
- [ ] Добавлены команды на форму `МФормаСписка`
- [ ] Реализованы обработчики команд

### Серверная логика
- [ ] Реализована постановка в очередь
- [ ] Реализована немедленная выгрузка
- [ ] Доработана синхронизация классификаторов
- [ ] Созданы регламентные задания

### Данные
- [ ] Заполнены `Префикс` и `ВидЦен` у кассовых узлов
- [ ] Создана иерархия классификаторов (2 уровня)
- [ ] Заполнены цены в регистре `ЦеныНоменклатуры`
- [ ] Выполнена первичная синхронизация классификаторов

### Тестирование
- [ ] Протестирована постановка в очередь (все 3 режима)
- [ ] Протестирована немедленная выгрузка
- [ ] Протестирована автоматическая выгрузка
- [ ] Протестирована синхронизация классификаторов
- [ ] Проверены формы мониторинга

---

## ⚙️ Технические детали

### Приоритет разработки:
1. **Этап 1 (1-2 дня):** Команды на форме + постановка в очередь
2. **Этап 2 (1 день):** Немедленная выгрузка + доработка синхронизации классификаторов
3. **Этап 3 (1 день):** Регламентные задания + тестирование
4. **Этап 4 (1 день):** Доработка форм мониторинга + финальное тестирование

### Оценка трудозатрат:
- **Разработка:** 4-5 дней
- **Тестирование:** 1-2 дня
- **Внедрение:** 1 день
- **ИТОГО:** ~7 дней

---

**Последнее обновление:** 10.11.2025  
**Следующий шаг:** Начать с Этапа 1 — реализация команд на форме списка

