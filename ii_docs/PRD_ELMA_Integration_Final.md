# PRD + План имплементации: Интеграция 1С:ERP ↔ ЕЛМА
## Приказ на установку цен

**Версия:** 2.2 (добавлена форма параметров приказа и шаблон Word)  
**Дата обновления:** 18.11.2025  
**Статус:** 📋 Ready for Development

---

## 📊 Статус выполнения

| Этап | Прогресс | Статус |
|------|----------|--------|
| 1. Метаданные | 0/3 | ⬜ Не начато |
| 2. Записи в РС.ДополнительныеКонстанты | 0/7 | ⬜ Не начато |
| 3. Общие модули | 0/5 | ⬜ Не начато |
| 4. Регламентные задания | 0/1 | ⬜ Не начато |
| 5. Доработка форм | 0/1 | ⬜ Не начато |
| 6. Заполнение Uid организаций/физлиц | 0/1 | ⬜ Не начато |
| 7. Тестирование | 0/7 | ⬜ Не начато |
| **ИТОГО** | **0/25** | **0%** |

---

# ЧАСТЬ 1: PRODUCT REQUIREMENTS (PRD)

## 1. Концепция и бизнес-ценность

### 1.1 Назначение
1. Автоматически создавать в ЕЛМА процесс **«Оформление приказа по организации»** при команде "Отправить в ЕЛМА" в документе 1С **«Установка цен номенклатуры»**
2. Передавать реквизиты приказа и файл Word (печатная форма)
3. Синхронизировать статусы согласования/подписания из ЕЛМА обратно в 1С
4. Проводить документ и выгружать цены в Simphony только после подписания приказа и наступления даты применения

### 1.2 Бизнес-ценность
- ✅ Сокращение цикла утверждения новых цен
- ✅ Исключение ручных ошибок и дублей приказов
- ✅ Прозрачность статусов для всех участников процесса
- ✅ Полный аудит: связка 1С ↔ ЕЛМА и журнал интеграции
- ✅ Контроль даты применения цен (согласование заранее, применение по графику)

### 1.3 Границы проекта
- **Источник:** 1С:ERP 2.5, документ "Установка цен номенклатуры"
- **Приемник:** ЕЛМА, процесс "Оформление приказа по организации"
- **Среды:** Preprod (http://10.21.180.147:8000), Prod (https://...)
- **Интеграция:** Через REST API ЕЛМА (метод `/API/REST/Process/StartWithData`)

### 1.4 Роли и участники
| Роль | Обязанности | Права в 1С |
|------|-------------|-----------|
| Пользователь 1С | Создает и заполняет документ "Установка цен" | Чтение, Добавление |
| Администратор 1С | Отправляет документ в ЕЛМА командой "Отправить в ЕЛМА" | Администрирование |
| Регламентное задание | Обрабатывает очередь, опрашивает статусы, проводит документы | Система |
| ЕЛМА | Управляет процессом согласования и подписания приказа | Внешняя система |

### 1.5 Не входит в scope (Out of scope)
❌ Ручное редактирование приказа в ЕЛМА → синхронизация обратно в 1С  
❌ Массовая отправка документов в ЕЛМА одной командой  
❌ Интеграция других типов документов 1С с ЕЛМА  
❌ Импорт справочников организаций/пользователей из ЕЛМА (заполняется вручную)

---

## 2. Функциональные требования

### 2.1 Core Functions (Основные функции)

#### CF-1: Отправка документа в ЕЛМА через форму параметров
**Описание:** При проведении документа "Установка цен" (если ВидЦены привязан к питУдаленныеКассы) автоматически открывается форма для заполнения параметров приказа ЕЛМА

**Триггер:** Проведение документа "Установка цен номенклатуры"

**Предусловия:**
- ✅ Документ заполнен (дата начала действия, табличная часть с ценами и видами цен)
- ✅ ВидЦены привязан к узлу плана обмена питУдаленныеКассы с заполненной Организацией
- ✅ Для Организации заполнена запись в РС.ДополнительныеКонстанты с ИмяКонстанты="УИДЕЛМА"
- ✅ У текущего пользователя заполнено ФизическоеЛицо
- ✅ Для ФизическогоЛица заполнена запись в РС.ДополнительныеКонстанты с ИмяКонстанты="УИДЕЛМА"

**Процесс:**
1. При проведении документа проверить:
   - ЕСЛИ ВидЦены привязан к питУдаленныеКассы → открыть форму параметров приказа
   - ИНАЧЕ → обычное проведение без ЕЛМА

2. **Форма "Параметры приказа ЕЛМА":**
   - Поле: **ОтветственныйЗаИсполнение** (СправочникСсылка.ФизическиеЛица) — обязательное
   - Табличная часть: **СписокКОзнакомлению** (ФизическоеЛицо) — необязательная
   - Кнопки: "ОК", "Отмена"

3. При нажатии "ОК":
   а) Валидация:
      - Проверить заполнение ОтветственногоЗаИсполнение
      - Проверить наличие Uid для всех физлиц в РС.ДополнительныеКонстанты
   
   б) Проверка наличия Uid в РС.ДополнительныеКонстанты:
      - Получить Организацию через ВидЦены → питУдаленныеКассы
      - Проверить наличие Uid организации в РС (блокировать если нет)
      - Получить ФизическоеЛицо текущего пользователя
      - Проверить наличие Uid физлица в РС (блокировать если нет)
   
   в) Сохранить параметры во временном хранилище
   
   г) Генерация печатной формы Word (DOCX) из шаблона
   
   д) Конвертация файла в Base64
   
   е) Формирование JSON с ProcessToken и Context
   
   ж) Постановка в очередь (регистр MRS_ПроцессыЕЛМА, статус "ВОчереди")
   
   з) Завершить проведение документа
   
   и) Сообщение пользователю: "Документ проведен. Приказ поставлен в очередь на отправку в ЕЛМА"

4. При нажатии "Отмена":
   - Отменить проведение документа
   - Сообщение: "Проведение отменено. Для отправки в ЕЛМА необходимо заполнить параметры приказа"

**Постусловия:**
- ✅ Документ проведен
- ✅ Запись в регистре MRS_ПроцессыЕЛМА со статусом "ВОчереди"
- ✅ Регламентное задание отправит в ЕЛМА в течение заданного интервала

**Альтернативный сценарий (ручная отправка администратором):**
- Администратор может использовать команду "Отправить в ЕЛМА" на форме документа
- Форма параметров откроется повторно
- Создается новый процесс в ЕЛМА (если предыдущий был отклонен/согласован)

---

#### CF-2: Обработка очереди отправки (РЗ)
**Описание:** Регламентное задание обрабатывает очередь документов на отправку в ЕЛМА

**Триггер:** Каждые 5 минут

**Процесс:**
1. Авторизация в ЕЛМА (получение AuthToken)
2. Выборка записей из MRS_ПроцессыЕЛМА:
   - Статус = "ВОчереди" ИЛИ (Статус = "Ошибка" И КоличествоПопыток < 3)
   - Ограничение: 10 записей
3. Для каждой записи:
   - Формирование JSON запроса (ProcessToken + Context)
   - POST /API/REST/Process/StartWithData
   - При успехе (200 OK):
     * Сохранение УИДЕЛМА из ответа
     * Статус = "НаСогласовании"
     * ДатаОтправки = текущая дата
   - При ошибке:
     * КоличествоПопыток += 1
     * ТекстОшибки = текст ошибки
     * Если попыток >= 3: Статус = "ТребуетВнимания"

**Постусловия:**
- ✅ Документы отправлены в ЕЛМА
- ✅ Созданы процессы в ЕЛМА с уникальными УИДЕЛМА
- ✅ Статусы обновлены в регистре

---

#### CF-3: Опрос статусов из ЕЛМА (РЗ)
**Описание:** Регламентное задание опрашивает статусы процессов в ЕЛМА и обновляет их в 1С

**Триггер:** Каждые 5 минут

**Процесс:**
1. Авторизация в ЕЛМА
2. Выборка документов:
   - УИДЕЛМА заполнен
   - Статус НЕ в ("Подписан", "Отклонен", "Отменен")
   - ДатаПоследнегоОпроса < (текущая дата - 5 минут)
3. Для каждого документа:
   - GET /API/REST/Entity/Load?type={TypeUid}&id={УИДЕЛМА}
   - Извлечение статуса из WorkflowInstance
   - Обновление полей в MRS_ПроцессыЕЛМА:
     * СтатусЕЛМА = новый статус
     * Статус = преобразованный статус (НаСогласовании/Согласован/Подписан/Отклонен)
     * ДатаПоследнегоОпроса = текущая дата
   - Если статус = "Отклонен": сохранить КомментарийЕЛМА

**Постусловия:**
- ✅ Статусы синхронизированы с ЕЛМА
- ✅ Комментарии при отклонении сохранены

---

#### CF-4: Проведение документа по дате применения (РЗ)
**Описание:** Регламентное задание проверяет условия и проводит документы с подписанными приказами

**Триггер:** Каждые 5 минут (в составе основного РЗ)

**Условия проведения:**
- ✅ Статус в ЕЛМА = "Подписан"
- ✅ ТекущаяДата >= ДатаНачалаДействияЦен
- ✅ Документ не проведен

**Процесс:**
1. Выборка документов, удовлетворяющих условиям
2. Для каждого документа:
   - ДокументОбъект.Записать(РежимЗаписиДокумента.Проведение)
   - Записать в регистр ЦеныНоменклатуры
3. Запуск выгрузки в Simphony:
   - Отбор видов цен из документа
   - Проверка наличия в питУдаленныеКассы.ВидЦен
   - Постановка позиций меню в очередь MRS_ОчередьВыгрузкиМенюSimphony

**Постусловия:**
- ✅ Документ проведен
- ✅ Цены записаны в регистр
- ✅ Позиции меню поставлены в очередь Simphony

---

#### CF-5: Отмена приказа при удалении документа
**Описание:** При пометке документа на удаление отправляется отмена приказа в ЕЛМА

**Триггер:** Событие "ПриУстановкеПометкиУдаления" документа УстановкаЦенНоменклатуры

**Условия:**
- ✅ Документ отправлен в ЕЛМА (УИДЕЛМА заполнен)
- ✅ Статус НЕ "Отменен"

**Процесс:**
1. Получить УИДЕЛМА из регистра
2. Авторизация в ЕЛМА
3. POST /API/REST/Entity/Update/{TypeUid}/{УИДЕЛМА} с телом {"Status": "Cancelled"}
4. Обновить статус в регистре: Статус = "Отменен"
5. Если документ был проведен: снять с проведения

**Постусловия:**
- ✅ Статус в 1С = "Отменен"
- ✅ Приказ отменен в ЕЛМА

---

### 2.2 Flows (Потоки данных)

#### Flow-1: Создание и отправка приказа (Happy Path)

```
1. Пользователь создает документ "Установка цен"
   ├─ Указывает дату начала действия (например, через 2 недели)
   └─ Заполняет табличную часть с ценами

2. Администратор нажимает "Отправить в ЕЛМА"
   ├─ Валидация данных (организация, пользователь, Uid)
   ├─ Генерация Word-файла (печатная форма)
   └─ Постановка в очередь (MRS_ПроцессыЕЛМА, статус "ВОчереди")

3. РЗ обрабатывает очередь (каждые 5 мин)
   ├─ Авторизация в ЕЛМА (AuthToken)
   ├─ Формирование JSON (ProcessToken + Context)
   ├─ POST /API/REST/Process/StartWithData
   └─ Сохранение УИДЕЛМА, статус = "НаСогласовании"

4. Согласование в ЕЛМА (действия пользователей ЕЛМА)
   ├─ Согласователи одобряют приказ
   └─ Руководитель подписывает приказ

5. РЗ опрашивает статусы (каждые 5 мин)
   ├─ GET /API/REST/Entity/Load
   ├─ Статус обновлен: "Согласован" → "Подписан"
   └─ Обновление в MRS_ПроцессыЕЛМА

6. РЗ проверяет дату применения (каждые 5 мин)
   ├─ ЕСЛИ ТекущаяДата >= ДатаНачалаДействия
   ├─ Проведение документа
   └─ Постановка в очередь Simphony

7. Выгрузка в Simphony (существующий механизм)
   └─ Цены применены на кассах
```

---

#### Flow-2: Отклонение приказа

```
1. Документ отправлен в ЕЛМА (статус "НаСогласовании")

2. Согласователь в ЕЛМА отклоняет приказ с комментарием
   └─ Комментарий: "Цены завышены, пересмотреть"

3. РЗ опрашивает статусы
   ├─ Статус изменен на "Отклонен"
   ├─ Сохранен КомментарийЕЛМА
   └─ Документ остается непроведенным

4. Пользователь видит статус "Отклонен" на форме документа
   ├─ Может прочитать комментарий
   └─ Может исправить цены и отправить повторно
```

---

#### Flow-3: Ошибка при отправке (Retry Logic)

```
1. Документ в очереди (статус "ВОчереди")

2. РЗ пытается отправить в ЕЛМА
   └─ Ошибка: нет связи с ЕЛМА / таймаут / 500 Internal Server Error

3. Обработка ошибки:
   ├─ КоличествоПопыток = 1
   ├─ ТекстОшибки = "Connection timeout"
   ├─ Статус = "Ошибка"
   └─ ДатаПоследнейПопытки = текущая дата

4. Повторные попытки (2-я и 3-я)
   └─ Каждые 5 минут

5. После 3-й неудачной попытки:
   ├─ Статус = "ТребуетВнимания"
   └─ Уведомление администратору (опционально)

6. Администратор устраняет проблему
   └─ Вручную повторяет отправку
```

---

## 3. Данные и интеграции

### 3.1 Структура запроса к ЕЛМА (API Contract)

**Endpoint:** `POST /API/REST/Process/StartWithData`

**Headers:**
```json
{
  "ApplicationToken": "{константа MRS_ApplicationTokenЕЛМА}",
  "AuthToken": "{получен через LoginWith}",
  "Content-Type": "application/json"
}
```

**Body:**
```json
{
  "ProcessToken": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",
  "Context": {
    "Organization": {"Uid": "c20aa541-d17e-499b-a9e8-755b2803a9b3"},
    "Initiator": {"Uid": "70f51fc1-1c5c-4ef2-870a-c1bec8788322"},
    "DecreeType": {"Uid": "571c78cc-baa0-4529-a00f-8e6c65790ae7"},
    "DecreeHeadline": "Об установке цен для ООО 'Компания' с 01.12.2025",
    "Preamble": "В целях актуализации прейскуранта",
    "DecreeContent": "1. Установить цены согласно приложению №1.\n2. Контроль за внесением изменений возложить на финансового контролера.\n3. Контроль за исполнением приказа возложить на директора департамента.",
    "StartDate": "2025-12-01T00:00:00Z",
    "DecreeResponsible": {"Uid": "ответственный-uid"},
    "NotificationUsr": [
      {"Uid": "пользователь1-uid"},
      {"Uid": "пользователь2-uid"}
    ],
    "FileDecree": "UEsDBBQABgAIAAAAIQ..."
  }
}
```

**Response (Success 200 OK):**
```json
{
  "ProcessId": 12345,
  "Uid": "262d1c28-1711-4e9d-b65c-65d8ed8c0d68",
  "Status": "Created"
}
```

---

### 3.2 Маппинг полей 1С → ЕЛМА

| Поле ЕЛМА | Источник 1С | Тип | Обязательность | Примечание |
|-----------|-------------|-----|----------------|------------|
| **ProcessToken** | РС.ДополнительныеКонстанты: MRS_ProcessTokenЕЛМА | GUID | ✅ Да | `262d1c28-1711-4e9d-b65c-65d8ed8c0d68` |
| **Organization.Uid** | ВидЦены → питУдаленныеКассы.Организация → РС.ДополнительныеКонстанты | GUID | ✅ Да | Через план обмена и РС |
| **Initiator.Uid** | ТекущийПользователь().ФизЛицо → РС.ДополнительныеКонстанты | GUID | ✅ Да | Из РС по физлицу |
| **DecreeType.Uid** | РС.ДополнительныеКонстанты: MRS_DecreeTypeУИДЕЛМА | GUID | ✅ Да | `571c78cc-baa0-4529-a00f-8e6c65790ae7` |
| **DecreeHeadline** | Шаблон | String | ✅ Да | "Об установке цен для {Орг} с {Дата}" |
| **Preamble** | Константа | String | ✅ Да | "В целях актуализации прейскуранта" |
| **DecreeContent** | Формируется | String | ✅ Да | Пункты приказа через \n |
| **StartDate** | УстановкаЦен.ДатаНачалаДействия | DateTime | ✅ Да | ISO 8601 (UTC) |
| **DecreeResponsible.Uid** | Документ.Ответственный.ФизЛицо → РС.ДополнительныеКонстанты | GUID | ⚠️ Опционально | Ответственный за исполнение |
| **NotificationUsr[]** | ТЧ Ознакомить → ФизЛица → РС.ДополнительныеКонстанты | Array | ⚠️ Опционально | Массив Uid для ознакомления |
| **FileDecree** | Печатная форма DOCX | Base64 String | ✅ Да | Word-файл в base64 |

---

### 3.3 Справочник статусов

| Статус 1С | Статус ЕЛМА | Описание | Действия пользователя |
|-----------|-------------|----------|----------------------|
| **ВОчереди** | — | Документ в очереди на отправку | Ожидание (до 5 мин) |
| **НаСогласовании** | InProgress | Процесс согласования в ЕЛМА | Ожидание, мониторинг |
| **Согласован** | Approved | Все согласователи одобрили | Ожидание подписания |
| **Подписан** | Signed | Приказ подписан руководителем | Ожидание даты применения |
| **Отклонен** | Rejected | Отклонен согласователем | Исправить и отправить повторно |
| **Отменен** | Cancelled | Документ удален в 1С | Архив |
| **Ошибка** | — | Ошибка при отправке (попыток < 3) | Автоматический retry |
| **ТребуетВнимания** | — | Ошибка после 3 попыток | Ручное вмешательство |

---

## 4. Non-Functional Requirements (NFR)

### 4.1 Производительность
- ⏱️ Время отправки документа в ЕЛМА: < 30 секунд
- ⏱️ Частота опроса статусов: каждые 5 минут
- ⏱️ Таймаут HTTP-запроса: 30 секунд
- 📊 Обработка очереди: до 10 документов за раз

### 4.2 Надежность
- 🔄 Retry logic: 3 попытки с интервалом 5 минут
- 💾 Идемпотентность: повторная отправка не создает дубли (проверка УИДЕЛМА)
- 📝 Логирование всех операций в регистр MRS_ПроцессыЕЛМА

### 4.3 Безопасность
- 🔐 Токены и пароли хранятся в РС.ПЛ_ДополнительныеКонстанты с ограниченными правами
- 🔐 Доступ к команде "Отправить в ЕЛМА" только для администраторов
- 🔐 Логи не содержат чувствительных данных (токены, пароли)

### 4.4 Масштабируемость
- 📈 Поддержка до 100 документов в очереди одновременно
- 📈 Ограничение обработки: 10 документов за цикл (5 минут)

---

# ЧАСТЬ 2: ТЕХНИЧЕСКИЙ ПЛАН ИМПЛЕМЕНТАЦИИ

## 5. Объекты метаданных

### 5.1 Перечисление (1 шт)

#### ⬜ Перечисление.MRS_СтатусыСинхронизацииЕЛМА

**Значения:**
- [ ] ВОчереди
- [ ] НаСогласовании
- [ ] Согласован
- [ ] Подписан
- [ ] Отклонен
- [ ] Отменен
- [ ] Ошибка
- [ ] ТребуетВнимания

---

### 5.2 Регистр сведений (1 шт)

#### ⬜ РС.MRS_ПроцессыЕЛМА

**Периодичность:** Непериодический  
**Режим записи:** Независимый

**Измерения:**
- [ ] Документ1С (ДокументСсылка.УстановкаЦенНоменклатуры)

**Ресурсы:**
- [ ] УИДЕЛМА (Строка 36) — **главное поле!**
- [ ] Статус (ПеречислениеСсылка.MRS_СтатусыСинхронизацииЕЛМА)
- [ ] СтатусЕЛМА (Строка 50)
- [ ] ДатаОтправки (Дата)
- [ ] ДатаПоследнегоОпроса (Дата)
- [ ] КомментарийЕЛМА (Строка 500)
- [ ] КоличествоПопыток (Число 3,0)
- [ ] ТекстОшибки (Строка 500)
- [ ] ДатаПоследнейПопытки (Дата)

**Индексы:**
- [ ] По полю `Статус`
- [ ] По полю `УИДЕЛМА`

---

### 5.3 Константы в РС.ПЛ_ДополнительныеКонстанты (7 записей)

- [ ] **MRS_URLСервисаЕЛМА** — `https://elma.company.ru` или `http://10.21.180.147:8000`
- [ ] **MRS_ApplicationTokenЕЛМА** — токен приложения ЕЛМА
- [ ] **MRS_ИмяПользователяЕЛМА** — логин для авторизации
- [ ] **MRS_ПарольЕЛМА** — пароль (зашифрованный)
- [ ] **MRS_ProcessTokenЕЛМА** — `262d1c28-1711-4e9d-b65c-65d8ed8c0d68`
- [ ] **MRS_DecreeTypeУИДЕЛМА** — `571c78cc-baa0-4529-a00f-8e6c65790ae7`
- [ ] **MRS_ТаймаутЗапросаЕЛМА** — `30`

---

### 5.4 Хранение UID объектов в РС.ДополнительныеКонстанты

**Примечание:** UID организаций и физических лиц хранятся в **РС.ПЛ_ДополнительныеКонстанты**, а не в реквизитах справочников.

#### ⬜ Записи в РС.ПЛ_ДополнительныеКонстанты для UID

**Для каждой организации:**
- [ ] **Объект** = Справочники.Организации."Организация1"
- [ ] **ИмяКонстанты** = "MRS_УИДЕЛМА_Организация"
- [ ] **Значение** = "c20aa541-d17e-499b-a9e8-755b2803a9b3" (GUID из ЕЛМА)

**Для каждого физического лица:**
- [ ] **Объект** = Справочники.ФизическиеЛица."ИвановИИ"
- [ ] **ИмяКонстанты** = "MRS_УИДЕЛМА_ФизЛицо"
- [ ] **Значение** = "70f51fc1-1c5c-4ef2-870a-c1bec8788322" (GUID из ЕЛМА)

**Логика получения:**
```bsl
// Получение UID организации через план обмена
УзелПланаОбмена = НайтиУзелПоВидуЦены(ВидЦены);
Организация = УзелПланаОбмена.Организация;
UidОрганизации = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту(
    "MRS_УИДЕЛМА_Организация", 
    Организация
);

// Получение UID физического лица
ФизЛицо = ТекущийПользователь().ФизическоеЛицо;
UidФизЛица в РС.ДополнительныеКонстанты запросом
```

---

### 5.5 Общие модули (5 шт)

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАСервер
**Тип:** Серверный, Вызов сервера

**Экспортные функции/процедуры:**

**Основная функция для РЗ:**
- [ ] `ВыполнитьИнтеграциюЕЛМА()` — главная функция РЗ (очередь + опрос + проведение)

**Работа с очередью:**
- [ ] `ПроверитьНеобходимостьОтправкиВЕЛМА(Документ, Отказ)` — вызывается при проведении документа
- [ ] `ПоставитьДокументВОчередьЕЛМА(ДокументСсылка, ПараметрыПриказа)`
- [ ] `ОбработатьОчередьОтправки()`

**Работа со статусами:**
- [ ] `ОпроситьСтатусыДокументов()`
- [ ] `ПолучитьСтатусДокумента(ДокументСсылка)`
- [ ] `ЗагрузитьСтатусДокумента(Форма)`
- [ ] `ОбновитьСтатусДокумента(ДокументСсылка, НовыйСтатус, Комментарий)`

**Работа с ЕЛМА API:**
- [ ] `АвторизоватьсяВЕЛМА()`
- [ ] `ОтправитьВЕЛМА(ДокументСсылка, AuthToken)`
- [ ] `ПолучитьСтатусИзЕЛМА(УИДЕЛМА, AuthToken)`
- [ ] `ОтменитьПриказВЕЛМА(ДокументСсылка)`

**Формирование данных:**
- [ ] `СформироватьJSONДляЕЛМА(ДокументСсылка, ПараметрыПриказа)`
- [ ] `СформироватьDecreeContent(ДокументСсылка)`
- [ ] `СформироватьWordФайлПриказа(ДокументСсылка, ПараметрыПриказа)` — заполнение шаблона Word
- [ ] `КонвертироватьВBase64(ДвоичныеДанные)` — конвертация в Base64

**Работа с UID объектов (через РС.ДополнительныеКонстанты):**
- [ ] `ПолучитьUidОрганизацииИзПланаОбмена(ВидЦены)` — получить Uid организации через питУдаленныеКассы
- [ ] `ПолучитьUidФизЛицаЕЛМА(ФизЛицоСсылка)` — получить Uid физлица из РС.ДополнительныеКонстанты
- [ ] `ПолучитьUidТекущегоПользователяЕЛМА()` — получить Uid инициатора (текущего пользователя)
- [ ] `ПолучитьМассивUidПользователейЕЛМА(МассивПользователей1С)` — массив Uid для ознакомления

**Проведение и выгрузка:**
- [ ] `ПровестиДокументыПоДатеПрименения()`
- [ ] `ПровестиДокументУстановкаЦен(ДокументСсылка)`
- [ ] `ВыгрузитьЦеныВSimphony(ДокументСсылка)`

**Уведомления:**
- [ ] `ОтправитьEmailУведомлениеОСменеСтатуса(ДокументСсылка, НовыйСтатус)` — отправка email автору документа

**Служебные:**
- [ ] `ПреобразоватьСтатусЕЛМАВСтатус1С(СтатусЕЛМА)`
- [ ] `ПолучитьEmailАвтораДокумента(ДокументСсылка)` — получить email из ФизЛицо.КонтактнаяИнформация

---

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАКлиент
**Тип:** Клиентский

- [ ] `КомандаОтправитьВЕЛМА(Форма)`
- [ ] `ПоказатьИсториюИнтеграцииЕЛМА(ДокументСсылка)`
- [ ] `ОбновитьСтатусНаФорме(Форма)`

---

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАКлиентСервер
**Тип:** Клиент-сервер

- [ ] `ПриСозданииНаСервере(Форма, Отказ, СтандартнаяОбработка)`
- [ ] `ПолучитьТекстСтатусаЕЛМА(СтатусЕЛМА)`
- [ ] `ПолучитьЦветСтатусаЕЛМА(СтатусЕЛМА)`
- [ ] `ВалидироватьДокументПередОтправкой(Документ)`

---

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАСервер
**Тип:** Серверный

- [ ] `СоздатьПроцессВЕЛМА(ДокументСсылка, Режим = "Приказ") Экспорт`
  - Создание процесса в ELMA через REST API
  - Формирование JSON-структуры данных документа
  - Сохранение идентификатора процесса в РС.MRS_ПроцессыELMA
  
- [ ] `ОбновитьСтатусПроцессаЕЛМА(ИдентификаторПроцессаELMA) Экспорт`
  - Получение текущего статуса процесса из ELMA
  - Обновление статуса в РС.MRS_ПроцессыELMA
  - Возврат структуры с текущим статусом
  
- [ ] `ОтменитьПроцессВЕЛМА(ИдентификаторПроцессаELMA) Экспорт`
  - Отмена процесса в ELMA через REST API
  - Обновление статуса в РС.MRS_ПроцессыELMA

- [ ] `ПолучитьДанныеДокументаДляELMA(ДокументСсылка) Экспорт`
  - Формирование структуры данных документа для отправки в ELMA
  - Преобразование табличной части в JSON-массив

---

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАКлиент
**Тип:** Клиентский

- [ ] `ОтправитьВЕЛМА(Форма) Экспорт`
  - Обработчик команды отправки документа в ELMA
  - Вызов серверной функции создания процесса
  - Обновление формы после отправки

- [ ] `ОбновитьСтатусЕЛМА(Форма) Экспорт`
  - Обработчик команды обновления статуса процесса ELMA
  - Вызов серверной функции получения статуса
  - Обновление декорации статуса на форме

- [ ] `ПодключитьОбработчикОжидания(Форма) Экспорт`
  - Подключение обработчика ожидания для автоматического обновления статуса
  - Интервал проверки: 30 секунд

- [ ] `ОбработчикОжиданияОбновлениеСтатуса(Форма) Экспорт`
  - Автоматическое обновление статуса процесса ELMA
  - Отключение обработчика при завершении процесса

---

#### ⬜ ОМ.MRS_ИнтеграцияЕЛМАКлиентСервер
**Тип:** Клиент-серверный

- [ ] `ПолучитьЦветСтатусаELMA(Статус) Экспорт`
  - Возвращает цвет для отображения статуса на форме
  - Новый → Серый
  - Отправлен → Синий
  - В работе → Желтый
  - Утвержден → Зеленый
  - Отклонен → Красный
  - Отменен → Серый

- [ ] `ПолучитьТекстСтатусаELMA(Статус) Экспорт`
  - Возвращает текстовое представление статуса
  - Преобразование перечисления в строку для отображения

- [ ] `СформироватьJSONДокумента(ДанныеДокумента) Экспорт`
  - Преобразование структуры данных документа в JSON
  - Валидация обязательных полей

**Подписки на события:**
- [ ] ПередЗаписью (УстановкаЦенНоменклатуры)
- [ ] ПриУстановкеПометкиУдаления (УстановкаЦенНоменклатуры)

---

### 5.6 Регламентное задание (1 шт)

#### ⬜ РЗ.MRS_ИнтеграцияЕЛМА

- **Метод:** `MRS_ИнтеграцияЕЛМАСервер.ВыполнитьИнтеграциюЕЛМА()`
- **Наименование:** "Интеграция с ЕЛМА"
- **Расписание:** Каждые 5 минут
- **Использование:** Да
- **Попытки при ошибке:** 3
- **Интервал повтора:** 1 минута

**Выполняемые операции:**
1. Обработка очереди отправки (Insert)
2. Опрос статусов из ЕЛМА (Load)
3. Проверка дат применения и проведение документов
4. Выгрузка цен в Simphony

---

### 5.7 Формы (2 шт)

#### ⬜ Форма 1: УстановкаЦенНоменклатуры.ФормаДокумента

**Добавить в модуль документа (подписка ПередЗаписью):**

```bsl
//>> Швецов. ERP-XXXX. 18.11.25. Интеграция с ЕЛМА
Если РежимЗаписи = РежимЗаписиДокумента.Проведение Тогда
    MRS_ИнтеграцияЕЛМАСервер.ПроверитьНеобходимостьОтправкиВЕЛМА(ЭтотОбъект, Отказ);
КонецЕсли;
//<<
```

**Программно добавляемые элементы (через ОМ.МодификацияКонфигурацииПереопределяемый):**
- [ ] Реквизит формы `СтатусЕЛМА` (Строка)
- [ ] Реквизит формы `ДатаОтправкиВЕЛМА` (Дата)
- [ ] Реквизит формы `УИДЕЛМА` (Строка)
- [ ] Команда формы `КомандаОтправитьВЕЛМА` (для администратора)
- [ ] Команда формы `КомандаОбновитьСтатусЕЛМА`
- [ ] Группа `ГруппаСтатусЕЛМА` (в шапке документа)
- [ ] Поле декорации для `СтатусЕЛМА` (с цветовым оформлением)
- [ ] Поле декорации для `ДатаОтправкиВЕЛМА`
- [ ] Кнопки команд в командной панели

---

#### ⬜ Форма 2: MRS_ФормаПараметровПриказаЕЛМА (новая форма)

**Назначение:** Форма для заполнения параметров приказа ЕЛМА при проведении документа "Установка цен"

**Тип:** Управляемая форма (клиент-серверная)

**Реквизиты формы:**
- [ ] **ОтветственныйЗаИсполнение** (СправочникСсылка.ФизическиеЛица) — обязательное поле
- [ ] **СписокКОзнакомлению** (ТабличнаяЧасть):
  - ФизическоеЛицо (СправочникСсылка.ФизическиеЛица)

**Элементы формы:**
- [ ] Поле выбора "Ответственный за исполнение" (с отбором по ФизическимЛицам)
- [ ] Таблица "Список к ознакомлению" (с возможностью добавления/удаления строк)
- [ ] Кнопка "ОК" (основная кнопка формы)
- [ ] Кнопка "Отмена"

**Команды формы:**
- [ ] `ОК()` — валидация данных, сохранение параметров, закрытие формы с результатом
- [ ] `Отмена()` — закрытие формы без сохранения

**Обработчики:**
```bsl
&НаКлиенте
Процедура ОК(Команда)
    
    Если НЕ ЗначениеЗаполнено(ОтветственныйЗаИсполнение) Тогда
        Сообщить("Необходимо заполнить поле 'Ответственный за исполнение'");
        Возврат;
    КонецЕсли;
    
    РезультатВыбора = Новая Структура;
    РезультатВыбора.Вставить("ОтветственныйЗаИсполнение", ОтветственныйЗаИсполнение);
    РезультатВыбора.Вставить("СписокКОзнакомлению", СписокКОзнакомлению.Выгрузить());
    
    Закрыть(РезультатВыбора);
    
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
    Закрыть(Неопределено);
КонецПроцедуры
```

---

## 6. Шаблон Word-документа приказа

### 6.1 Структура шаблона

**Файл:** `Шаблон_приказа_Изменение_в_POS_системах.docx`  
**Расположение:** Хранится в конфигурации (ХранилищеОбщихНастроек) или в файловой системе сервера

**Переменные шаблона (синтаксис ЕЛМА):**

| Переменная | Источник 1С | Пример значения |
|------------|-------------|-----------------|
| `{$Legal.Name}` | Организация.Наименование | "ООО 'МРС Холдинг'" |
| `{SubString({$Legal.Name};4)}` | Substring(Организация.Наименование, 4) | "'МРС Холдинг'" |
| `{$Legal.INN}` | Организация.ИНН | "9103069850" |
| `{$DateOfDecree}` | ТекущаяДатаСеанса() | "18.11.2025" |
| `{$RegistrNumber}` | Документ.Номер | "УЦ-000123" |
| `{$TopicOfDecree}` | DecreeHeadline | "Об установке цен..." |
| `{$Aim}` | Preamble | "актуализации прейскуранта" |
| `{$System.Value}` | План обмена → тип системы | "Micros" / "TNG" / "Без заведения в POS-систему" |
| `{$Initiator.FullName}` | ТекущийПользователь().ФизЛицо.Наименование | "Иванов Иван Иванович" |
| `{$MarketPlace.Value}` | План обмена → точка продаж | "Кафе 'Сезон'" |
| `{$TypeOfChange.Value}` | Тип изменения | "Создание" / "Изменение цен" |
| `{$Signatory.FullName}` | Ответственный за подписание | "Петров П.П." |
| `{$SignatoryPosition.Value}` | Должность подписанта | "Генеральный директор" |

**Условные блоки в шаблоне:**
- `{if{$System.Value} = 'Micros'}...{end if}` — блок для Micros
- `{if{$Legal.INN} = '9103069850'}...{else}...{end}` — условие по ИНН организации
- `{for item in {$BlokMicrosCreate}}...{end}` — цикл по позициям меню

**Табличные части в шаблоне:**
1. `{$BlokNoSystemChange}` — изменения без POS-системы
2. `{$BlokMicrosCreate}` — создание новых позиций в Micros
3. `{$BlockTNGSCreate}` — создание в TNG
4. `{$BlockMicroChange}` — изменение позиций в Micros
5. `{$BlokTNGChange}` — изменение позиций в TNG

---

## 7. Алгоритмы ключевых функций

### 7.1 АвторизоватьсяВЕЛМА()

```
Функция АвторизоватьсяВЕЛМА()

1. Получить параметры из РС.ПЛ_ДополнительныеКонстанты:
   - URL = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_URLСервисаЕЛМА")
   - ApplicationToken = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_ApplicationTokenЕЛМА")
   - Логин = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_ИмяПользователяЕЛМА")
   - Пароль = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_ПарольЕЛМА")

2. Сформировать URL запроса:
   - ПолныйURL = URL + "/API/REST/Authorization/LoginWith?username=" + Логин

3. Создать HTTP-запрос:
   - Метод: POST
   - Заголовки:
     * ApplicationToken: {ApplicationToken}
     * Content-Type: text/plain; charset=utf-8
   - Тело: Пароль

4. Выполнить запрос

5. Обработать ответ:
   - ЕСЛИ код = 200:
     * Прочитать AuthToken из JSON ответа
     * Вернуть Структуру {Успех: Истина, Токен: AuthToken}
   - ИНАЧЕ:
     * Прочитать текст ошибки
     * Вернуть Структуру {Успех: Ложь, ТекстОшибки: "..."}

КонецФункции
```

---

### 7.2 СформироватьWordФайлПриказа(ДокументСсылка, ПараметрыПриказа)

```
Функция СформироватьWordФайлПриказа(ДокументСсылка, ПараметрыПриказа)

1. Загрузить шаблон Word:
   - Путь к шаблону из константы или ХранилищаОбщихНастроек
   - ШаблонДокумента = ПолучитьШаблонWord()

2. Получить данные для заполнения:
   - Документ = ДокументСсылка
   - Организация = ПолучитьОрганизациюИзПланаОбмена(Документ.ВидЦены)
   - ТекущееФизЛицо = ПользователиИнформационнойБазы.ТекущийПользователь().ФизическоеЛицо
   - ОтветственныйЗаИсполнение = ПараметрыПриказа.ОтветственныйЗаИсполнение

3. Подготовить структуру данных для замены переменных:
   Данные = Новая Структура;
   Данные.Вставить("LegalName", Организация.Наименование);
   Данные.Вставить("LegalINN", Организация.ИНН);
   Данные.Вставить("DateOfDecree", Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy"));
   Данные.Вставить("RegistrNumber", Документ.Номер);
   Данные.Вставить("TopicOfDecree", DecreeHeadline);
   Данные.Вставить("Aim", "актуализации прейскуранта");
   Данные.Вставить("InitiatorFullName", ТекущееФизЛицо.Наименование);
   // ... остальные переменные

4. Подготовить табличные данные:
   - BlokMicrosCreate = Новый Массив (позиции для создания в Micros)
   - BlockMicroChange = Новый Массив (позиции для изменения в Micros)
   - Для каждой строки документа.ЦеныНоменклатуры:
     * Добавить в соответствующий массив данные о позиции

5. Заполнить шаблон (используя библиотеку заполнения Word):
   - ЗаполненныйДокумент = ЗаполнитьШаблонWord(ШаблонДокумента, Данные)

6. Сохранить во временный файл:
   - ВременныйФайл = ПолучитьИмяВременногоФайла("docx")
   - ЗаполненныйДокумент.Записать(ВременныйФайл)

7. Прочитать как двоичные данные:
   - ДвоичныеДанные = Новый ДвоичныеДанные(ВременныйФайл)

8. Удалить временный файл:
   - УдалитьФайлы(ВременныйФайл)

9. Вернуть ДвоичныеДанные

КонецФункции
```

**Примечание:** Для заполнения Word-шаблона использовать встроенные механизмы 1С (МакетОбластиДокумента) или библиотеку работы с DOCX.

---

### 7.3 СформироватьJSONДляЕЛМА(ДокументСсылка, ПараметрыПриказа)

```
Функция СформироватьJSONДляЕЛМА(ДокументСсылка, ПараметрыПриказа)

1. Прочитать документ УстановкаЦенНоменклатуры

2. Получить Uid организации через план обмена:
   а) Получить ВидЦены из документа (первая строка табличной части)
   б) Найти узел плана обмена питУдаленныеКассы, где ВидЦен = ВидЦены
   в) Получить Организацию из узла плана обмена
   г) Запрос к РС.ПЛ_ДополнительныеКонстанты:
      - ГДЕ Объект = Организация И ИмяКонстанты = "MRS_УИДЕЛМА_Организация"
      - UidОрганизации = Значение

3. Получить Uid инициатора (текущего пользователя):
   а) ТекущийПользователь = ПользователиИнформационнойБазы.ТекущийПользователь()
   б) ФизЛицо = ТекущийПользователь.ФизическоеЛицо
   в) Запрос к РС.ПЛ_ДополнительныеКонстанты:
      - ГДЕ Объект = ФизЛицо И ИмяКонстанты = "MRS_УИДЕЛМА_ФизЛицо"
      - UidИнициатора = Значение

4. Получить Uid ответственного (из ПараметрыПриказа):
   а) ФизЛицоОтветственного = ПараметрыПриказа.ОтветственныйЗаИсполнение
   б) UidОтветственного = ПолучитьUidФизЛицаЕЛМА(ФизЛицоОтветственного)
   в) ЕСЛИ UidОтветственного = Неопределено:
      - Вызвать исключение "Для ответственного не заполнен Uid ЕЛМА"

5. Получить массив Uid для ознакомления (из ПараметрыПриказа):
   а) СписокКОзнакомлению = ПараметрыПриказа.СписокКОзнакомлению
   б) МассивUidДляОзнакомления = ПолучитьМассивUidПользователейЕЛМА(СписокКОзнакомлению)

6. ЕСЛИ UidОрганизации = Неопределено ИЛИ UidИнициатора = Неопределено:
   - Вызвать исключение "Не заполнен Uid ЕЛМА для организации/пользователя в РС.ПЛ_ДополнительныеКонстанты"

7. Сформировать DecreeHeadline:
   - "Об установке цен для " + НаименованиеОрганизации 
     + " с " + Формат(Документ.ДатаНачалаДействия, "ДФ=dd.MM.yyyy")

8. Сформировать DecreeContent:
   - "1. Установить цены согласно приложению №1.\n"
   - "2. Контроль за внесением изменений возложить на финансового контролера.\n"
   - "3. Контроль за исполнением приказа возложить на директора департамента."

9. Сформировать Word-файл:
   - ДвоичныеДанные = СформироватьWordФайлПриказа(ДокументСсылка, ПараметрыПриказа)
   - FileBase64 = Base64Строка(ДвоичныеДанные)

10. Получить константы:
   - ProcessToken = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_ProcessTokenЕЛМА")
   - DecreeTypeUid = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_DecreeTypeУИДЕЛМА")

11. Сформировать структуру JSON:

   Результат = Новая Структура;
   Результат.Вставить("ProcessToken", ProcessToken);
   
   Context = Новая Структура;
   Context.Вставить("Organization", Новая Структура("Uid", UidОрганизации));
   Context.Вставить("Initiator", Новая Структура("Uid", UidИнициатора));
   Context.Вставить("DecreeType", Новая Структура("Uid", DecreeTypeUid));
   Context.Вставить("DecreeHeadline", DecreeHeadline);
   Context.Вставить("Preamble", "В целях актуализации прейскуранта");
   Context.Вставить("DecreeContent", DecreeContent);
   Context.Вставить("StartDate", XMLСтрока(Документ.ДатаНачалаДействия)); // ISO 8601
   
   ЕСЛИ UidОтветственного <> Неопределено:
     Context.Вставить("DecreeResponsible", Новая Структура("Uid", UidОтветственного));
   КонецЕсли;
   
   ЕСЛИ МассивUidДляОзнакомления.Количество() > 0:
     Context.Вставить("NotificationUsr", МассивUidДляОзнакомления);
   КонецЕсли;
   
   Context.Вставить("FileDecree", FileBase64);
   
   Результат.Вставить("Context", Context);

12. Вернуть Результат

КонецФункции
```

---

### 7.4 ОбработатьОчередьОтправки()

```
Функция ОбработатьОчередьОтправки()

1. Авторизация в ЕЛМА:
   - РезультатАвторизации = АвторизоватьсяВЕЛМА()
   - ЕСЛИ НЕ РезультатАвторизации.Успех:
     * ЗаписатьЛог("Ошибка авторизации в ЕЛМА")
     * Возврат Новая Структура("Отправлено", 0, "Ошибок", 0)
   - AuthToken = РезультатАвторизации.Токен

2. Выборка документов из очереди:
   - ВЫБРАТЬ * ИЗ РС.MRS_ПроцессыЕЛМА
   - ГДЕ Статус = "ВОчереди" 
     ИЛИ (Статус = "Ошибка" И КоличествоПопыток < 3)
   - УПОРЯДОЧИТЬ ПО ДатаПоследнейПопытки
   - ПЕРВЫЕ 10

3. Для каждого документа в выборке:
   
   а) Получить данные документа:
      - ДокументСсылка = Запись.Документ1С
   
   б) Сформировать JSON:
      - JSONДанные = СформироватьJSONДляЕЛМА(ДокументСсылка)
      - JSONТекст = ЗаписатьJSON(JSONДанные)
   
   в) Определить URL:
      - URLСервиса = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_URLСервисаЕЛМА")
      - URL = URLСервиса + "/API/REST/Process/StartWithData"
   
   г) Выполнить POST запрос:
      - Метод: POST
      - Заголовки:
        * AuthToken: {AuthToken}
        * Content-Type: application/json
      - Тело: JSONТекст
      - Таймаут: ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_ТаймаутЗапросаЕЛМА")
   
   д) Обработать ответ:
      
      ЕСЛИ КодОтвета = 200:
        - ОтветJSON = ПрочитатьJSON(ТелоОтвета)
        - УИДЕЛМА = ОтветJSON.Uid ИЛИ ОтветJSON.ProcessId
        - Обновить запись в регистре:
          * УИДЕЛМА = УИДЕЛМА
          * Статус = "НаСогласовании"
          * ДатаОтправки = ТекущаяДата()
          * ТекстОшибки = ""
          * КоличествоПопыток = 0
        - СчетчикОтправлено += 1
      
      ИНАЧЕ:
        - ТекстОшибки = "Код " + КодОтвета + ": " + ТелоОтвета
        - Обновить запись в регистре:
          * КоличествоПопыток += 1
          * ТекстОшибки = ТекстОшибки
          * ДатаПоследнейПопытки = ТекущаяДата()
          * ЕСЛИ КоличествоПопыток >= 3:
            - Статус = "ТребуетВнимания"
          ИНАЧЕ:
            - Статус = "Ошибка"
        - СчетчикОшибок += 1
      
      КонецЕсли

4. Вернуть статистику:
   - Новая Структура("Отправлено", СчетчикОтправлено, "Ошибок", СчетчикОшибок)

КонецФункции
```

---

### 7.5 ОпроситьСтатусыДокументов()

```
Функция ОпроситьСтатусыДокументов()

1. Авторизация в ЕЛМА:
   - AuthToken = АвторизоватьсяВЕЛМА().Токен

2. Выборка документов для опроса:
   - ВЫБРАТЬ * ИЗ РС.MRS_ПроцессыЕЛМА
   - ГДЕ УИДЕЛМА <> ""
     И Статус НЕ В ("Подписан", "Отклонен", "Отменен")
     И (ДатаПоследнегоОпроса < ТекущаяДата() - 5 минут ИЛИ ДатаПоследнегоОпроса = NULL)

3. Для каждого документа:
   
   а) Получить УИДЕЛМА:
      - УИДЕЛМА = Запись.УИДЕЛМА
   
   б) Запросить статус из ЕЛМА:
      - DecreeTypeUid = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту("MRS_DecreeTypeУИДЕЛМА")
      - URL = URLСервиса + "/API/REST/Entity/Load?type=" + DecreeTypeUid + "&id=" + УИДЕЛМА
      - GET запрос с заголовком AuthToken
   
   в) Обработать ответ:
      
      ЕСЛИ КодОтвета = 200:
        - ОтветJSON = ПрочитатьJSON(ТелоОтвета)
        - СтатусЕЛМА = ОтветJSON.WorkflowInstance.Status (или аналогичное поле)
        - КомментарийЕЛМА = ОтветJSON.ApprovementResult.Comment (если есть)
        
        - Преобразовать статус:
          * Статус1С = ПреобразоватьСтатусЕЛМАВСтатус1С(СтатусЕЛМА)
        
        - Обновить запись в регистре:
          * СтатусЕЛМА = СтатусЕЛМА
          * Статус = Статус1С
          * ДатаПоследнегоОпроса = ТекущаяДата()
          * ЕСЛИ КомментарийЕЛМА <> "":
            - КомментарийЕЛМА = КомментарийЕЛМА
        
        - СчетчикОбновлено += 1
      
      ИНАЧЕ:
        - ЗаписатьЛог("Ошибка опроса статуса для " + УИДЕЛМА)
        - СчетчикОшибок += 1
      
      КонецЕсли

4. Вернуть статистику:
   - Новая Структура("Обновлено", СчетчикОбновлено, "Ошибок", СчетчикОшибок)

КонецФункции
```

---

### 7.6 ПровестиДокументыПоДатеПрименения()

```
Функция ПровестиДокументыПоДатеПрименения()

1. Выборка документов для проведения:
   - ВЫБРАТЬ 
       РС.Документ1С КАК Документ,
       Док.ДатаНачалаДействия КАК ДатаПрименения
     ИЗ РС.MRS_ПроцессыЕЛМА КАК РС
     ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.УстановкаЦенНоменклатуры КАК Док
       ПО РС.Документ1С = Док.Ссылка
   - ГДЕ РС.Статус = "Подписан"
     И Док.ДатаНачалаДействия <= ТекущаяДата()
     И НЕ Док.Проведен

2. Для каждого документа:
   
   а) ДокументОбъект = Документ.ПолучитьОбъект()
   
   б) Попытка:
      - ДокументОбъект.Записать(РежимЗаписиДокумента.Проведение)
      - СчетчикПроведено += 1
      
      в) Выгрузить цены в Simphony:
         - ВыгрузитьЦеныВSimphony(Документ.Ссылка)
   
   Исключение:
      - ЗаписатьЛог("Ошибка проведения документа: " + ОписаниеОшибки())
      - СчетчикОшибок += 1
   КонецПопытки

3. Вернуть статистику:
   - Новая Структура("Проведено", СчетчикПроведено, "Ошибок", СчетчикОшибок)

КонецФункции
```

---

### 7.7 ВыгрузитьЦеныВSimphony(ДокументСсылка)

```
Функция ВыгрузитьЦеныВSimphony(ДокументСсылка)

1. Проверить условия:
   - ЕСЛИ НЕ ДокументСсылка.Проведен:
     * Возврат Ложь
   - ЕСЛИ ПолучитьСтатусДокумента(ДокументСсылка) <> "Подписан":
     * Возврат Ложь

2. Получить виды цен из документа:
   - ВидыЦенДокумента = Новый Массив
   - Для каждой строки в ДокументСсылка.ЦеныНоменклатуры:
     * ЕСЛИ ВидыЦенДокумента.Найти(Строка.ВидЦены) = Неопределено:
       - ВидыЦенДокумента.Добавить(Строка.ВидЦены)

3. Найти кассовые узлы с этими видами цен:
   - Запрос:
     ВЫБРАТЬ
       питУдаленныеКассы.Ссылка,
       питУдаленныеКассы.ВидЦен,
       питУдаленныеКассыВидыМеню.ВидМеню
     ИЗ ПланОбмена.питУдаленныеКассы КАК питУдаленныеКассы
       ВНУТРЕННЕЕ СОЕДИНЕНИЕ питУдаленныеКассыВидыМеню
     ГДЕ питУдаленныеКассы.ВидЦен В (&ВидыЦен)

4. Для каждого кассового узла:
   
   а) Получить позиции меню для данного вида меню:
      - Запрос:
        ВЫБРАТЬ питМеню.Ссылка, питМеню.Номенклатура
        ИЗ Справочник.питМеню
        ГДЕ питМеню.Владелец = &ВидМеню
          И НЕ питМеню.ЭтоГруппа
   
   б) Для каждой позиции меню:
      - Проверить наличие цены в документе для этой номенклатуры
      - ЕСЛИ найдена:
        * MRS_ИнтеграцияSimphonyСервер.ПоставитьВОчередьВыгрузки(
            ПозицияМеню,
            КассовыйУзел
          )

5. Возврат Истина

КонецФункции
```

---

### 7.8 Вспомогательные функции получения UID

#### ПолучитьUidОрганизацииИзПланаОбмена(ВидЦены)

```
Функция ПолучитьUidОрганизацииИзПланаОбмена(ВидЦены)

1. Запрос к плану обмена:
   - ВЫБРАТЬ питУдаленныеКассы.Организация
   - ИЗ ПланОбмена.питУдаленныеКассы КАК питУдаленныеКассы
   - ГДЕ питУдаленныеКассы.ВидЦен = &ВидЦены
   - ПЕРВЫЕ 1

2. ЕСЛИ НЕ найдено:
   - Вызвать исключение "Не найден узел плана обмена питУдаленныеКассы для ВидаЦены"

3. Организация = Выборка.Организация

4. Получить Uid из РС.ДополнительныеКонстанты:
   - Запрос: ГДЕ Объект = Организация И ИмяКонстанты = "MRS_УИДЕЛМА_Организация"

5. ЕСЛИ НЕ найдено:
   - Вызвать исключение "Для организации " + Организация.Наименование + " не заполнен Uid ЕЛМА в РС.ДополнительныеКонстанты"

6. Вернуть Uid

КонецФункции
```

---

#### ПолучитьUidФизЛицаЕЛМА(ФизЛицоСсылка)

```
Функция ПолучитьUidФизЛицаЕЛМА(ФизЛицоСсылка)

1. ЕСЛИ ФизЛицоСсылка = Неопределено ИЛИ ФизЛицоСсылка.Пустая():
   - Вернуть Неопределено

2. Получить Uid из РС.ДополнительныеКонстанты:
   - Uid = ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту(
       "MRS_УИДЕЛМА_ФизЛицо", 
       ФизЛицоСсылка
     )

3. ЕСЛИ Uid = Неопределено:
   - ЗаписатьПредупреждение("Для физлица " + ФизЛицоСсылка + " не заполнен Uid ЕЛМА")
   - Вернуть Неопределено

4. Вернуть Uid

КонецФункции
```

---

#### ПолучитьUidТекущегоПользователяЕЛМА()

```
Функция ПолучитьUidТекущегоПользователяЕЛМА()

1. ТекущийПользователь = ПользователиИнформационнойБазы.ТекущийПользователь()

2. ЕСЛИ НЕ ТекущийПользователь.Свойства().ФизическоеЛицо:
   - Вызвать исключение "У пользователя не заполнен реквизит ФизическоеЛицо"

3. ФизЛицо = ТекущийПользователь.ФизическоеЛицо

4. ЕСЛИ ФизЛицо = Неопределено ИЛИ ФизЛицо.Пустая():
   - Вызвать исключение "У текущего пользователя не заполнено ФизическоеЛицо"

5. Uid = ПолучитьUidФизЛицаЕЛМА(ФизЛицо)

6. ЕСЛИ Uid = Неопределено:
   - Вызвать исключение "Для физлица " + ФизЛицо + " не заполнен Uid ЕЛМА в РС.ДополнительныеКонстанты"

7. Вернуть Uid

КонецФункции
```

---

#### ПолучитьМассивUidПользователейЕЛМА(МассивПользователей1С)

```
Функция ПолучитьМассивUidПользователейЕЛМА(МассивПользователей1С)

1. МассивРезультат = Новый Массив

2. Для каждого Пользователя Из МассивПользователей1С:
   
   а) ФизЛицо = Пользователя.ФизическоеЛицо
   
   б) ЕСЛИ ФизЛицо = Неопределено ИЛИ ФизЛицо.Пустая():
      - ЗаписатьПредупреждение("У пользователя " + Пользователя + " не заполнено ФизическоеЛицо")
      - Продолжить
   
   в) Uid = ПолучитьUidФизЛицаЕЛМА(ФизЛицо)
   
   г) ЕСЛИ Uid <> Неопределено:
      - МассивРезультат.Добавить(Новая Структура("Uid", Uid))

3. Вернуть МассивРезультат

КонецФункции
```

---

## 8. Тестовые сценарии

### ⬜ ТС-1: Создание и отправка нового приказа (Happy Path)

**Шаги:**
1. Создать документ "Установка цен номенклатуры"
   - Организация: ООО "Тестовая" (с заполненным MRS_УИДЕЛМА)
   - Дата начала действия: +14 дней от текущей
   - Табличная часть: 10 позиций с ценами
2. Сохранить документ (без проведения)
3. Нажать команду "Отправить в ЕЛМА" (доступна администратору)
4. Подтвердить отправку

**Ожидаемый результат:**
- ✅ Сообщение: "Документ поставлен в очередь на отправку"
- ✅ Запись в MRS_ПроцессыЕЛМА со статусом "ВОчереди"
- ✅ Через 5 мин РЗ отправляет в ЕЛМА
- ✅ Статус изменен на "НаСогласовании"
- ✅ УИДЕЛМА заполнен
- ✅ На форме документа отображается статус "На согласовании"

---

### ⬜ ТС-2: Согласование и подписание в ЕЛМА

**Предусловие:** Документ отправлен (ТС-1)

**Шаги:**
1. В ЕЛМА согласователи одобряют приказ (1-2 этапа согласования)
2. Дождаться опроса статусов РЗ (до 5 мин)
3. В ЕЛМА руководитель подписывает приказ
4. Дождаться опроса статусов РЗ

**Ожидаемый результат:**
- ✅ После согласования: статус в 1С = "Согласован"
- ✅ После подписания: статус в 1С = "Подписан"
- ✅ Документ остается непроведенным (дата применения в будущем)
- ✅ На форме отображается статус "Подписан", дата подписания

---

### ⬜ ТС-3: Автоматическое проведение при наступлении даты

**Предусловие:** Документ подписан (ТС-2), дата применения наступила

**Шаги:**
1. Изменить системное время на дату начала действия цен (или дождаться)
2. Дождаться выполнения РЗ (до 5 мин)

**Ожидаемый результат:**
- ✅ Документ автоматически проведен
- ✅ Записи в регистре ЦеныНоменклатуры созданы
- ✅ Позиции меню поставлены в очередь Simphony (MRS_ОчередьВыгрузкиМенюSimphony)
- ✅ Цены выгружены в Simphony в течение 5 мин

---

### ⬜ ТС-4: Отклонение приказа в ЕЛМА

**Предусловие:** Документ отправлен (ТС-1)

**Шаги:**
1. В ЕЛМА согласователь отклоняет приказ
2. Добавляет комментарий: "Цены завышены, пересмотреть"
3. Дождаться опроса статусов РЗ (до 5 мин)
4. Открыть документ в 1С

**Ожидаемый результат:**
- ✅ Статус в 1С = "Отклонен"
- ✅ КомментарийЕЛМА заполнен: "Цены завышены, пересмотреть"
- ✅ На форме документа статус отображается красным цветом
- ✅ Документ остается непроведенным
- ✅ Выгрузка в Simphony НЕ происходит

---

### ⬜ ТС-5: Ошибка при отправке (Retry Logic)

**Шаги:**
1. Временно изменить URL ЕЛМА на неверный (симуляция недоступности)
2. Создать и отправить документ
3. Дождаться выполнения РЗ (1-я попытка)
4. Дождаться еще 2 попытки (каждые 5 мин)
5. Проверить запись в регистре

**Ожидаемый результат:**
- ✅ После 1-й попытки: Статус = "Ошибка", КоличествоПопыток = 1
- ✅ ТекстОшибки заполнен (например, "Connection timeout")
- ✅ После 2-й попытки: КоличествоПопыток = 2
- ✅ После 3-й попытки: Статус = "ТребуетВнимания", КоличествоПопыток = 3
- ✅ Дальнейшие автоматические попытки НЕ выполняются

**Восстановление:**
1. Исправить URL ЕЛМА
2. Через обработку "Мониторинг интеграции" повторить отправку вручную
3. Документ успешно отправлен

---

### ⬜ ТС-6: Отмена приказа при удалении документа

**Предусловие:** Документ отправлен в ЕЛМА (ТС-1)

**Шаги:**
1. Пометить документ на удаление
2. Подтвердить

**Ожидаемый результат:**
- ✅ Вызвана функция `ОтменитьПриказВЕЛМА()`
- ✅ В ЕЛМА отправлен запрос на отмену (POST Update с Status="Cancelled")
- ✅ Статус в регистре MRS_ПроцессыЕЛМА = "Отменен"
- ✅ Если документ был проведен: снят с проведения

---

### ⬜ ТС-7: Отсутствие Uid в РС.ДополнительныеКонстанты

**Шаги:**
1. Создать документ "Установка цен"
2. В документе указать ВидЦены, для которого в плане обмена питУдаленныеКассы есть организация
3. Для этой организации НЕ заполнена запись в РС.ДополнительныеКонстанты с ИмяКонстанты = "MRS_УИДЕЛМА_Организация"
4. Попытаться отправить в ЕЛМА

**Ожидаемый результат:**
- ✅ Валидация не пройдена
- ✅ Сообщение: "Для организации '{Наименование}' не заполнен Uid ЕЛМА. Добавьте запись в РС.ПЛ_ДополнительныеКонстанты с ИмяКонстанты='MRS_УИДЕЛМА_Организация' и Объект=Организация."
- ✅ Документ НЕ поставлен в очередь

**Альтернативный сценарий (отсутствие Uid физлица):**
1. Для текущего пользователя НЕ заполнено ФизическоеЛицо
2. ИЛИ для ФизическогоЛица НЕ заполнена запись в РС.ДополнительныеКонстанты
3. Сообщение: "Для пользователя '{Имя}' не заполнен Uid ЕЛМА. Проверьте заполнение ФизическогоЛица и запись в РС.ПЛ_ДополнительныеКонстанты."

---

## 9. Чек-лист внедрения

### ⬜ Фаза 1: Подготовка метаданных (0.5 дня)

- [ ] Создать перечисление MRS_СтатусыСинхронизацииЕЛМА (8 значений)
- [ ] Создать регистр сведений MRS_ПроцессыЕЛМА (1 измерение, 9 ресурсов, 2 индекса)
- [ ] Добавить записи в РС.ПЛ_ДополнительныеКонстанты (7 записей для параметров подключения)

**Примечание:** UID организаций и физлиц хранятся в РС.ДополнительныеКонстанты (не требуется доработка справочников)

---

### ⬜ Фаза 2: Разработка общих модулей (4 дня)

**ОМ.MRS_ИнтеграцияЕЛМАСервер:**
- [ ] Функция ВыполнитьИнтеграциюЕЛМА() (главная для РЗ)
- [ ] Функция АвторизоватьсяВЕЛМА()
- [ ] Функция ПоставитьДокументВОчередьЕЛМА()
- [ ] Функция ОбработатьОчередьОтправки()
- [ ] Функция ОпроситьСтатусыДокументов()
- [ ] Функция ПровестиДокументыПоДатеПрименения()
- [ ] Функция СформироватьJSONДляЕЛМА()
- [ ] Функция СформироватьWordФайлПриказа()
- [ ] Функция ВыгрузитьЦеныВSimphony()
- [ ] Функция ПолучитьUidОрганизацииЕЛМА()
- [ ] Функция ПолучитьUidПользователяЕЛМА()
- [ ] Функция ПолучитьМассивUidПользователейЕЛМА()
- [ ] Прочие вспомогательные функции (10+ функций)

**ОМ.MRS_ИнтеграцияЕЛМАКлиент:**
- [ ] Процедура КомандаОтправитьВЕЛМА()
- [ ] Процедура ПоказатьИсториюИнтеграцииЕЛМА()
- [ ] Процедура ОбновитьСтатусНаФорме()

**ОМ.MRS_ИнтеграцияЕЛМАКлиентСервер:**
- [ ] Функция ПриСозданииНаСервере() — программная модификация формы
- [ ] Функция ПолучитьТекстСтатусаЕЛМА()
- [ ] Функция ПолучитьЦветСтатусаЕЛМА()
- [ ] Функция ВалидироватьДокументПередОтправкой()

**ОМ.MRS_ИнтеграцияЕЛМАКлиентПодключаемый:**
- [ ] Процедура КомандаОтправитьВЕЛМА()
- [ ] Процедура КомандаОбновитьСтатусЕЛМА()
- [ ] Процедура ОбработчикОжиданияОбновлениеСтатуса()

**ОМ.MRS_ИнтеграцияЕЛМАПодписки:**
- [ ] Процедура УстановкаЦенНоменклатуры_ПередЗаписью()
- [ ] Процедура УстановкаЦенНоменклатуры_ПриУстановкеПометкиУдаления()
- [ ] Создать подписки на события в конфигураторе

---

### ⬜ Фаза 3: Регламентное задание и доработка форм (1 день)

- [ ] Создать РЗ MRS_ИнтеграцияЕЛМА (расписание: каждые 5 мин)
- [ ] Настроить использование РЗ
- [ ] Добавить вызов в ОМ.МодификацияКонфигурацииПереопределяемый для формы УстановкаЦенНоменклатуры
- [ ] Проверить программное создание элементов формы (команды, поля, группы)

---

### ⬜ Фаза 4: Настройка констант и справочников (0.5 дня)

**Заполнение констант в РС.ПЛ_ДополнительныеКонстанты:**
- [ ] MRS_URLСервисаЕЛМА = `http://10.21.180.147:8000` (preprod)
- [ ] MRS_ApplicationTokenЕЛМА = (получить от команды ЕЛМА)
- [ ] MRS_ИмяПользователяЕЛМА = (логин интеграционного пользователя)
- [ ] MRS_ПарольЕЛМА = (пароль, зашифрованный)
- [ ] MRS_ProcessTokenЕЛМА = `262d1c28-1711-4e9d-b65c-65d8ed8c0d68`
- [ ] MRS_DecreeTypeУИДЕЛМА = `571c78cc-baa0-4529-a00f-8e6c65790ae7`
- [ ] MRS_ТаймаутЗапросаЕЛМА = `30`

**Заполнение Uid в РС.ДополнительныеКонстанты:**
- [ ] Для каждой Организации, участвующей в плане обмена питУдаленныеКассы:
  - Создать запись: Объект=Организация, ИмяКонстанты="MRS_УИДЕЛМА_Организация", Значение="{GUID из ЕЛМА}"
- [ ] Для каждого ФизическогоЛица пользователей, которые будут работать с интеграцией:
  - Создать запись: Объект=ФизЛицо, ИмяКонстанты="MRS_УИДЕЛМА_ФизЛицо", Значение="{GUID из ЕЛМА}"
- [ ] Проверить заполнение реквизита ФизическоеЛицо у всех пользователей

---

### ⬜ Фаза 5: Тестирование (2 дня)

- [ ] ТС-1: Создание и отправка нового приказа
- [ ] ТС-2: Согласование и подписание в ЕЛМА
- [ ] ТС-3: Автоматическое проведение при наступлении даты
- [ ] ТС-4: Отклонение приказа в ЕЛМА
- [ ] ТС-5: Ошибка при отправке (Retry Logic)
- [ ] ТС-6: Отмена приказа при удалении документа
- [ ] ТС-7: Отсутствие Uid в справочниках

**Интеграционное тестирование:**
- [ ] Проверить выгрузку цен в Simphony после проведения документа
- [ ] Проверить корректность формирования Word-файла
- [ ] Проверить работу при большом объеме документов в очереди (10+)

---

### ⬜ Фаза 6: Документация и обучение (1 день)

- [ ] Создать инструкцию для администраторов:
  - Настройка констант
  - Заполнение Uid организаций/пользователей
  - Мониторинг очереди
  - Ручная переотправка при ошибках
- [ ] Создать инструкцию для пользователей:
  - Как создать документ "Установка цен"
  - Как отправить в ЕЛМА
  - Как отслеживать статус
  - Что делать при отклонении
- [ ] Провести обучение ключевых пользователей

---

### ⬜ Фаза 7: Запуск в prod (0.5 дня)

- [ ] Переключить MRS_URLСервисаЕЛМА на prod: `https://elma.company.ru`
- [ ] Обновить токены для prod-окружения
- [ ] Провести smoke-тестирование на prod
- [ ] Мониторинг работы в течение первых 3 дней

---

## 📊 Общая оценка трудоемкости

| Фаза | Трудоемкость | Статус |
|------|--------------|--------|
| Фаза 1: Метаданные | 0.5 дня | ⬜ |
| Фаза 2: Общие модули | 4 дня | ⬜ |
| Фаза 3: РЗ и формы | 1 день | ⬜ |
| Фаза 4: Настройка | 0.5 дня | ⬜ |
| Фаза 5: Тестирование | 2 дня | ⬜ |
| Фаза 6: Документация | 1 день | ⬜ |
| Фаза 7: Prod-запуск | 0.5 дня | ⬜ |
| **ИТОГО** | **9.5 дней** | **0%** |

---

## 10. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Несоответствие API ЕЛМА документации | Средняя | Высокое | Тестирование на preprod, тесная коммуникация с командой ЕЛМА |
| Проблемы с формированием Word-файлов | Средняя | Среднее | Использовать готовые механизмы 1С, упростить шаблон |
| Отсутствие Uid для организаций/пользователей | Высокая | Высокое | Заранее подготовить таблицу соответствий, обязательная валидация |
| Блокировки документов при проведении | Низкая | Среднее | Использовать управляемые блокировки, обработка ошибок |
| Расхождение статусов 1С ↔ ЕЛМА | Средняя | Среднее | Регулярный опрос (каждые 5 мин), логирование всех операций |
| Большой объем документов в очереди | Низкая | Низкое | Ограничение 10 записей за раз, приоритезация |
| Изменение ProcessToken или DecreeTypeUid в ЕЛМА | Низкая | Высокое | Хранение в константах, легкое изменение без доработки кода |

---

## 11. Ответы на вопросы (согласовано с заказчиком)

1. ✅ **DecreeResponsible и NotificationUsr:** Заполняются через **специальную форму** при проведении документа. Форма открывается автоматически, если ВидЦены привязан к питУдаленныеКассы.
   - Поля формы:
     * Ответственный за исполнение (СправочникСсылка.ФизическиеЛица)
     * Список к ознакомлению (ТабличнаяЧасть с ФизическимиЛицами)

2. ✅ **Формат файла FileDecree:** DOCX (Microsoft Word), генерируется из **готового шаблона** с переменными типа `{$Legal.Name}`, `{$DateOfDecree}`, `{$MarketPlace.Value}` и т.д.

3. ✅ **StartDate:** Используется `ТекущаяДатаСеанса()` (локальное время сервера 1С, не UTC).

4. ⚠️ **Статусы процесса ЕЛМА:** Пока детальный перечень не определен. Будет уточнено позже при интеграционном тестировании.

5. ✅ **Заполнение Uid:** Реализовано через **другой модуль** (вне scope данного проекта).

6. ✅ **Повторная отправка:** **Создавать новый процесс** в ЕЛМА (если статус "Отклонен" или "Согласован"). Каждая отправка = новый процесс.

7. ✅ **Отсутствие Uid:** **Блокировать отправку**. Выдавать ошибку:
   - В ЖурналРегистрации
   - На клиенте (пользователю)
   - Не ставить в очередь

7a. ✅ **Множественные виды цен:** Такого случая **не может быть**. В документе всегда один ВидЦены → одна Организация.

8. ✅ **Частота опроса:** Настраивается вручную администратором в параметрах регламентного задания.

9. ✅ **Безопасность:** 
   - Команда "Отправить в ЕЛМА" доступна только **администратору**
   - Фактическую отправку выполняет **регламентное задание** (не пользователь)

10. ✅ **Уведомления:** При смене статуса отправлять email на адрес из **ФизЛицо.КонтактнаяИнформация** (email) пользователя, который создал документ "Установка цен".

---

## 12. Контакты и ответственные

**Product Manager:** [Имя]  
**Senior 1C Developer:** [Имя]  
**Tech Lead ЕЛМА:** [Имя]  
**QA Engineer:** [Имя]

**Дата готовности PRD:** 18.11.2025  
**Планируемая дата запуска:** [Дата]

---

## 13. История изменений

| Версия | Дата | Автор | Изменения |
|--------|------|-------|-----------|
| 2.2 | 18.11.2025 | PM | **Согласованы все открытые вопросы.** Добавлена форма параметров приказа (ОтветственныйЗаИсполнение, СписокКОзнакомлению). Описан шаблон Word с переменными. Уточнен процесс: форма открывается при проведении документа (если ВидЦены привязан к питУдаленныеКассы). Email-уведомления при смене статуса. StartDate = ТекущаяДатаСеанса(). |
| 2.1 | 18.11.2025 | PM | **Уточнение архитектуры:** UID хранятся в РС.ДополнительныеКонстанты (не в реквизитах справочников). Организация получается через план обмена питУдаленныеКассы. Использование функции `ПЛ_ДополнительныеКонстантыПовтИсп.ВернутьДополнительнуюКонстанту()` |
| 2.0 | 18.11.2025 | PM | Консолидация PRD + Implementation Plan + TODO в единый документ |
| 1.1 | 18.11.2025 | PM | Обновление структуры API (ProcessToken, Context, новые поля) |
| 1.0 | 14.11.2025 | PM | Первая версия PRD |

---

**© 2025 | PRD + Implementation Plan v2.2 | Статус: 📋 Ready for Development**
