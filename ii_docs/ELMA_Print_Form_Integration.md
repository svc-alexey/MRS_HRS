# Интеграция печатной формы приказа ЕЛМА

## Описание

Реализована функциональность формирования Word-документа приказа для отправки в ЕЛМА с использованием БСП (Библиотека Стандартных Подсистем).

## Как это работает

1. Пользователь нажимает кнопку **"Печать приказа ЕЛМА"** в форме документа
2. На сервере формируется Word-документ:
   - Заполняются теги `{v8 Параметр}` в шапке
   - Заполняется таблица с товарами (по закладке `СтрокаТаблицы`)
3. Документ открывается для просмотра пользователю
4. Далее документ будет отправляться в ЕЛМА (TODO)

## Требования к Word-макету

### 1. Теги для параметров шапки

Используйте теги формата `{v8 ИмяПараметра}` прямо в тексте документа:

```
ПРИКАЗ № {v8 НомерПриказа} от {v8 СсылкаДата}

Организация: {v8 Организация}
Тема: {v8 TopicOfDecree}
Дата начала действия: {v8 ДатаНачалаДействия}
```

**Доступные параметры:**
- `{v8 Организация}` - название организации
- `{v8 СсылкаДата}` - дата документа
- `{v8 НомерПриказа}` - номер приказа
- `{v8 TopicOfDecree}` - тема приказа
- `{v8 ДатаНачалаДействия}` - дата начала действия цен

### 2. Закладка для таблицы товаров

**ОБЯЗАТЕЛЬНО!** Для строки таблицы нужна закладка `СтрокаТаблицы`:

1. Создайте таблицу в Word:
```
| Точка продажи           | Товар                     | Тип изменения       | Цена                | Название                  | НДС                 |
|-------------------------|---------------------------|---------------------|---------------------|---------------------------|---------------------|
| {v8 ТочкаПродажи}       | {v8 ТоварыНоменклатура}   | {v8 ТипИзменения}   | {v8 НоваяЦена}      | {v8 НовоеНазвание}        | {v8 СтавкаНДС}      |
```

2. **Выделите всю последнюю строку** (со всеми ячейками)
3. **Вставка** → **Закладка** (Insert → Bookmark)
4. Имя закладки: **`СтрокаТаблицы`** (точно так, с большой буквы!)
5. Нажмите **Добавить**

**Теги для строки таблицы:**
- `{v8 ТочкаПродажи}` - точка продажи (пока пусто)
- `{v8 ТоварыНоменклатура}` - название товара
- `{v8 ТипИзменения}` - "Установка цен"
- `{v8 НоваяЦена}` - новая цена
- `{v8 НовоеНазвание}` - новое название
- `{v8 СтавкаНДС}` - ставка НДС (пока пусто)

## Размещение макета

### Вариант 1: РС.ПользовательскиеМакетыПечати (рекомендуется)

1. Откройте `РС.ПользовательскиеМакетыПечати`
2. Создайте новую запись:
   - **Объект**: `Документ.УстановкаЦенНоменклатуры`
   - **Имя макета**: `ПриказЕЛМА`
   - **Использование**: `Истина`
   - **Макет**: загрузите ваш DOCX-файл
3. Сохраните

### Вариант 2: Метаданные (для расширений)

1. В конфигураторе перейдите к `Документы` → `УстановкаЦенНоменклатуры`
2. Добавьте макет (правая кнопка → Добавить → Макет)
3. Имя: **`ПриказЕЛМА`**
4. Тип: **Офисный документ Word**
5. Загрузите DOCX-файл

## Измененные файлы

### CommonModules/MRS_ИнтеграцияЕЛМАСервер/Ext/Module.bsl

**Функция `СформироватьПриказЕЛМА(СсылкаДокумента)`:**

- Получает макет Word из метаданных или РС.ПользовательскиеМакетыПечати
- Инициализирует печатную форму через БСП
- Заполняет параметры шапки методом `УправлениеПечатью.ЗаполнитьПараметры()`
- Заполняет таблицу товаров по закладке `СтрокаТаблицы`
- Возвращает `ДвоичныеДанные` Word-файла

### CommonModules/MRS_ИнтеграцияЕЛМАКлиент/Ext/Module.bsl

**Процедура `КомандаПечатьПриказаЕЛМА(Форма)`:**

- Проверяет, что документ записан
- Вызывает серверную функцию формирования
- Помещает результат во временное хранилище
- Открывает Word-документ для просмотра пользователю

## Использование

1. Создайте Word-макет с тегами и закладкой
2. Загрузите макет в РС.ПользовательскиеМакетыПечати или метаданные
3. Откройте документ `УстановкаЦенНоменклатуры`
4. Заполните товары и цены
5. Нажмите кнопку **"Печать приказа ЕЛМА"**
6. Проверьте сформированный Word-документ

## Особенности реализации

### Преимущества подхода БСП

✅ **Не требуется COM** - работает на любой платформе
✅ **Простое заполнение** - теги `{v8 Параметр}` заполняются автоматически
✅ **Гибкость** - макет легко менять без изменения кода
✅ **Стандартный механизм** - используется подсистема УправлениеПечатью из БСП

### Логика заполнения

1. **Шапка** - БСП заменяет все теги `{v8 Параметр}` на значения из структуры `Данные`
2. **Таблица** - БСП находит закладку `СтрокаТаблицы`, копирует её для каждого элемента массива `МассивСтрок`, заполняя теги

### Что заполняется автоматически

- Организация (из плана обмена через вид цены)
- Дата и номер документа
- Дата начала действия (из параметров приказа)
- Список товаров с ценами

### TODO (требуется доработка)

- `ТочкаПродажи` - получать из вида цены
- `СтавкаНДС` - получать из номенклатуры

## Устранение проблем

### Ошибка: "Макет печати не найден"

**Причина:** Макет не создан или неправильно назван.

**Решение:**
1. Проверьте наличие макета `ПриказЕЛМА` в РС.ПользовательскиеМакетыПечати
2. Проверьте, что объект = `Документ.УстановкаЦенНоменклатуры`
3. Проверьте, что флаг "Использование" = `Истина`

### Ошибка: "Не удалось сформировать приказ"

**Причина:** Отсутствует закладка `СтрокаТаблицы` в Word-макете.

**Решение:**
1. Откройте Word-макет
2. Выделите последнюю строку таблицы (полностью!)
3. Вставка → Закладка → имя `СтрокаТаблицы`
4. Сохраните и перезагрузите макет

### Данные не заполняются

**Причина:** Неправильные имена тегов.

**Решение:**
1. Проверьте написание тегов: `{v8 Параметр}` (v8 маленькими!)
2. Имена параметров чувствительны к регистру
3. Список доступных параметров см. выше

## Дальнейшие шаги

1. ✅ Формирование Word-документа - **ГОТОВО**
2. ✅ Показ документа пользователю - **ГОТОВО**
3. ⏳ Отправка документа в ЕЛМА через API - **TODO**
4. ⏳ Получение ответа от ЕЛМА - **TODO**
5. ⏳ Отображение результата пользователю - **TODO**

## Пример макета

Минимальный рабочий пример Word-документа:

```
ПРИКАЗ № {v8 НомерПриказа} от {v8 СсылкаДата}

Организация: {v8 Организация}

Установить цены с {v8 ДатаНачалаДействия}

┌─────────────────┬──────────────┬────────┐
│ Товар           │ Цена         │ Тип    │
├─────────────────┼──────────────┼────────┤
│ {v8 ТоварыНоменклатура} │ {v8 НоваяЦена} │ {v8 ТипИзменения} │  ← эту строку выделить и создать закладку "СтрокаТаблицы"
└─────────────────┴──────────────┴────────┘
```

Вот и всё! 🎉
